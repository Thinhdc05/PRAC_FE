# 📘 SỔ TAY ÔN TẬP JAVASCRIPT TOÀN DIỆN (W3SCHOOLS & PHỎNG VẤN SENIOR)

> Sổ tay tổng hợp toàn bộ kiến thức JavaScript từ nền tảng đến chuyên sâu, mổ xẻ cơ chế hoạt động của Browser Engine & V8 Engine, bộ nhớ RAM (Call Stack & Memory Heap), bảng tra cứu phương thức chuẩn W3Schools đầy đủ $100\%$ và các bẫy phỏng vấn hóc búa.

---

## 🗺️ LỘ TRÌNH 6 GIAI ĐOẠN TOÀN DIỆN (W3SCHOOLS & PHỎNG VẤN SENIOR)

### 📍 GIAI ĐOẠN 1: NỀN TẢNG CỐT LÕI (CORE FUNDAMENTALS)
- [x] **Bài 1.1:** Cách nhúng JS, Cây DOM & Các phương thức can thiệp (`innerHTML`, `innerText`, `textContent`, `.style`, `.value`).
- [x] **Bài 1.2:** Biến & Bộ nhớ: `var` vs `let` vs `const` (Phạm vi Scope, Redeclare, Reassign, Hoisting & TDZ).
- [x] **Bài 1.3:** Kiểu dữ liệu (Data Types):
  - [x] **1.3.1:** Primitive vs Reference (Call Stack vs Memory Heap, Shallow vs Deep Copy).
  - [x] **1.3.2:** Chuyên sâu Kiểu Number & Toàn bộ Bảng tham chiếu W3Schools (Methods & Properties).
  - [x] **1.3.3:** Chuyên sâu Kiểu String & Toàn bộ Bảng tham chiếu W3Schools (Methods & Properties).
- [x] **Bài 1.4:** Toán tử & Ép kiểu: Số học, Gán, So sánh `==` (loose) vs `===` (strict), Truthy & Falsy values.

---

### 📍 GIAI ĐOẠN 2: HÀM & LUỒNG ĐIỀU KHIỂN (FUNCTIONS & CONTROL FLOW)
- [x] **Bài 2.1:** Các loại Hàm: Function Declaration, Function Expression, Arrow Function, IIFE, Callback cơ bản, con trỏ `this`, đối tượng `arguments`.
- [x] **Bài 2.2:** Tham số & Trả về: Default parameters, Rest parameter `...args`, Từ khóa `return`, Bẫy đột biến tham chiếu & Pure Functions.
- [x] **Bài 2.3:** Câu lệnh rẽ nhánh: `if / else if / else`, `switch / case` và Toán tử 3 ngôi (Ternary Operator `? :`).
- [x] **Bài 2.4:** Vòng lặp toàn diện (JS Loops & Iterations): `for`, `while`, `do...while`, `for...in` (cho Object), `for...of` (cho Array/Iterable).

---

### 📍 GIAI ĐOẠN 3: CẤU TRÚC DỮ LIỆU & BUILT-IN OBJECTS (ARRAYS, OBJECTS, SETS, MAPS, MATH, DATES, REGEXP)
- [x] **Bài 3.1:** Object chuyên sâu (JS Objects): Truy xuất `.` vs `[]`, Thêm/Sửa/Xóa thuộc tính, Object Methods, Prototype cơ bản.
- [x] **Bài 3.2:** Cú pháp hiện đại: Destructuring (Phá vỡ cấu trúc), Spread Operator `...` (Sao chép nông vs sâu).
- [x] **Bài 3.3:** Mảng toàn diện (JS Arrays): Các hàm thêm/bớt/cắt mảng (`push`, `pop`, `shift`, `unshift`, `splice`, `slice`, `concat`, `flat`).
- [x] **Bài 3.4 (🔥 TRỌNG TÂM SỐ 1 - CHIẾM 70% THỜI GIAN):** Bộ ba thần thánh sống còn: **`map`**, **`filter`**, **`forEach`** & Higher-Order Functions (`find`, `reduce`, `some`, `every`, `flatMap`, `Symbol.iterator`).
- [x] **Bài 3.5:** Cấu trúc dữ liệu hiện đại: **JS Sets** (Tập hợp không trùng lặp) & **JS Maps** (Bảng tra cứu Key-Value linh hoạt).
- [x] **Bài 3.6:** Tiện ích số học & Thời gian: **JS Math** (Làm tròn, random) & **JS Dates** (Format, timestamp, tính ngày giờ).
- [x] **Bài 3.7:** Biểu thức chính quy (**JS RegExp**): Cú pháp Regular Expression, Pattern matching, Validate Form (Email, SĐT, Password).

---

### 📍 GIAI ĐOẠN 4: THAO TÁC GIAO DIỆN & WEB APIS (DOM, EVENTS, WINDOW, STORAGE, JSON)
- [x] **Bài 4.1:** Cây DOM & Bộ chọn: `getElementById`, `querySelector`, `querySelectorAll` (NodeList vs HTMLCollection, DOM Navigation, Contextual APIs `closest`/`matches`/`contains`, Node Types).
- [x] **Bài 4.2:** Thao tác phần tử: Đổi nội dung (`textContent`, `innerHTML`), Đổi thuộc tính (`getAttribute`, `setAttribute`, `dataset`), Đổi Style, `classList` (`add`, `remove`, `toggle`), Hiệu năng (Reflow, Repaint, `DocumentFragment`).
- [x] **Bài 4.3:** Xử lý sự kiện (HTML Events): `addEventListener`, Event Object `e`, Event Bubbling vs Capturing, Event Delegation, `preventDefault()`, `stopPropagation()`, Memory Leaks.
- [x] **Bài 4.4:** Trình duyệt Web APIs: **Window API** (Location, History, Navigator), **Web Storage** (`localStorage` vs `sessionStorage` vs Cookies, Token Security XSS/CSRF).
- [x] **Bài 4.5:** Định dạng dữ liệu **JS JSON**: `JSON.stringify()`, `JSON.parse()`, Serialization traps, Whitelist Replacer & Date Reviver.
- [x] **Bài 4.6:** Dự án Mini thực chiến: Ứng dụng To-do List / Giỏ hàng chuẩn JS thuần tương tác với `localStorage` (Kiến trúc Data-Driven).

---

### 📍 GIAI ĐOẠN 5: BẤT ĐỒNG BỘ & GIAO TIẾP MẠNG (ASYNC JS & NETWORKING)
- [x] **Bài 5.1:** Quản lý thời gian & Event Loop (**JS Timers**): `setTimeout`, `setInterval`, `clearTimeout`, Call Stack vs Web APIs vs Callback Queue.
- [x] **Bài 5.2:** Tiến trình Bất đồng bộ: Callback Hell → Đối tượng **Promise** (`resolve`, `reject`, `.then()`, `.catch()`, `.finally()`, `Promise.all()`).
- [x] **Bài 5.3:** Cú pháp hiện đại: **`async / await`**, tối ưu luồng gọi bất đồng bộ tuần tự vs song song.
- [x] **Bài 5.4:** Giao tiếp mạng (**JS Fetch API**): Gửi nhận dữ liệu HTTP (GET, POST, PUT, DELETE), xử lý lỗi mạng & HTTP Status Codes.

---

### 📍 GIAI ĐOẠN 6: JAVASCRIPT CHUYÊN SÂU & NÂNG CAO (JS ADVANCED & OOP)
- [ ] **Bài 6.1:** Cơ chế bao đóng nâng cao: **Closures & Scope Chain** (Private variables, Module Pattern).
- [ ] **Bài 6.2:** Lập trình hướng đối tượng (**JS Classes & OOP**): `class`, `constructor`, `extends`, `super`, `static`, Private fields `#`.
- [ ] **Bài 6.3:** Hệ thống Mô-đun (**JS Modules**): Cú pháp `import` / `export`, Default export vs Named export.
- [ ] **Bài 6.4:** Quản lý lỗi & Gỡ lỗi (**JS Errors & Debugging**): `try...catch...finally`, `throw new Error()`, Breakpoint & Console tips.
- [ ] **Bài 6.5:** Metaprogramming & Tương lai: **JS Proxy & Reflect** (Cơ chế Reactive của Vue 3), **JS Temporal** (Chuẩn ngày giờ thế hệ mới).

---

## 📖 NỘI DUNG CHI TIẾT GIAI ĐOẠN 1

---

### 📌 BÀI 1.1: CÁCH NHÚNG JS, CÂY DOM & 4 PHƯƠNG THỨC CAN THIỆP NỘI DUNG

<details open>
<summary><b>1. Vị trí nhúng JavaScript & Cơ chế chặn luồng (Render Blocking)</b> <i>(Bấm để xem)</i></summary>

* **Cơ chế đọc của trình duyệt:** HTML Parser quét từ trên xuống dưới theo từng dòng code.
* **Các vị trí nhúng:**
  1. **Trong thẻ `<head>` không có thuộc tính:** Gặp `<script>`, trình duyệt lập tức DỪNG render HTML lại, tải JS về và chạy xong mới đọc tiếp $\rightarrow$ Gây hiện tượng "Màn hình trắng" và lỗi `null` khi tóm phần tử DOM.
  2. **Trước thẻ đóng `</body>` (Khuyên dùng chuẩn):** Toàn bộ khung xương DOM đã được dựng xong $100\%$, mã JS chạy truy xuất phần tử an toàn tuyệt đối.
  3. **Trong thẻ `<head>` có `defer` (`<script src="..." defer></script>`):** Trình duyệt tải JS song song ở chế độ chạy ngầm và CHỈ thực thi sau khi cây DOM đã sẵn sàng (tối ưu tốc độ tải trang lớn).
</details>

<br>

<details open>
<summary><b>2. Mổ xẻ Cơ chế Vào - Ra: `innerHTML` vs `innerText` vs `textContent`</b> <i>(Bấm để xem)</i></summary>

* **Bản chất hoạt động của Trình duyệt (Browser Engine):**
  * **`innerHTML` (Thế giới của Mã Nguồn & Parser):**
    * **Chiều GHI (Input):** Nhận chuỗi HTML $\rightarrow$ Gọi bộ dịch **HTML Parser** bóc tách các thẻ $\rightarrow$ Dựng các Node con mới nhét vào cây DOM.
    * **Chiều ĐỌC (Output):** Nhìn vào cây Node con $\rightarrow$ Gọi bộ **HTML Serialization** dịch ngược tất cả Node thành một chuỗi mã HTML thô đầy đủ thẻ đóng/mở.
  * **`innerText` (Thế giới của Mắt Người Nhìn & Render Tree):**
    * **Chiều GHI (Input):** Nhận chuỗi $\rightarrow$ Coi toàn bộ là chữ thuần $\rightarrow$ Mã hóa dấu `<`, `>` an toàn, tạo 1 TextNode duy nhất.
    * **Chiều ĐỌC (Output):** Nhảy sang **Render Tree** (tính cả CSS) để xem mắt người thực tế nhìn thấy gì. Bỏ qua thẻ có `display: none`, thẻ khối tự chèn `\n`, bóc sạch vỏ tag $\rightarrow$ Trả về chuỗi text sạch y hệt thao tác bôi đen copy chuột.
  * **`textContent` (Thế giới của DOM Tree nguyên bản):**
    * Đọc/Ghi dữ liệu text trực tiếp trên cây DOM, hoàn toàn ngó lơ Render Tree và CSS. Lấy được cả chữ đang bị ẩn bởi `display: none`. Chạy cực nhanh vì không gây Reflow.

| Tiêu chí | `innerHTML` | `innerText` | `textContent` |
| :--- | :--- | :--- | :--- |
| **Bản chất dữ liệu** | Chuỗi HTML Markup thô | Chữ hiển thị mắt người nhìn thấy | Dữ liệu TextNode trên cây DOM |
| **Phân tích thẻ tag khi ghi?**| Có (Parser dịch thành thẻ) | Không (Coi là chữ thô) | Không (Coi là chữ thô) |
| **Bị ảnh hưởng bởi CSS?** | Không | Có (Bỏ qua `display: none`) | Không (Lấy cả chữ ẩn) |
| **Tác động hiệu năng** | Nặng (Gọi HTML Parser) | Nặng (Gây Reflow / Layout) | ⚡ **Rất nhanh** (Đọc thẳng DOM) |
| **Nguy cơ bảo mật** | ⚠️ Rất cao (Lỗ hổng XSS) | 🛡️ Tuyệt đối an toàn | 🛡️ Tuyệt đối an toàn |

* **Bẫy bảo mật XSS (Cross-Site Scripting):**
  ```javascript
  // Kẻ gian nhập vào ô bình luận:
  const badInput = `<img src=x onerror="alert('Mã độc đánh cắp Cookie!')">`;
  
  // ❌ NGUY HIỂM: Kích hoạt mã độc ngay khi chèn
  box.innerHTML = badInput;
  
  // ✔️ AN TOÀN TUYỆT ĐỐI: Coi là text thô vô hại
  box.textContent = badInput;
  ```
</details>

---

### 📌 BÀI 1.2: BẢN CHẤT BIẾN — `var` vs `let` vs `const` (SCOPE, HOISTING & TDZ)

<details open>
<summary><b>1. Hai Pha Thực Thi của JS Engine & Cơ Chế Hoisting</b> <i>(Bấm để xem)</i></summary>

* **Pha 1: Creation / Compilation Phase (Quét bộ nhớ):**
  * JS Engine quét toàn bộ mã nguồn trước khi thực thi.
  * Phát hiện khai báo biến và hàm $\rightarrow$ Cấp phát vùng nhớ trên Call Stack (**Hoisting**).
  * `var`: Cấp phát ô nhớ và **tự động gán mặc định `= undefined`**.
  * `let` / `const`: Cấp phát ô nhớ nhưng **KHÔNG gán giá trị**, biến bị đưa vào **Vùng chết tạm thời (TDZ - Temporal Dead Zone)**.
* **Pha 2: Execution Phase (Chạy từng dòng code):**
  * Chạy tuần tự từ trên xuống dưới để gán giá trị và thực thi biểu thức.

* **Bảng so sánh 3 từ khóa biến (Chuẩn Senior):**

| Tiêu chí | `var` (ES5) | `let` (ES6) | `const` (ES6) |
| :--- | :--- | :--- | :--- |
| **Phạm vi (Scope)** | **Function Scope** (Xuyên thủng Block `{}`) | **Block Scope `{}`** | **Block Scope `{}`** |
| **Khai báo lại (Redeclare)** | ✔️ Cho phép trùng tên trong cùng scope | ❌ Báo lỗi `SyntaxError` | ❌ Báo lỗi `SyntaxError` |
| **Gán lại giá trị (Reassign)**| ✔️ Cho phép thay đổi giá trị | ✔️ Cho phép thay đổi giá trị | ❌ Báo lỗi `TypeError` |
| **Khởi tạo ban đầu** | Không bắt buộc (mặc định `undefined`) | Không bắt buộc (mặc định `undefined`) | ⚠️ **Bắt buộc phải gán giá trị** |
| **Hoisting** | Có (khởi tạo `undefined`) | Có (bị nhốt trong TDZ) | Có (bị nhốt trong TDZ) |
</details>

<br>

<details open>
<summary><b>2. Bẫy Phỏng Vấn Senior: Chứng Minh `let` Bị Hoisting & Bẫy Immutability</b> <i>(Bấm để xem)</i></summary>

* **Chứng minh `let` VẪN BỊ HOISTING bằng Scope Shadowing:**
  ```javascript
  let x = "Global";
  function testScope() {
      // Nếu let KHÔNG bị hoist, dòng dưới sẽ tìm ra ngoài lấy "Global".
      // Nhưng thực tế: Báo lỗi ReferenceError!
      console.log(x); // ❌ ReferenceError: Cannot access 'x' before initialization
      let x = "Local"; // Biến này bị hoist lên đầu hàm và nhốt vào TDZ
  }
  testScope();
  ```

* **Bẫy tính bất biến của `const` với Object/Array:**
  * `const` chỉ bảo vệ **Liên kết con trỏ địa chỉ (Reference Binding)** trên Call Stack, KHÔNG bảo vệ dữ liệu bên trong Memory Heap!
  ```javascript
  const user = { name: "Nam", age: 20 };
  // user = { name: "An" }; // ❌ LỖI: Gán lại địa chỉ mới
  user.age = 21;            // ✔️ HỢP LỆ: Sửa thuộc tính bên trong
  
  // Muốn đóng băng tuyệt đối không cho sửa thuộc tính:
  Object.freeze(user);
  user.age = 30; // Vô hiệu hóa, user.age vẫn là 21!
  ```

* **Tỷ lệ vàng thực chiến:** **80% `const` - 19% `let` - 0% `var`**.
</details>

---

### 📌 BÀI 1.3: BẢN CHẤT KIỂU DỮ LIỆU TOÀN DIỆN (DATA TYPES MASTER)

---

#### 1.3.1. PRIMITIVE VS REFERENCE (BỘ NHỚ CALL STACK & MEMORY HEAP)

<details open>
<summary><b>Mổ xẻ Cơ chế Bộ nhớ: Call Stack vs Memory Heap</b> <i>(Bấm để xem)</i></summary>

* **7 Kiểu Primitive (Nguyên thủy):** `Number`, `String`, `Boolean`, `Null`, `Undefined`, `Symbol`, `BigInt`.
  * **Nơi lưu:** Lưu trữ trực tiếp giá trị vào ô nhớ trên **Call Stack**. Bất biến (Immutable).
  * **Cơ chế sao chép:** **Pass by Value** (Tạo một ô nhớ Stack mới độc lập và sao chép nguyên vẹn giá trị sang).
* **3 Kiểu Reference (Tham chiếu):** `Object`, `Array`, `Function`.
  * **Nơi lưu:** Dữ liệu thực tế nằm trên **Memory Heap**. Biến trên **Call Stack** chỉ lưu **địa chỉ con trỏ (Pointer)** trỏ sang ô nhớ Heap đó. Khả biến (Mutable).
  * **Cơ chế sao chép:** **Pass by Reference** (Chỉ sao chép địa chỉ con trỏ, hai biến cùng trỏ chung 1 ô nhớ trên Heap $\rightarrow$ sửa 1 bên là nát cả hai).

* **Bẫy so sánh tham chiếu:**
  ```javascript
  console.log({} === {}); // false (2 ô nhớ Heap khác nhau: #001 !== #002)
  console.log([] === []); // false
  ```

* **Bẫy `typeof null === "object"`:** Bug lịch sử 30 năm (do mã nhị phân Type Tag của `null` là `000` trùng với Object). Kiểm tra chuẩn: `val === null`.

* **Cách Clone độc lập dữ liệu:**
  ```javascript
  const original = { name: "Thịnh", details: { city: "HN" } };
  
  // 1. Shallow Copy (Chỉ copy tầng 1):
  const shallow = { ...original };
  
  // 2. Deep Copy (Tách độc lập toàn bộ các tầng con):
  const deep = structuredClone(original); // Chuẩn ES2022
  ```
</details>

---

#### 1.3.2. CHUYÊN SÂU KIỂU NUMBER & BẢNG THAM CHIẾU W3SCHOOLS ĐẦY ĐỦ

<details open>
<summary><b>1. Cơ chế Số học IEEE 754, Auto-boxing & Bẫy `0.1 + 0.2`</b> <i>(Bấm để xem)</i></summary>

* **Bản chất:** JS chỉ có 1 kiểu `Number` duy nhất (chuẩn dấu phẩy động 64-bit IEEE 754).
* **Cơ chế Auto-boxing:** `(123).toFixed()` tự động bọc tạm `new Number(123)` để gọi method rồi hủy ngay.
* **Bẫy kinh điển `0.1 + 0.2 === 0.3` trả về `false`:**
  * Hệ nhị phân không lưu chính xác số thập phân tuần hoàn $\rightarrow$ Cắt cụt tạo sai số dư `0.30000000000000004`.
  * **Xử lý chuẩn Senior bằng `Number.EPSILON`:**
    ```javascript
    const isEqual = Math.abs((0.1 + 0.2) - 0.3) < Number.EPSILON; // true
    ```
* **Bẫy `Number.isNaN()` vs `isNaN()`:**
  * `isNaN("abc")` trả về `true` (ép kiểu ẩu thành NaN trước khi xét).
  * `Number.isNaN("abc")` trả về `false` (kiểm tra nghiêm ngặt: phải là số VÀ mang giá trị NaN).
</details>

<br>

<details open>
<summary><b>2. DANH SÁCH TRA CỨU PHƯƠNG THỨC NUMBER (NUMBER METHODS W3SCHOOLS)</b> <i>(Bấm để xem)</i></summary>

* **`toFixed()`:** `num.toFixed(digits)` — Làm tròn số thập phân đến n chữ số sau dấu phẩy (trả về String). Ví dụ: `(12.3456).toFixed(2)` $\rightarrow$ `"12.35"`.
* **`toPrecision()`:** `num.toPrecision(len)` — Định dạng số với tổng độ dài len chữ số có nghĩa (trả về String). Ví dụ: `(12.3456).toPrecision(4)` $\rightarrow$ `"12.35"`.
* **`toString()`:** `num.toString(radix)` — Chuyển số thành chuỗi theo hệ cơ số (2: nhị phân, 16: hex). Ví dụ: `(255).toString(16)` $\rightarrow$ `"ff"`.
* **`toLocaleString()`:** `num.toLocaleString(locale)` — Định dạng số kèm dấu phân cách hàng nghìn theo quốc gia. Ví dụ: `(1000000).toLocaleString('vi-VN')` $\rightarrow$ `"1.000.000"`.
* **`toExponential()`:** `num.toExponential(x)` — Chuyển số sang dạng số mũ khoa học ($e+x$). Ví dụ: `(1234).toExponential(2)` $\rightarrow$ `"1.23e+3"`.
* **`valueOf()`:** `num.valueOf()` — Lấy giá trị số nguyên thủy từ Wrapper Object. Ví dụ: `(123).valueOf()` $\rightarrow$ `123`.
* **`parseInt()`:** `Number.parseInt(str, radix)` — Bóc tách số nguyên từ chuỗi, bỏ chữ phía sau. Ví dụ: `parseInt("42px", 10)` $\rightarrow$ `42`.
* **`parseFloat()`:** `Number.parseFloat(str)` — Bóc tách số thực từ chuỗi. Ví dụ: `parseFloat("42.58rem")` $\rightarrow$ `42.58`.
* **`isInteger()`:** `Number.isInteger(val)` — Kiểm tra xem giá trị có phải số nguyên không. Ví dụ: `Number.isInteger(10.5)` $\rightarrow$ `false`.
* **`isSafeInteger()`:** `Number.isSafeInteger(val)` — Kiểm tra số nguyên an toàn trong khoảng $-(2^{53}-1)$ đến $(2^{53}-1)$. Ví dụ: `Number.isSafeInteger(9007199254740992)` $\rightarrow$ `false`.
* **`isFinite()`:** `Number.isFinite(val)` — Kiểm tra số hữu hạn (trả về false nếu là Infinity hoặc NaN). Ví dụ: `Number.isFinite(1 / 0)` $\rightarrow$ `false`.
* **`isNaN()`:** `Number.isNaN(val)` — Kiểm tra chính xác có phải là NaN không (chuẩn ES6, không ép kiểu ngầm). Ví dụ: `Number.isNaN("abc")` $\rightarrow$ `false`.

</details>

<br>

<details open>
<summary><b>3. DANH SÁCH HẰNG SỐ & THUỘC TÍNH NUMBER (NUMBER PROPERTIES W3SCHOOLS)</b> <i>(Bấm để xem)</i></summary>

* **`Number.EPSILON`:** $\approx 2.22 \times 10^{-16}$ — Khoảng chênh lệch nhỏ nhất giữa 1 và số thực kế tiếp, dùng xử lý sai số `0.1 + 0.2 === 0.3`.
* **`Number.MAX_SAFE_INTEGER`:** $2^{53} - 1$ ($9,007,199,254,740,991$) — Số nguyên an toàn lớn nhất có thể tính toán chính xác tuyệt đối.
* **`Number.MIN_SAFE_INTEGER`:** $-(2^{53} - 1)$ — Số nguyên âm an toàn nhỏ nhất.
* **`Number.MAX_VALUE`:** $\approx 1.79 \times 10^{308}$ — Con số dương lớn nhất trong JS (vượt qua là `Infinity`).
* **`Number.MIN_VALUE`:** $\approx 5 \times 10^{-324}$ — Con số dương nhỏ nhất gần $0$ (dưới ngưỡng bị coi là $0$).
* **`Number.NaN`:** Đại diện cho kết quả tính toán số học thất bại (ví dụ: `0 / 0`, `"chữ" * 5`).
* **`Number.POSITIVE_INFINITY`:** `Infinity` — Trả về khi số dương bị tràn bộ nhớ hoặc chia cho $0$ (`10 / 0`).
* **`Number.NEGATIVE_INFINITY`:** `-Infinity` — Trả về khi số âm bị tràn bộ nhớ hoặc chia cho $0$ (`-10 / 0`).

</details>

---

#### 1.3.3. CHUYÊN SÂU KIỂU STRING & BẢNG THAM CHIẾU W3SCHOOLS ĐẦY ĐỦ

<details open>
<summary><b>1. Tính Bất Biến (Immutability), Template Literals & Bẫy UTF-16</b> <i>(Bấm để xem)</i></summary>

* **Nguyên lý bất biến (Immutability):** Chuỗi ký tự trong JS vĩnh viễn không thể thay đổi từng ký tự (`str[0] = 'X'` bị vô hiệu hóa). Mọi phương thức string đều sinh ra **chuỗi mới**, muốn lưu phải gán lại vào biến: `str = str.trim()`.
* **Template Literals (Dấu backtick `` ` ``):**
  * Hỗ trợ nội suy biểu thức: `${user.name}`, `${score >= 5 ? "Đậu" : "Rớt"}`.
  * Hỗ trợ viết xuống dòng nhiều dòng tự nhiên để tạo HTML template sạch sẽ.
* **Bẫy `.length` với Emoji:** Thuộc tính `.length` đếm Code Unit 16-bit (UTF-16) chứ không đếm ký tự mắt người nhìn thấy (`"🚀".length === 2`, `"👨‍👩‍👧‍👦".length === 11`). Đếm chuẩn dùng: `[..."🚀"].length === 1`.
</details>

<br>

<details open>
<summary><b>2. DANH SÁCH TRA CỨU PHƯƠNG THỨC STRING (STRING METHODS W3SCHOOLS)</b> <i>(Bấm để xem)</i></summary>

* **`length`:** `str.length` — Đếm tổng số đơn vị mã 16-bit (UTF-16) có trong chuỗi. Ví dụ: `"Hello".length` $\rightarrow$ `5`.
* **`at()`:** `str.at(index)` — Lấy ký tự tại vị trí index, nhận số âm đếm ngược từ cuối (ES2022). Ví dụ: `"Dev".at(-1)` $\rightarrow$ `"v"`.
* **`charAt()`:** `str.charAt(index)` — Lấy ký tự tại vị trí index (không nhận số âm). Ví dụ: `"Dev".charAt(0)` $\rightarrow$ `"D"`.
* **`charCodeAt()`:** `str.charCodeAt(index)` — Trả về mã số Unicode (UTF-16) của ký tự tại vị trí index. Ví dụ: `"A".charCodeAt(0)` $\rightarrow$ `65`.
* **`slice()`:** `str.slice(start, end)` — Cắt chuỗi từ start đến sát end (hỗ trợ số âm đếm từ đuôi). Ví dụ: `"JavaScript".slice(-6)` $\rightarrow$ `"Script"`.
* **`substring()`:** `str.substring(start, end)` — Cắt chuỗi từ start đến end (số âm đổi thành 0, tự đảo nếu start > end). Ví dụ: `"JavaScript".substring(0, 4)` $\rightarrow$ `"Java"`.
* **`toUpperCase()`:** `str.toUpperCase()` — Chuyển toàn bộ chuỗi sang chữ in hoa. Ví dụ: `"dev".toUpperCase()` $\rightarrow$ `"DEV"`.
* **`toLowerCase()`:** `str.toLowerCase()` — Chuyển toàn bộ chuỗi sang chữ thường. Ví dụ: `"DEV".toLowerCase()` $\rightarrow$ `"dev"`.
* **`trim()`:** `str.trim()` — Cắt sạch khoảng trắng thừa ở cả 2 đầu chuỗi. Ví dụ: `"  admin  ".trim()` $\rightarrow$ `"admin"`.
* **`trimStart()` / `trimEnd()`:** Cắt khoảng trắng chỉ ở đầu hoặc chỉ ở cuối chuỗi. Ví dụ: `"  hi".trimStart()` $\rightarrow$ `"hi"`.
* **`padStart()`:** `str.padStart(len, pad)` — Độn thêm ký tự vào đầu cho đủ độ dài len. Ví dụ: `"5".padStart(3, "0")` $\rightarrow$ `"005"`.
* **`padEnd()`:** `str.padEnd(len, pad)` — Độn thêm ký tự vào đuôi cho đủ độ dài len. Ví dụ: `"5".padEnd(3, "0")` $\rightarrow$ `"500"`.
* **`repeat()`:** `str.repeat(count)` — Nhân bản lặp lại chuỗi count lần. Ví dụ: `"Ha".repeat(3)` $\rightarrow$ `"HaHaHa"`.
* **`replace()`:** `str.replace(old, new)` — Chỉ thay thế ký tự ĐẦU TIÊN tìm thấy. Ví dụ: `"mèo và mèo".replace("mèo", "chó")` $\rightarrow$ `"chó và mèo"`.
* **`replaceAll()`:** `str.replaceAll(old, new)` — Thay thế TẤT CẢ các từ trùng khớp (ES2021). Ví dụ: `"mèo và mèo".replaceAll("mèo", "chó")` $\rightarrow$ `"chó và chó"`.
* **`split()`:** `str.split(separator)` — Bẻ chuỗi thành mảng theo ký tự phân tách. Ví dụ: `"HTML,CSS,JS".split(",")` $\rightarrow$ `["HTML", "CSS", "JS"]`.
* **`concat()`:** `str.concat(s1, s2)` — Nối chuỗi (khuyên dùng backtick thay thế). Ví dụ: `"A".concat("B")` $\rightarrow$ `"AB"`.
* **`indexOf()`:** `str.indexOf(search)` — Tìm vị trí đầu tiên của từ khóa (không thấy trả về -1). Ví dụ: `"test@gmail.com".indexOf("@")` $\rightarrow$ `4`.
* **`lastIndexOf()`:** `str.lastIndexOf(search)` — Tìm vị trí cuối cùng của từ khóa (không thấy trả về -1). Ví dụ: `"canal".lastIndexOf("a")` $\rightarrow$ `3`.
* **`includes()`:** `str.includes(search)` — Kiểm tra chuỗi có chứa từ khóa không (trả về true/false). Ví dụ: `"developer".includes("dev")` $\rightarrow$ `true`.
* **`startsWith()`:** `str.startsWith(prefix)` — Kiểm tra chuỗi có bắt đầu bằng tiền tố prefix không. Ví dụ: `"index.html".startsWith("index")` $\rightarrow$ `true`.
* **`endsWith()`:** `str.endsWith(suffix)` — Kiểm tra chuỗi có kết thúc bằng hậu tố suffix không. Ví dụ: `"avatar.png".endsWith(".png")` $\rightarrow$ `true`.
* **`search()`:** `str.search(regex)` — Tìm kiếm theo biểu thức chính quy Regex, trả về index đầu tiên. Ví dụ: `"Tuổi: 25".search(/\d+/)` $\rightarrow$ `6`.
* **`match()`:** `str.match(regex)` — So khớp theo Regex, trả về mảng kết quả. Ví dụ: `"123 và 456".match(/\d+/g)` $\rightarrow$ `["123", "456"]`.

</details>

---

### 📌 BÀI 1.4: TOÁN TỬ & NGHỆ THUẬT ÉP KIỂU (OPERATORS & TYPE COERCION)

<details open>
<summary><b>1. Cơ chế Ép Kiểu Ngầm (Type Coercion) & Phép Toán Số Học</b> <i>(Bấm để xem)</i></summary>

* **Dấu cộng `+` bị nạp chồng:** Nếu có ít nhất 1 vế là String $\rightarrow$ Tự động ép tất cả sang String để **nối chuỗi**. Ví dụ: `"5" + 2` $\rightarrow$ `"52"`.
* **Dấu trừ `-`, nhân `*`, chia `/`:** $100\%$ ép tất cả các vế về **Number** để tính toán. Ví dụ: `"5" - 2` $\rightarrow$ `3`, `"5" * "2"` $\rightarrow$ `10`.
* **Thứ tự ưu tiên ghép chuỗi:** `1 + 2 + "3"` $\rightarrow$ `"33"` (cộng $1+2=3$ trước), nhưng `"1" + 2 + 3` $\rightarrow$ `"123"` (nối chuỗi từ trái qua phải).
</details>

<br>

<details open>
<summary><b>2. So Sánh `==` (Loose) vs `===` (Strict) & Bẫy `[] == ![]`</b> <i>(Bấm để xem)</i></summary>

* **`===` (Strict Equality):** So sánh cả Type lẫn Giá trị. Khác Type $\rightarrow$ `false` ngay lập tức (không ép kiểu ngầm).
* **`==` (Loose Equality):** Tự động ép kiểu theo thuật toán ECMAScript: Boolean $\rightarrow$ Number ($1/0$), String $\rightarrow$ Number, Object $\rightarrow$ Primitive qua `valueOf()` hoặc `toString()`.
* **Bẫy phỏng vấn Senior: `[] == ![]` tại sao bằng `true`?**
  * Bước 1: `[]` là Object (Truthy) $\rightarrow$ Phủ định `![]` thành `false`. Biểu thức thành `[] == false`.
  * Bước 2: Ép boolean `false` thành số $0$ $\rightarrow$ Biểu thức thành `[] == 0`.
  * Bước 3: Ép Object `[]` về chuỗi qua `[].toString()` thành chuỗi rỗng `""` $\rightarrow$ Biểu thức thành `"" == 0`.
  * Bước 4: Ép chuỗi rỗng `""` về số $0$ $\rightarrow$ Biểu thức thành `0 == 0` $\rightarrow$ **`true`**!
* **Bẫy `null` và `undefined`:** `null == undefined` $\rightarrow$ `true` (quy ước riêng), nhưng `null === undefined` $\rightarrow$ `false`. `null > 0` $\rightarrow$ `false`, `null == 0` $\rightarrow$ `false`, nhưng `null >= 0` $\rightarrow$ `true` (do `>=` ép kiểu sang số).
</details>

<br>

<details open>
<summary><b>3. BẢNG 8 GIÁ TRỊ FALSY DUY NHẤT TRONG JAVASCRIPT</b> <i>(Bấm để xem)</i></summary>

* **`false`:** Bản thân giá trị boolean sai.
* **`0` và `-0`:** Con số không.
* **`0n`:** Số BigInt không.
* **`""` (Chuỗi rỗng):** Chuỗi không có ký tự nào.
* **`null`:** Rỗng không có giá trị.
* **`undefined`:** Chưa được định nghĩa giá trị.
* **`NaN`:** Kết quả tính toán số học thất bại (Not-a-Number).
* ⚠️ **Quy tắc vàng:** Ngoài 8 giá trị trên, **TẤT CẢ MỌI THỨ CÒN LẠI ĐỀU LÀ TRUTHY!** (kể cả mảng rỗng `[]`, object rỗng `{}`, chuỗi có dấu cách `" "`).
</details>

<br>

<details open>
<summary><b>4. Bộ Ba Toán Tử Logic: `&&`, `||` và `??` (Nullish Coalescing)</b> <i>(Bấm để xem)</i></summary>

* **`||` (Logical OR):** Tìm giá trị TRUTHY đầu tiên từ trái qua phải. Ví dụ: `"" || "Ẩn danh"` → `"Ẩn danh"`.
* **`&&` (Logical AND):** Tìm giá trị FALSY đầu tiên từ trái qua phải. Ví dụ: `"User" && 25 && "OK"` → `"OK"`.
* **`??` (Nullish Coalescing - ES2020):** CHỈ bỏ qua khi giá trị là `null` hoặc `undefined`. Không bỏ qua số `0` hoặc chuỗi rỗng `""`.
  * Ví dụ bẫy: `let score = 0;`
  * `score || 10` → `10` (Bị sai vì coi 0 là Falsy và nuốt mất số 0).
  * `score ?? 10` → `0` (Chuẩn xác: 0 là điểm số hợp lệ, giữ nguyên 0).
</details>

<br>

<details open>
<summary><b>5. Lời Khuyên Thực Chiến (Best Practices)</b> <i>(Bấm để xem)</i></summary>

* **Luật bất di bất dịch:** Luôn dùng **`===`** và **`!==`** trong mọi phép so sánh.
* **Ép kiểu chủ động (Explicit Coercion):**
  * Ép sang Số: Dùng dấu cộng đơn nguyên `+str` hoặc `Number(str)`. Ví dụ: `+"42"` → `42`.
  * Ép sang Boolean: Dùng phủ định kép `!!val`. Ví dụ: `!!user` → `true`.
* **Gán giá trị mặc định cho cấu hình/cài đặt:** Luôn dùng toán tử **`??`** thay cho `||`.
</details>

---

### 📌 BÀI 2.1: CÁC LOẠI HÀM TRONG JAVASCRIPT (FUNCTIONS MASTER)

<details open>
<summary><b>1. Phân Biệt 5 Hình Thái Hàm</b> <i>(Bấm để xem)</i></summary>

* **Function Declaration:** `function sum(a, b) { return a + b; }` — Khai báo truyền thống, được Full Hoist (gọi trước khai báo thoải mái).
* **Function Expression:** `const sum = function(a, b) { return a + b; };` — Gán hàm vào biến, tuân theo quy tắc biến (gọi trước bị báo lỗi).
* **Arrow Function (ES6):** `const sum = (a, b) => a + b;` — Cú pháp mũi tên ngắn gọn, không có `this` riêng, không có `arguments`.
* **IIFE (Tự chạy tức thì):** `(function() { ... })();` — Tạo phạm vi kín bảo vệ biến, chạy ngay lập tức khi nạp file.
* **Callback Function:** `btn.addEventListener("click", myFunc);` — Truyền hàm như một đối số vào hàm khác để được gọi lại sau.
</details>

<br>

<details open>
<summary><b>2. Bẫy Hoisting: Declaration vs Expression (Kèm Code Minh Họa)</b> <i>(Bấm để xem)</i></summary>

* **Cơ chế ngầm:** Declaration được bốc cả thân hàm lên đầu file. Expression chỉ bốc biến lên đầu (nếu là `var` thì bằng `undefined`, nếu là `const/let` thì bị nhốt vào TDZ).
```javascript
sayHello(); // ✔️ Chạy tốt: in ra "Hello!"
sayHi();    // ❌ BÁO LỖI: TypeError: sayHi is not a function

function sayHello() { console.log("Hello!"); }
var sayHi = function() { console.log("Hi!"); };
```
</details>

<br>

<details open>
<summary><b>3. Bẫy Tử Huyệt Con Trỏ `this`: 4 Quy Tắc Ràng Buộc & Sự Khác Biệt Giữa Hàm Thường vs Arrow</b> <i>(Bấm để xem)</i></summary>

* **4 Quy tắc vàng xác định `this` trong Hàm Thường (Regular Function):**
  1. **Default Binding:** Gọi hàm rời rạc `fn()` → `this` trỏ về `window` (hoặc `undefined` nếu bật `'use strict'`).
  2. **Implicit Binding:** Gọi qua đối tượng `obj.fn()` → `this` chính là `obj` ngay trước dấu chấm.
  3. **Explicit Binding:** Dùng `call()`, `apply()`, `bind()` → `this` do lập trình viên chỉ định thủ công.
  4. **`new` Binding:** Gọi qua hàm khởi tạo `new Fn()` → `this` trỏ vào đối tượng rỗng mới vừa được sinh ra.

* **Bản chất Arrow Function:** **Hoàn toàn KHÔNG CÓ `this` riêng**. Nó không tuân theo 4 quy tắc trên mà kế thừa `this` từ phạm vi cha bao bọc nó lúc được định nghĩa (**Lexical `this`**).

* **Code bẫy thực chiến 1: Làm Method của Object (Cấm dùng Arrow Function)**
```javascript
const user = {
    name: "Thịnh",
    // ❌ LỖI: Arrow function không có this -> Nhảy ra ngoài mượn window -> window.name là undefined
    getNameArrow: () => console.log(this.name),
    
    // ✔️ ĐÚNG: Hàm thường áp dụng Implicit Binding -> this chính là user
    getNameNormal() { console.log(this.name); }
};
user.getNameArrow();  // undefined
user.getNameNormal(); // "Thịnh"
```

* **Code bẫy thực chiến 2: Callback lồng trong Method (Lý do Arrow Function ra đời!)**
```javascript
const team = {
    title: "Frontend Team",
    members: ["Thịnh", "Nam"],
    showMembers() {
        // ❌ Hàm thường: forEach gọi callback rời rạc -> this bị reset về window/undefined
        this.members.forEach(function(member) {
            console.log(`${member} thuộc ${this.title}`); // "Thịnh thuộc undefined"
        });

        // ✔️ Arrow Function: Kế thừa trực tiếp this.title từ phương thức showMembers()
        this.members.forEach(member => {
            console.log(`${member} thuộc ${this.title}`); // "Thịnh thuộc Frontend Team"
        });
    }
};
team.showMembers();
```

* **Code bẫy thực chiến 3: Mất context khi tách hàm hoặc dùng `setTimeout`**
```javascript
const counter = {
    count: 10,
    increase() { console.log(this.count); }
};

// ❌ LỖI MẤT THIS: Truyền hàm làm callback, setTimeout tự gọi hàm rời rạc -> this = window -> undefined
setTimeout(counter.increase, 100); 

// ✔️ Cách fix 1: Dùng Arrow Function bọc ngoài giữ nguyên context
setTimeout(() => counter.increase(), 100); // 10

// ✔️ Cách fix 2: Dùng .bind() để khóa cứng this vào object counter
setTimeout(counter.increase.bind(counter), 100); // 10
```

* **Code bẫy thực chiến 4: DOM Event Listener**
```javascript
const button = document.getElementById("btn");

// ❌ LỖI: this trỏ ra window -> window.classList báo lỗi sập ứng dụng!
button.addEventListener("click", () => {
    this.classList.toggle("active");
});

// ✔️ ĐÚNG: Hàm thường tự động gán this trỏ vào chính thẻ HTML kích hoạt sự kiện
button.addEventListener("click", function() {
    this.classList.toggle("active"); // this = chính là <button>
});
```
</details>

<br>

<details open>
<summary><b>4. Mổ Xẻ Đối Tượng `arguments` (ES5) vs Rest Parameter `...args` (ES6)</b> <i>(Bấm để xem)</i></summary>

* **Bản chất `arguments` trong Hàm Thường:**
  * Là một **Array-like Object** (đối tượng dạng mảng: có thuộc tính `.length`, truy cập qua chỉ mục index `[0]`, `[1]`).
  * **CẢNH BÁO:** **KHÔNG PHẢI LÀ MẢNG THẬT!** Không có các phương thức như `.map()`, `.filter()`, `.reduce()`, `.forEach()`.
  * **Arrow Function:** Hoàn toàn **KHÔNG TỒN TẠI** `arguments` (gọi sẽ báo `ReferenceError: arguments is not defined`).

* **Code minh họa bẫy `arguments` & Cách ép kiểu sang Mảng (ES5):**
```javascript
function sumOld() {
    console.log(arguments.length); // ✔️ Có length: 3
    console.log(arguments[0]);      // ✔️ Truy cập theo index: 10
    
    // ❌ LỖI TỬ HUYỆT: arguments.map is not a function!
    // arguments.map(x => x * 2);

    // Cách xử lý ES5: Ép kiểu Array-like sang Mảng xịn bằng Array.from() hoặc slice()
    const realArray = Array.from(arguments);
    return realArray.reduce((sum, num) => sum + num, 0); // ✔️ Tính toán bình thường
}
console.log(sumOld(10, 20, 30)); // 60
```

* **Chuẩn ES6 Hiện Đại: Dùng Rest Parameter (`...args`) thay thế hoàn toàn `arguments`**
```javascript
// ❌ Arrow Function: Báo lỗi ReferenceError nếu gọi arguments
// const sumModern = () => console.log(arguments);

// ✔️ Dùng Rest Parameter: args là MẢNG THẬT 100% (Array Instance), hỗ trợ cả Hàm thường & Arrow
const sumModern = (...args) => {
    console.log(Array.isArray(args)); // true (Mảng xịn)
    return args.reduce((sum, num) => sum + num, 0); // Sử dụng trực tiếp mọi hàm mảng
};

console.log(sumModern(1, 2, 3, 4, 5)); // 15
```
</details>

<br>

<details open>
<summary><b>5. Quy Tắc Cú Pháp & Ghi Nhớ Vàng Của Arrow Function (Chuẩn W3Schools)</b> <i>(Bấm để xem)</i></summary>

* **Quy tắc ngoặc đơn `()`:**
  * **0 tham số:** BẮT BUỘC có ngoặc đơn: `const greet = () => "Hello";`
  * **1 tham số:** Được phép bỏ ngoặc đơn: `const square = x => x * x;`
  * **Từ 2 tham số trở lên:** BẮT BUỘC có ngoặc đơn: `const add = (a, b) => a + b;`
* **Quy tắc `return` và ngoặc nhọn `{}`:**
  * Chỉ bỏ `return` và `{}` (Implicit Return) khi hàm là **duy nhất 1 câu lệnh biểu thức**: `x => x * 2`.
  * Có từ **2 câu lệnh trở lên**, BẮT BUỘC phải giữ `{}` và `return`:
    ```javascript
    const calc = (a, b) => {
        let total = a + b;
        return total * 2;
    };
    ```
* **Bắt buộc định nghĩa trước:** Arrow Function phải định nghĩa trước khi gọi (không được gọi trước như Declaration).
</details>

<br>

<details open>
<summary><b>6. Bảng Chiến Lược: Khi Nào NÊN & KHÔNG NÊN Dùng Arrow Function</b> <i>(Bấm để xem)</i></summary>

* **Khi NÊN dùng Arrow Function:**
  * ✔️ Dùng cho các hàm ngắn gọn (chức năng 1 dòng).
  * ✔️ Dùng làm hàm Callback cho các phương thức mảng (`map`, `filter`, `find`, `forEach`, `reduce`).
  * ✔️ Khi bạn muốn giữ nguyên ngữ cảnh `this` của phạm vi cha bao bọc (Lexical `this` trong React / Vue).
* **Khi TUYỆT ĐỐI KHÔNG NÊN dùng Arrow Function:**
  * ❌ **Không dùng làm Method của Object:** Vì Arrow Function không có `this` riêng, nó sẽ trỏ ra `window` làm `this.property` bị `undefined`.
  * ❌ **Không dùng trong DOM Event Listener:** Nếu bạn muốn dùng từ khóa `this` để đại diện cho chính phần tử HTML vừa được click.
  * ❌ **Không dùng làm Constructor:** Cấm dùng từ khóa `new` với Arrow Function (báo `TypeError`).
</details>

---

### 📌 BÀI 2.2: THAM SỐ, ĐỐI SỐ & GIÁ TRỊ TRẢ VỀ (PARAMETERS & RETURN)

---

#### 2.2.1. CƠ CHẾ TRUYỀN THAM SỐ: PASS BY VALUE VS PASS BY REFERENCE

<details open>
<summary><b>1. Bản Chất Phân Biệt Parameter vs Argument & Cơ Chế Bộ Nhớ</b> <i>(Bấm để xem)</i></summary>

* **Parameter (Tham số):** Biến đại diện khai báo ở định nghĩa hàm.
* **Argument (Đối số):** Giá trị thực tế ném vào hàm khi gọi thực thi.
* **Quy luật bộ nhớ:**
  * **Truyền Primitive (Pass by Value):** Hàm tạo một bản sao giá trị độc lập trên Call Stack. Mọi thay đổi trong hàm **hoàn toàn không ảnh hưởng** tới biến gốc bên ngoài.
  * **Truyền Object/Array (Pass by Reference):** Hàm sao chép địa chỉ con trỏ, cả hai cùng trỏ chung vào 1 ô nhớ trên Memory Heap. Thay đổi thuộc tính trong hàm sẽ **làm biến dạng trực tiếp dữ liệu bên ngoài**.
</details>

<br>

<details open>
<summary><b>2. Giá Trị Mặc Định (Default Parameters) & Rest Parameter (`...args`)</b> <i>(Bấm để xem)</i></summary>

* **Default Parameters (`param = defaultValue`):**
  * **Cơ chế ngầm:** Chỉ kích hoạt khi giá trị truyền vào là `undefined` (hoặc không truyền).
  * **Lưu ý:** Nếu truyền `null`, `false`, `0`, JS xem là giá trị hợp lệ nên **KHÔNG** lấy giá trị mặc định.
* **Rest Parameter (`...args`):**
  * Gom các đối số còn lại thành một **Mảng thật 100%**.
  * **Quy tắc bắt buộc:** Phải đứng ở **vị trí cuối cùng** trong danh sách tham số (`(a, ...rest)` hợp lệ, `(...rest, a)` báo lỗi cú pháp).
</details>

<br>

<details open>
<summary><b>3. Bản Chất Từ Khóa `return` & Bẫy Chèn Chấm Phẩy Tự Động (ASI Trap)</b> <i>(Bấm để xem)</i></summary>

* **Bản chất `return`:** Lập tức ngắt luồng thực thi hàm và trả kết quả về nơi gọi. Hàm không có `return` sẽ mặc định trả về `undefined`.
* **Bẫy ASI (Automatic Semicolon Insertion):**
  * **Lỗi kinh điển:** Viết chữ `return` rồi xuống dòng mới mở ngoặc `{` hoặc viết giá trị. JS Engine tự động chèn dấu `;` ngay sau `return` $\rightarrow$ Hàm dừng ngay lập tức và trả về `undefined`!
  ```javascript
  // ❌ SAI: Trả về undefined vì JS tự chèn dấu chấm phẩy thành "return;"
  function getUser() {
      return
      { name: "Thịnh" };
  }

  // ✔️ ĐÚNG: Giữ dấu mở ngoặc cùng dòng với return
  function getUser() {
      return {
          name: "Thịnh"
      };
  }
  ```
</details>

<br>

<details open>
<summary><b>⭐ [TRỌNG TÂM CẦN LUYỆN TẬP THỰC HÀNH CHUYÊN SÂU] Bẫy Đột Biến Tham Chiếu (Side Effects) & Hàm Thuần Khiết (Pure Functions)</b> <i>(Bấm để xem)</i></summary>

> ⚠️ **MỤC TIÊU LUYỆN TẬP:** Đây là tử huyệt gây ra 80% bug rò rỉ dữ liệu trong các dự án thực tế và là câu hỏi kiểm tra tư duy Senior kinh điển. Cần làm bài tập thực hành code nhiều lần để hiểu sâu!

* **Hiện tượng Đột biến (Mutation / Side Effect):**
  * Do Object/Array được truyền theo dạng tham chiếu, khi bạn sửa thuộc tính của tham số bên trong hàm, bạn đang trực tiếp "phá hoại" ô nhớ gốc trên Memory Heap mà các phần khác của chương trình đang dùng chung.
  ```javascript
  // ❌ NGUY HIỂM (Impure Function / Gây Side Effect):
  function updateCart(cart) {
      cart.total = 100; // Sửa thẳng vào bộ nhớ của biến bên ngoài truyền vào!
  }
  const myCart = { total: 0 };
  updateCart(myCart);
  console.log(myCart.total); // Bị biến đổi thành 100 ngoài ý muốn!
  ```

* **Chuẩn giải pháp: Hàm thuần khiết (Pure Function)**
  * **Nguyên tắc:** Không bao giờ sửa trực tiếp tham số đầu vào. Luôn tạo bản sao (Clone bằng Spread `{ ...cart }` hoặc `structuredClone`) rồi mới chỉnh sửa và trả về Object mới:
  ```javascript
  // ✔️ AN TOÀN TUYỆT ĐỐI (Pure Function):
  const updateCartPure = (cart) => ({
      ...cart,
      total: 100
  });
  const newCart = updateCartPure(myCart); // myCart gốc được bảo toàn nguyên vẹn
  ```
</details>

<br>

<details open>
<summary><b>5. Bẫy Cú Pháp Thường Gặp & Lời Khuyên Thực Chiến</b> <i>(Bấm để xem)</i></summary>

* **Bẫy Arrow Function trả về Object:**
  * Nếu viết `() => { name: "Thịnh" }`, JS hiểu nhầm `{}` là thân hàm $\rightarrow$ Trả về `undefined`.
  * **Cách sửa:** Bọc Object trong ngoặc đơn: `() => ({ name: "Thịnh" })`.
* **Bẫy tham số mặc định phụ thuộc lẫn nhau:**
  * Tham số sau có thể lấy giá trị tham số trước: `(a = 1, b = a * 2)` $\rightarrow$ Hợp lệ.
  * Tham số trước gọi tham số sau: `(a = b * 2, b = 1)` $\rightarrow$ Báo lỗi `ReferenceError` do dính vùng chết TDZ.
</details>

---

### 📌 BÀI 2.3: CÂU LỆNH RẼ NHÁNH (IF/ELSE, SWITCH/CASE, TERNARY)

---

<details open>
<summary><b>1. Bản Chất Câu Lệnh (Statement) vs Biểu Thức (Expression)</b> <i>(Bấm để xem)</i></summary>

* **`if/else` & `switch` là Statement (Câu lệnh):** Chỉ thực hiện hành vi rẽ nhánh, **không tự sinh ra giá trị**, không thể gán trực tiếp cho biến.
* **Toán tử 3 ngôi (`? :`) là Expression (Biểu thức):** Tự tính toán và **trả về giá trị**, dùng được trong phép gán biến, hàm `return` hoặc chuỗi Template String.
</details>

<br>

<details open>
<summary><b>2. Cơ Chế So Sánh Trong `switch/case` & Bẫy Tuột Dốc (Fall-through)</b> <i>(Bấm để xem)</i></summary>

* **So sánh nghiêm ngặt (`===`):** `switch` so sánh giá trị và kiểu dữ liệu tuyệt đối (không ép kiểu ngầm). Số `5` sẽ không khớp với `case "5"`.
* **Cơ chế Fall-through (Tuột dốc):** Thiếu từ khóa `break;`, JS Engine sẽ chạy tuột dốc thực thi mọi `case` bên dưới bất chấp điều kiện.
* **Ứng dụng Senior:** Cố tình gom nhóm nhiều `case` chung 1 kết quả xử lý:
```javascript
switch (day) {
    case "Thứ 7":
    case "Chủ Nhật":
        console.log("Cuối tuần"); // Cả 2 case đều chạy vào đây
        break;
}
```
</details>

<br>

<details open>
<summary><b>3. Kỹ Thuật Clean Code: Guard Clause (Early Return) & Object Lookup</b> <i>(Bấm để xem)</i></summary>

* **Guard Clause (Early Return):** Kiểm tra điều kiện sai để `return` đuổi về ngay ở đầu hàm, triệt tiêu hoàn toàn mã lồng nhau dạng kim tự tháp (Arrow Anti-Pattern).
```javascript
// ✔️ Chuẩn Senior: Code phẳng, cực sạch, không lồng if/else
function processOrder(order) {
    if (!order) return;
    if (order.items.length === 0) return;
    if (!order.isPaid) return;
    // Logic chính xử lý đơn hàng tại đây...
}
```
* **Object Lookup (Bảng tra cứu):** Thay thế khối `switch/case` dài lê thê bằng một Object tra cứu key-value:
```javascript
const roleMap = { admin: "Quản trị", editor: "Biên tập" };
const label = roleMap[role] || "Khách";
```
</details>

<br>

<details open>
<summary><b>4. Bẫy Phỏng Vấn Thực Chiến: Nested Ternary & Bẫy Số `0` Với `&&`</b> <i>(Bấm để xem)</i></summary>

* **Cấm lạm dụng Toán tử 3 ngôi lồng nhau:** Quá 2 nhánh rẽ (`a ? b : c ? d : e`) sẽ làm nát tính dễ đọc của code. Hãy quay về `if/else` hoặc Object Lookup.
* **Bẫy số `0` khi dùng `&&` (Lỗi kinh điển trong UI / React):**
```javascript
const count = 0;
// ❌ LỖI: count = 0 là Falsy -> Toán tử && dừng lại trả về số 0 -> In số 0 lên màn hình!
const msg = count && "Có tin nhắn mới"; // msg = 0

// ✔️ ĐÚNG: Dùng toán tử 3 ngôi để kiểm tra rõ ràng
const safeMsg = count > 0 ? "Có tin nhắn mới" : null;
```
</details>

---

### 📌 BÀI 2.4: VÒNG LẶP TOÀN DIỆN (JS LOOPS & ITERATIONS)

---

<details open>
<summary><b>1. Phân Biệt 5 Loại Vòng Lặp Chính Trong JavaScript</b> <i>(Bấm để xem)</i></summary>

* **`for` truyền thống:** Dùng khi biết trước số lần lặp cụ thể dựa trên chỉ số đếm `i`.
* **`while`:** Dùng khi chưa biết trước số lần lặp, chỉ dừng khi điều kiện trở thành `false`.
* **`do...while`:** Luôn chạy khối lệnh **ít nhất 1 lần đầu tiên** rồi mới bắt đầu kiểm tra điều kiện.
* **`for...in` (Nhặt Key):** Dùng để duyệt qua các **Tên thuộc tính (Keys)** của Object. Lấy ra chuỗi String.
* **`for...of` (Nhặt Value):** Dùng để duyệt qua các **Giá trị (Values)** của cấu trúc có tính lặp (Iterable: Array, String, Set, Map). Object thông thường không dùng được vì thiếu giao thức ngầm `[Symbol.iterator]`.
</details>

<br>

<details open>
<summary><b>2. Từ Khóa Điều Khiển: `break` vs `continue`</b> <i>(Bấm để xem)</i></summary>

* **`break;`:** Lập tức bẻ gãy và thoát khỏi vòng lặp hoàn toàn.
* **`continue;`:** Bỏ qua các câu lệnh còn lại của vòng lặp hiện tại để nhảy ngay sang bước lặp kế tiếp.
</details>

<br>

<details open>
<summary><b>3. Bẫy Phỏng Vấn Senior Kinh Điển Về Vòng Lặp</b> <i>(Bấm để xem)</i></summary>

* **Bẫy 1: `var` trong vòng `for` kèm `setTimeout` (Scope & Closure)**
```javascript
// ❌ LỖI: var có Function Scope -> Cả 3 callback đều nhìn chung 1 ô nhớ i (khi vòng for chạy xong thì i = 3)
for (var i = 0; i < 3; i++) {
    setTimeout(() => console.log(i), 100); // In ra: 3, 3, 3
}

// ✔️ ĐÚNG: let có Block Scope -> Mỗi vòng lặp sinh ra 1 biến i độc lập
for (let i = 0; i < 3; i++) {
    setTimeout(() => console.log(i), 100); // In ra: 0, 1, 2
}
```

* **Bẫy 2: Cấm dùng `for...in` để duyệt Mảng**
```javascript
const list = ["A", "B"];
list.customProp = "Lạ"; // Gán thêm thuộc tính tùy ý

for (const key in list) {
    console.log(key); // In ra: "0", "1" và cả "customProp"! (Bị lôi cả thuộc tính mở rộng)
}
// ✔️ DUYỆT MẢNG LUÔN DÙNG: for...of hoặc các hàm mảng (forEach, map)
```

* **Bẫy 3: Cấm dùng `break` hoặc `continue` trong `forEach`**
  * `forEach` nhận hàm callback độc lập, việc gọi `break;` bên trong sẽ quăng lỗi `SyntaxError: Illegal break statement`. Muốn dừng giữa chừng, bắt buộc dùng `for...of`.
</details>

<br>

<details open>
<summary><b>4. Lời Khuyên Thực Chiến</b> <i>(Bấm để xem)</i></summary>

* **Duyệt Object chuẩn:** Thay vì dùng `for...in` dễ bị lôi thuộc tính từ chuỗi Prototype, hãy dùng bộ 3 phương thức tĩnh chuẩn ES6:
  * `Object.keys(obj)`: Trả về mảng các key.
  * `Object.values(obj)`: Trả về mảng các giá trị.
  * `Object.entries(obj)`: Trả về mảng các cặp `[key, value]`.
* **Phòng ngừa Vòng lặp vô tận (Infinite Loop):** Khi dùng `while`, luôn kiểm tra biến điều kiện phải được cập nhật ở cuối khối lệnh để tránh làm treo đơ trình duyệt.
</details>

---

### 📌 BÀI 3.1: ĐỐI TƯỢNG CHUYÊN SÂU TOÀN DIỆN (JAVASCRIPT OBJECTS MASTER)

---

#### 3.1.1. 6 CÁCH ĐỊNH NGHĨA ĐỐI TƯỢNG (OBJECT DEFINITIONS)

<details open>
<summary><b>Mổ xẻ 6 Phương Pháp Khởi Tạo Object Trong JavaScript</b> <i>(Bấm để xem)</i></summary>

1. **Object Literal (Khởi tạo trực tiếp — Dùng 95% thực chiến):**
   ```javascript
   const user = { name: "Thịnh", age: 20 };
   ```
2. **Từ khóa `new Object()`:**
   ```javascript
   const user = new Object();
   user.name = "Thịnh";
   ```
3. **Hàm khởi tạo (Constructor Function) — Bản thiết kế sinh hàng loạt đối tượng:**
   ```javascript
   function Person(name, age) {
       this.name = name;
       this.age = age;
   }
   const p1 = new Person("An", 20);
   ```
   * **Cơ chế ngầm 4 bước của toán tử `new` (Senior Trap):**
     1. Tạo ra một Object rỗng mới toanh `{}`.
     2. Gán con trỏ Prototype của object mới trỏ vào `Person.prototype`.
     3. Ràng buộc `this` trỏ vào object mới và thực thi thân hàm `Person`.
     4. Tự động trả về object mới vừa tạo.
4. **`Object.assign(target, ...sources)` (Sao chép / Gộp Object):**
   ```javascript
   const merged = Object.assign({ a: 1 }, { b: 2 }); // { a: 1, b: 2 }
   ```
5. **`Object.create(proto)` (Tạo Object kế thừa Prototype chỉ định):**
   ```javascript
   const proto = { sayHi() { return "Hi"; } };
   const obj = Object.create(proto); // obj kế thừa trực tiếp sayHi() từ proto
   ```
6. **`Object.fromEntries(iterable)` (ES2019 — Đảo ngược mảng/Map thành Object):**
   ```javascript
   const entries = [["name", "Thịnh"], ["role", "Admin"]];
   const obj = Object.fromEntries(entries); // { name: "Thịnh", role: "Admin" }
   ```
</details>

---

#### 3.1.2. THAO TÁC THUỘC TÍNH (PROPERTIES) & CÁCH HIỂN THỊ (DISPLAY)

<details open>
<summary><b>1. Truy Xuất, Thêm, Sửa, Xóa Thuộc Tính & Bẫy Thêm Vào Constructor</b> <i>(Bấm để xem)</i></summary>

* **Truy xuất: Dấu chấm (`.`) vs Ngoặc vuông (`[]`):**
  * **Dấu chấm (`.`):** Chỉ dùng cho tên thuộc tính tĩnh, hợp lệ theo quy tắc đặt tên biến.
  * **Ngoặc vuông (`[]`):** Bắt buộc dùng khi:
    * Tên thuộc tính là biến động: `const key = "email"; user[key]`.
    * Tên thuộc tính có khoảng trắng hoặc ký tự lạ: `user["full name"]`, `user["content-type"]`.
    * Tên thuộc tính là số: `user[123]` (JS tự ép thành chuỗi `"123"`).

* **Thêm mới thuộc tính:**
  * **Thêm vào 1 đối tượng cụ thể:** `p1.job = "Developer";` (Chỉ riêng `p1` có, các đối tượng khác không có).
  * **⭐ BẪY KINH ĐIỂN: Thêm thuộc tính cho TẤT CẢ đối tượng của Constructor:**
    * ❌ **SAI:** `Person.job = "Dev"` (Vô dụng! Các instance `p1`, `p2` không nhìn thấy).
    * ✔️ **ĐÚNG:** Phải thêm vào Prototype: `Person.prototype.job = "Developer";`

* **Xóa thuộc tính:**
  * `delete user.age;` (Chỉ xóa được thuộc tính của object, **không thể xóa biến thông thường**).

* **Toán tử an toàn Optional Chaining (`?.`) — ES2020:**
  * Tránh lỗi sập toàn bộ trang web khi truy cập thuộc tính của `undefined/null`:
  ```javascript
  const user = {};
  console.log(user?.profile?.avatar); // An toàn: Trả về undefined, không văng lỗi sập app!
  ```
</details>

<br>

<details open>
<summary><b>2. 4 Cách Hiển Thị Đối Tượng Ra Màn Hình (Tránh Lỗi `[object Object]`)</b> <i>(Bấm để xem)</i></summary>

* Nếu gán thẳng Object vào HTML (`document.write(person)`), trình duyệt sẽ gọi `.toString()` và in ra **`[object Object]`**.
* **4 Cách hiển thị chuẩn W3Schools:**
  1. **Hiển thị từng thuộc tính:** `person.name + " - " + person.age`.
  2. **Duyệt vòng lặp `for...in`:** Lấy từng `person[key]` nối chuỗi.
  3. **Bóc tách bằng `Object.values(person)`:** `Object.values(person).join(", ")`.
  4. **Chuyển thành JSON bằng `JSON.stringify(person)`:**
     * **Lưu ý Senior:** `JSON.stringify` sẽ **tự động bỏ qua (vứt bỏ)** các thuộc tính có giá trị là `Function`, `undefined`, hoặc `Symbol`!
</details>

---

#### 3.1.3. GETTER, SETTER & QUẢN LÝ BỘ CỜ (PROPERTY DESCRIPTORS)

<details open>
<summary><b>1. Getter & Setter (Object Accessors)</b> <i>(Bấm để xem)</i></summary>

* **Mục đích:** Đóng gói dữ liệu (Encapsulation), kiểm soát dữ liệu đầu vào (Validation) trước khi gán.
* **Đặc điểm:** Truy cập như một biến thông thường, **không dùng dấu ngoặc tròn `()`**:
```javascript
const user = {
    first: "Thịnh",
    last: "Đỗ",
    age: 20,
    // Getter: Đọc dữ liệu biến đổi
    get fullName() {
        return `${this.first} ${this.last}`;
    },
    // Setter: Kiểm tra hợp lệ trước khi gán
    set updateAge(val) {
        if (val <= 0) return console.log("Tuổi không hợp lệ!");
        this.age = val;
    }
};

console.log(user.fullName); // "Thịnh Đỗ" (Không gọi user.fullName())
user.updateAge = 25;         // Kích hoạt setter -> user.age = 25
user.updateAge = -5;         // "Tuổi không hợp lệ!" (Chặn đứng lỗi)
```
</details>

<br>

<details open>
<summary><b>2. Quản Lý Bộ Cờ Thuộc Tính (Property Descriptors) Với `Object.defineProperty`</b> <i>(Bấm để xem)</i></summary>

* Mỗi thuộc tính trong Object đều sở hữu 3 cờ ngầm kiểm soát quyền hạn:
  * **`writable`:** Nếu `false` $\rightarrow$ Chỉ đọc (Read-only), cấm sửa giá trị.
  * **`enumerable`:** Nếu `false` $\rightarrow$ Tàng hình, không hiện ra khi duyệt `for...in` hay `Object.keys()`.
  * **`configurable`:** Nếu `false` $\rightarrow$ Cấm dùng lệnh `delete` và cấm định nghĩa lại các cờ.
```javascript
const account = { username: "thinhdc" };

Object.defineProperty(account, "id", {
    value: 1001,
    writable: false,      // Cấm sửa id
    enumerable: false,    // Tàng hình
    configurable: false   // Cấm xóa
});

account.id = 9999;     // Vô hiệu!
delete account.id;     // Vô hiệu!
console.log(Object.keys(account)); // Chỉ hiện ["username"], id đã bị tàng hình!
```
* **Các phương thức hỗ trợ:**
  * `Object.getOwnPropertyDescriptor(obj, "prop")`: Đọc bộ cờ của 1 thuộc tính.
  * `Object.defineProperties(obj, descriptors)`: Thiết lập cờ cho nhiều thuộc tính cùng lúc.
  * `Object.getOwnPropertyNames(obj)`: Lấy tất cả tên thuộc tính (kể cả thuộc tính bị tàng hình `enumerable: false`).
</details>

---

#### 3.1.4. DUYỆT ĐỐI TƯỢNG (OBJECT ITERATIONS) & TÍNH NĂNG MỚI ES2024

<details open>
<summary><b>Bộ 4 Phương Thức Duyệt Đối Tượng Hiện Đại</b> <i>(Bấm để xem)</i></summary>

* **`Object.keys(obj)`:** Trả về mảng chứa toàn bộ Key (chuỗi String).
* **`Object.values(obj)`:** Trả về mảng chứa toàn bộ Value.
* **`Object.entries(obj)`:** Trả về mảng chứa các cặp `[Key, Value]`.
* **🌟 `Object.groupBy(items, callback)` (Chuẩn ES2024 mới toanh):**
  * Gom nhóm các phần tử mảng thành một Object theo điều kiện cực nhanh:
  ```javascript
  const users = [
      { name: "An", role: "admin" },
      { name: "Bình", role: "user" },
      { name: "Cường", role: "admin" }
  ];
  const grouped = Object.groupBy(users, u => u.role);
  // Kết quả: { admin: [{ An... }, { Cường... }], user: [{ Bình... }] }
  ```
</details>

---

#### 3.1.5. PROTOTYPE & CHUỖI KẾ THỪA (OBJECT PROTOTYPES)

<details open>
<summary><b>Bản Chất Chuỗi Kế Thừa Prototype & Tối Ưu RAM</b> <i>(Bấm để xem)</i></summary>

* **Bản chất Prototype:** Mọi đối tượng trong JS đều có một con trỏ ngầm trỏ đến `[[Prototype]]` của nó. Chuỗi kế thừa sẽ dừng lại ở `Object.prototype` (đỉnh của chuỗi có `[[Prototype]] === null`).
* **Kỹ thuật tối ưu bộ nhớ RAM (Senior Pattern):**
  * **❌ Không tối ưu:** Khai báo method trực tiếp trong thân Constructor (`this.sayHi = function...`) $\rightarrow$ Sinh ra 10.000 instance sẽ tốn 10.000 ô nhớ cho cùng 1 hàm.
  * **✔️ Chuẩn tối ưu:** Gán method vào `Constructor.prototype` $\rightarrow$ Hàng vạn instance cùng chia sẻ đúng **1 ô nhớ duy nhất**:
    ```javascript
    function User(name) { this.name = name; }
    User.prototype.sayHi = function() { console.log(this.name); };
    ```
* **Kiểm tra thuộc tính chính chủ: `Object.hasOwn(obj, prop)` (Chuẩn ES2022):**
  * Thay thế hoàn toàn `obj.hasOwnProperty()` cũ (vốn dễ bị lỗi khi Object tạo bằng `Object.create(null)`).
</details>

---

#### 3.1.6. CÁC CẤP ĐỘ BẢO VỆ ĐỐI TƯỢNG (OBJECT PROTECTION)

<details open>
<summary><b>Bảng So Sánh 3 Cấp Độ Bảo Vệ Object</b> <i>(Bấm để xem)</i></summary>

| Cấp độ bảo vệ | Thêm mới thuộc tính? | Xóa thuộc tính cũ? | Sửa giá trị hiện có? | Hàm kiểm tra tương ứng |
| :--- | :---: | :---: | :---: | :--- |
| **`Object.preventExtensions(obj)`** | ❌ CẤM | ✔️ Cho phép | ✔️ Cho phép | `Object.isExtensible(obj)` |
| **`Object.seal(obj)` (Niêm phong)** | ❌ CẤM | ❌ CẤM | ✔️ Cho phép | `Object.isSealed(obj)` |
| **`Object.freeze(obj)` (Đóng băng)** | ❌ CẤM | ❌ CẤM | ❌ CẤM | `Object.isFrozen(obj)` |

* **⚠️ BẪY SENIOR VỀ `Object.freeze()`:**
  * `Object.freeze()` chỉ đóng băng **Nông (Shallow Freeze)** ở tầng 1.
  * Nếu Object chứa Object con lồng bên trong, Object con đó **vẫn bị sửa bình thường**! Muốn bất biến toàn diện, bắt buộc phải duyệt đệ quy (Deep Freeze).
</details>

---

#### 3.1.7. BẢNG TRA CỨU PHƯƠNG THỨC OBJECT (OBJECT METHODS W3SCHOOLS)

<details open>
<summary><b>Danh Sách Tra Cứu Toàn Bộ Phương Thức Tĩnh Của Object</b> <i>(Bấm để xem)</i></summary>

* **`Object.assign(target, ...sources)`:** Sao chép các thuộc tính từ một hoặc nhiều source vào target. Ví dụ: `Object.assign({}, { a: 1 })` → `{ a: 1 }`.
* **`Object.create(proto)`:** Tạo object mới kế thừa trực tiếp nguyên mẫu proto. Ví dụ: `Object.create(parentObj)`.
* **`Object.defineProperty(obj, prop, desc)`:** Định nghĩa hoặc sửa đổi thuộc tính kèm bộ cờ (writable, enumerable, configurable).
* **`Object.defineProperties(obj, descs)`:** Định nghĩa nhiều thuộc tính kèm bộ cờ cùng lúc.
* **`Object.entries(obj)`:** Trả về mảng các cặp `[key, value]`. Ví dụ: `Object.entries({ a: 1 })` → `[["a", 1]]`.
* **`Object.fromEntries(iterable)`:** Chuyển đổi danh sách cặp `[key, value]` thành Object. Ví dụ: `Object.fromEntries([["a", 1]])` → `{ a: 1 }`.
* **`Object.freeze(obj)`:** Đóng băng đối tượng (cấm thêm, cấm xóa, cấm sửa).
* **`Object.isFrozen(obj)`:** Kiểm tra đối tượng có đang bị đóng băng hay không (trả về Boolean).
* **`Object.seal(obj)`:** Niêm phong đối tượng (cấm thêm, cấm xóa, cho phép sửa).
* **`Object.isSealed(obj)`:** Kiểm tra đối tượng có đang bị niêm phong hay không.
* **`Object.preventExtensions(obj)`:** Ngăn chặn thêm thuộc tính mới vào đối tượng.
* **`Object.isExtensible(obj)`:** Kiểm tra đối tượng có thể thêm thuộc tính mới được không.
* **`Object.getOwnPropertyDescriptor(obj, prop)`:** Lấy thông tin bộ cờ (descriptor) của một thuộc tính cụ thể.
* **`Object.getOwnPropertyDescriptors(obj)`:** Lấy thông tin bộ cờ của tất cả các thuộc tính trong object.
* **`Object.getOwnPropertyNames(obj)`:** Trả về mảng chứa tất cả tên thuộc tính (kể cả thuộc tính không đếm được - non-enumerable).
* **`Object.getPrototypeOf(obj)`:** Lấy prototype cha ngầm định (`[[Prototype]]`) của đối tượng.
* **`Object.setPrototypeOf(obj, proto)`:** Gán lại prototype cha cho một đối tượng.
* **`Object.hasOwn(obj, prop)`:** Kiểm tra thuộc tính có thuộc sở hữu trực tiếp của object không (chuẩn an toàn ES2022).
* **`Object.is(val1, val2)`:** So sánh 2 giá trị nghiêm ngặt tuyệt đối (xử lý chuẩn xác `Object.is(NaN, NaN) → true` và `Object.is(+0, -0) → false`).
* **`Object.keys(obj)`:** Trả về mảng chứa tất cả key có thể duyệt được (enumerable) của object.
* **`Object.values(obj)`:** Trả về mảng chứa tất cả value tương ứng của object.
* **`Object.groupBy(items, callback)`:** Gom nhóm các phần tử mảng thành Object phân loại (chuẩn ES2024 mới nhất).

</details>

---

### 📌 BÀI 3.2: CÚ PHÁP HIỆN ĐẠI (DESTRUCTURING, SPREAD & TEMPLATE LITERALS)

---

<details open>
<summary><b>1. Kỹ Thuật Phá Vỡ Cấu Trúc (Destructuring Master)</b> <i>(Bấm để xem)</i></summary>

* **Object Destructuring (Bóc tách theo Tên Key):**
  * **Cơ bản:** `const { name, age } = user;`
  * **Đổi tên biến (Alias):** `const { name: fullName } = user;` (Lấy `user.name` gán vào biến mới tên `fullName`).
  * **Giá trị mặc định (Default Value):** `const { role = "Khách" } = user;` (Chỉ kích hoạt khi `user.role === undefined`).
  * **Bóc tách ngay tại tham số hàm (Chuẩn React):**
    ```javascript
    function printUser({ name, role = "User" }) {
        console.log(`${name} - ${role}`);
    }
    ```

* **Array Destructuring (Bóc tách theo Thứ Tự Index):**
  * **Cơ bản:** `const [first, second] = ["A", "B", "C"];` → `first = "A", second = "B"`.
  * **Bỏ qua phần tử:** Dùng dấu phẩy trống: `const [first, , third] = list;`.
  * **Hoán đổi (Swap) 2 biến không cần biến trung gian:**
    ```javascript
    let a = 1, b = 2;
    [a, b] = [b, a]; // a = 2, b = 1
    ```
</details>

<br>

<details open>
<summary><b>2. Toán Tử Rải (Spread Operator `...`) vs Toán Tử Gom (Rest Parameter)</b> <i>(Bấm để xem)</i></summary>

* **Phân biệt bản chất:**
  * **Spread (Bung xòe):** Nằm bên phải dấu `=` hoặc trong tham số truyền vào hàm → Bóc tách các phần tử của mảng/object cũ nhét vào mảng/object mới: `const clone = [...arr];`.
  * **Rest (Gom lại):** Nằm bên trái dấu `=` hoặc ở định nghĩa tham số hàm → Gom toàn bộ phần dư thừa còn lại vào một Mảng: `const [first, ...rest] = arr;`.
* **Cơ chế ghi đè thuộc tính trong Spread Object (Thứ tự cực kỳ quan trọng):**
  * `const obj = { role: "Admin", ...user };` → `user.role` đứng sau sẽ **đè bẹp** `"Admin"`.
  * `const obj = { ...user, role: "Admin" };` → `"Admin"` đứng sau sẽ **ghi đè thành công** `user.role`.
* **Bản chất bộ nhớ:** Spread chỉ là **Shallow Copy (Sao chép nông tầng 1)**. Nếu có Object/Array con ở tầng 2, chúng vẫn trỏ chung ô nhớ trên Memory Heap. Muốn sao chép sâu độc lập 100%, phải dùng **`structuredClone(obj)`**.
</details>

<br>

<details open>
<summary><b>3. Bẫy Phỏng Vấn Senior Về Destructuring</b> <i>(Bấm để xem)</i></summary>

* **Bẫy 1: Sập ứng dụng khi Destructuring từ `null` hoặc `undefined`**
  ```javascript
  const res = null;
  // ❌ LỖI CRASH APP: TypeError: Cannot destructure property 'name' of null
  // const { name } = res;

  // ✔️ Chuẩn phòng thủ Senior: Luôn fallback về Object rỗng {}
  const { name } = res || {}; // name = undefined (An toàn tuyệt đối)
  ```
* **Bẫy 2: Bóc tách vào các biến đã khai báo trước bằng `let/var`:**
  * Nếu viết `{ a, b } = obj;` sẽ bị `SyntaxError` vì JS hiểu nhầm `{}` là khối lệnh (Block).
  * **Bắt buộc bọc trong ngoặc đơn:** `({ a, b } = obj);`.
</details>

<br>

<details open>
<summary><b>4. Chuyên Đề Mở Rộng: Chuỗi Mẫu Template Literals (Dấu Backtick `` `...` ``)</b> <i>(Bấm để xem)</i></summary>

* **Vị trí phím:** Dưới phím `Esc`, chung với dấu `~`.
* **3 Ưu điểm vượt trội so với nháy đơn `'...'` và nháy kép `"..."`:**
  1. **String Interpolation:** Nhúng biến và biểu thức trực tiếp bằng `${...}`, không cần cộng chuỗi `+`. Ví dụ: `` `Xin chào ${name}, ${age + 1} tuổi` ``.
  2. **Multiline Strings:** Cho phép Enter xuống dòng thoải mái mà không bị `SyntaxError`, cực kỳ tối ưu khi viết các đoạn mã HTML trong JavaScript.
  3. **Tagged Templates:** Truyền chuỗi mẫu vào hàm xử lý mà không cần cặp ngoặc tròn `()`: `highlight`Xin chào ${name}``.
* **Quy tắc thực chiến:** Dùng Backtick khi chuỗi có biến hoặc nhiều dòng; dùng nháy đơn/kép cho chuỗi tĩnh đơn giản.
</details>

---

### 📌 BÀI 3.3: MẢNG TOÀN DIỆN (JAVASCRIPT ARRAYS MASTER)

---

#### 3.3.1. BẢN CHẤT MẢNG, CÁCH NHẬN BIẾT & PHÂN BIỆT VỚI OBJECT

<details open>
<summary><b>1. Bẫy Nhận Diện Kiểu & 2 Cách Kiểm Tra Mảng Chuẩn 100%</b> <i>(Bấm để xem)</i></summary>

* **Bẫy `typeof [] === "object"`:** Trong JS, Mảng không phải là kiểu nguyên thủy độc lập mà là một **Object đặc biệt**. Lệnh `typeof []` luôn trả về `"object"`.
* **2 Cách chuẩn Senior để nhận diện Mảng:**
  1. **`Array.isArray(val)` (Chuẩn ES5 — Khuyên dùng số 1):**
     ```javascript
     Array.isArray([1, 2]); // true
     Array.isArray({});     // false
     ```
  2. **Toán tử `instanceof`:**
     ```javascript
     [1, 2] instanceof Array; // true
     ```
</details>

<br>

<details open>
<summary><b>2. So Sánh Mảng vs Đối Tượng & Bẫy Mảng Kết Hợp (Associative Array)</b> <i>(Bấm để xem)</i></summary>

* **Mảng (Array):** Sử dụng **chỉ số được đánh số (Numbered Index: 0, 1, 2...)**. Dùng khi cần lưu trữ danh sách các phần tử có thứ tự.
* **Đối tượng (Object):** Sử dụng **chỉ số được đặt tên (Named Index / Keys)**. Dùng khi cần mô tả chi tiết một thực thể có nhiều thuộc tính văn bản.
* **⚠️ BẪY: JavaScript KHÔNG HỖ TRỢ Mảng kết hợp (Associative Arrays):**
  * Nếu bạn cố tình gán chỉ số dạng chuỗi cho mảng: `arr["name"] = "Thịnh"`:
  * JS Engine xem `"name"` là **thuộc tính tùy biến của Object**, **KHÔNG PHẢI phần tử mảng**.
  * Thuộc tính `arr.length` **hoàn toàn không tăng** (vẫn bằng `0`).
  * Tất cả các hàm mảng (`push`, `pop`, `map`, `forEach`) đều **bỏ qua hoàn toàn** các thuộc tính chuỗi này!
</details>

---

#### 3.3.2. KHỞI TẠO MẢNG & BẪY "LỖI SỐ ĐƠN" (SINGLE NUMBER TRAP)

<details open>
<summary><b>`[]` vs `new Array()` & Cảnh Báo Tử Huyệt `new Array(number)`</b> <i>(Bấm để xem)</i></summary>

* **`new Array()` so với `Array()`:** Không có sự khác biệt về chức năng. JS Engine tự động chèn `new` ngầm nếu bạn quên gõ.
* **⚠️ BẪY "LỖI SỐ ĐƠN" CỰC KỲ NGUY HIỂM:**
  ```javascript
  const a = new Array(1, 2, 3); // [1, 2, 3] (Nhiều số: tạo mảng bình thường)
  const b = new Array("3");     // ["3"] (Chuỗi: mảng 1 phần tử)

  // ❌ BẪY SỐ ĐƠN: Truyền 1 con số duy nhất -> Tạo mảng chứa n ô trống (Empty Slots)!
  const c = new Array(3);       
  console.log(c);               // [empty × 3] (Không phải mảng chứa số 3!)
  console.log(c.length);        // 3
  console.log(c[0]);            // undefined

  // ✔️ Chuẩn an toàn: Luôn dùng cú pháp ngoặc vuông literal []:
  const d = [3];                // Mảng chứa đúng số 3: [3]
  ```
</details>

---

#### 3.3.3. BẢN CHẤT `const` TRONG MẢNG (JS ARRAY CONST)

<details open>
<summary><b>Tại Sao Khai Báo `const` Mà Vẫn Sửa Phần Tử Mảng Được?</b> <i>(Bấm để xem)</i></summary>

* **Nguyên tắc ngầm:** Từ khóa `const` **không làm cho mảng trở nên bất biến**. Nó chỉ khóa cứng **Địa chỉ con trỏ tham chiếu** trên Call Stack, cấm bạn gán lại biến đó sang một vùng nhớ khác.
* **Quy tắc thực chiến:**
  ```javascript
  const numbers = [1, 2, 3];

  // ✔️ HOÀN TOÀN HỢP LỆ (Thao tác trên ô nhớ Heap):
  numbers[0] = 99;      // Sửa
  numbers.push(4);      // Thêm
  numbers.pop();        // Xóa

  // ❌ LỖI CRASH (Cố tình gán lại mảng mới):
  // numbers = [10, 20]; // TypeError: Assignment to constant variable!
  ```
</details>

---

#### 3.3.4. THAO TÁC THÊM, BỚT, CẮT, GHÉP (MUTATING VS NON-MUTATING)

<details open>
<summary><b>1. Phân Loại Phương Thức Đột Biến (Mutating) vs Bất Biến (Non-Mutating)</b> <i>(Bấm để xem)</i></summary>

* **Nhóm Đột Biến (Mutating — Thay đổi trực tiếp mảng gốc):**
  * `push(...items)`: Thêm vào cuối mảng (Hiệu năng cực nhanh: $O(1)$).
  * `pop()`: Rút và trả về phần tử cuối mảng ($O(1)$).
  * `unshift(...items)`: Thêm vào đầu mảng (Chạy chậm: $O(n)$ vì phải đánh lại chỉ số toàn bộ mảng).
  * `shift()`: Rút phần tử đầu mảng ($O(n)$).
  * `splice(start, deleteCount, ...items)`: Phẫu thuật cắt, thêm, thay thế tại vị trí bất kỳ.
  * `reverse()` / `sort()`: Đảo ngược / Sắp xếp trực tiếp trên mảng gốc.

* **Nhóm Bất Biến (Non-Mutating — Trả về mảng mới, giữ nguyên mảng gốc):**
  * `slice(start, end)`: Trích xuất mảng con từ `start` đến sát `end`.
  * `concat(...arrays)`: Ghép nhiều mảng lại thành mảng mới.
  * `flat(depth)`: Làm phẳng mảng lồng nhau nhiều tầng.
  * `join(separator)`: Ghép các phần tử thành chuỗi String.
  * `at(index)` (ES2022): Lấy phần tử theo chỉ mục, hỗ trợ số âm đếm ngược: `arr.at(-1)` lấy phần tử cuối.
</details>

<br>

<details open>
<summary><b>2. Bẫy Tử Huyệt: `slice()` vs `splice()` & Lỗ Hổng Của Toán Tử `delete`</b> <i>(Bấm để xem)</i></summary>

* **Phân biệt sống còn `slice()` vs `splice()`:**
  ```javascript
  const arr = [1, 2, 3, 4];

  // 1. slice(start, end): An toàn, giữ nguyên mảng gốc
  const sub = arr.slice(1, 3); // [2, 3]
  console.log(arr);            // [1, 2, 3, 4] (Không đổi)

  // 2. splice(start, deleteCount): Cắt nát mảng gốc
  const removed = arr.splice(1, 2); // [2, 3]
  console.log(arr);                 // [1, 4] (Mảng gốc BỊ BIẾN DẠNG!)
  ```

* **Cấm dùng toán tử `delete` với phần tử mảng:**
  ```javascript
  const fruits = ["Cam", "Táo", "Xoài"];
  delete fruits[1];
  console.log(fruits);        // ["Cam", empty, "Xoài"] (Tạo ra lỗ hổng rách mảng!)
  console.log(fruits.length); // Vẫn là 3! (Không hề giảm)
  ```

* **Ma thuật của thuộc tính `.length`:**
  ```javascript
  const list = [1, 2, 3, 4, 5];
  list.length = 2; // Cắt cụt mảng: [1, 2]
  list.length = 0; // Làm rỗng sạch mảng siêu tốc!
  ```
</details>

---

#### 3.3.5. HỆ THỐNG TÌM KIẾM MẢNG (JS ARRAY SEARCH)

<details open>
<summary><b>Danh Sách Các Phương Thức Tìm Kiếm Mảng Chuẩn W3Schools</b> <i>(Bấm để xem)</i></summary>

* **`indexOf(item, start)`:** Tìm vị trí đầu tiên của phần tử (trả về `-1` nếu không thấy).
* **`lastIndexOf(item, start)`:** Tìm vị trí xuất hiện cuối cùng tính từ đuôi mảng lên.
* **`includes(item)` (ES2016):** Kiểm tra phần tử có nằm trong mảng không (trả về `true/false`, nhận biết được cả `NaN`).
* **`find(callback)`:** Trả về **giá trị phần tử đầu tiên** thỏa mãn hàm điều kiện.
* **`findIndex(callback)`:** Trả về **chỉ mục (index) đầu tiên** thỏa mãn hàm điều kiện.
* **`findLast(callback)` (ES2023):** Tìm từ cuối mảng lên và trả về **giá trị đầu tiên** khớp điều kiện.
* **`findLastIndex(callback)` (ES2023):** Tìm từ cuối mảng lên và trả về **index đầu tiên** khớp điều kiện.
</details>

---

#### 3.3.6. SẮP XẾP MẢNG (JS ARRAY SORT) & CẬP NHẬT BẤT BIẾN ES2023

<details open>
<summary><b>1. Bẫy Sắp Xếp Số & Cách Tìm Min / Max Chuẩn Xác</b> <i>(Bấm để xem)</i></summary>

* **Bẫy `sort()` mặc định:** Ép mọi phần tử sang chuỗi String và so sánh theo mã Unicode:
  ```javascript
  [1, 100, 25, 5].sort(); // Ra [1, 100, 25, 5] (SAI!)
  
  // ✔️ Bắt buộc truyền Compare Function để so sánh số:
  [1, 100, 25, 5].sort((a, b) => a - b); // Tăng dần: [1, 5, 25, 100]
  [1, 100, 25, 5].sort((a, b) => b - a); // Giảm dần
  ```
* **Tìm Min / Max chuẩn Senior (Độ phức tạp siêu nhanh $O(n)$):**
  ```javascript
  const scores = [40, 100, 1, 5, 25];
  const min = Math.min(...scores); // 1
  const max = Math.max(...scores); // 100
  ```
</details>

<br>

<details open>
<summary><b>2. Bộ Phương Thức ES2023 Không Đột Biến (Non-Mutating Replacements)</b> <i>(Bấm để xem)</i></summary>

* **`toSorted(compareFn)`:** Trả về mảng mới đã sắp xếp, giữ nguyên mảng gốc (thay thế an toàn cho `sort()`).
* **`toReversed()`:** Trả về mảng mới đảo ngược, giữ nguyên mảng gốc (thay thế an toàn cho `reverse()`).
* **`toSpliced(start, deleteCount, ...items)`:** Trả về mảng mới đã qua phẫu thuật cắt ghép, giữ nguyên mảng gốc (thay thế an toàn cho `splice()`).
* **`arr.with(index, value)`:** Thay thế giá trị tại một vị trí index và trả về **mảng mới**, không làm hỏng mảng gốc!
  ```javascript
  const original = ["A", "B", "C"];
  const updated = original.with(1, "MỚI"); // ["A", "MỚI", "C"]
  console.log(original);                  // ["A", "B", "C"] (Nguyên vẹn!)
  ```
</details>

---

#### 3.3.7. BẢNG TRA CỨU PHƯƠNG THỨC MẢNG (ARRAY METHODS W3SCHOOLS)

<details open>
<summary><b>Danh Sách Tra Cứu Toàn Bộ Phương Thức Mảng Thường Dùng</b> <i>(Bấm để xem)</i></summary>

* **`push(...items)`:** Thêm phần tử vào cuối mảng (trả về độ dài mới). Ví dụ: `arr.push(4)`.
* **`pop()`:** Xóa phần tử cuối mảng (trả về phần tử bị xóa). Ví dụ: `arr.pop()`.
* **`unshift(...items)`:** Thêm phần tử vào đầu mảng (trả về độ dài mới). Ví dụ: `arr.unshift(0)`.
* **`shift()`:** Xóa phần tử đầu mảng (trả về phần tử bị xóa). Ví dụ: `arr.shift()`.
* **`splice(start, count, ...items)`:** Cắt/thêm/xóa phần tử tại vị trí bất kỳ (làm đột biến mảng gốc). Ví dụ: `arr.splice(1, 1)`.
* **`slice(start, end)`:** Trích xuất mảng con từ start đến end (không sửa mảng gốc). Ví dụ: `arr.slice(0, 2)`.
* **`concat(...arrays)`:** Nối các mảng lại tạo mảng mới. Ví dụ: `arr1.concat(arr2)`.
* **`flat(depth)`:** Làm phẳng mảng lồng nhau. Ví dụ: `[1, [2]].flat()` → `[1, 2]`.
* **`join(separator)`:** Nối tất cả phần tử mảng thành chuỗi String. Ví dụ: `["A", "B"].join("-")` → `"A-B"`.
* **`reverse()`:** Đảo ngược thứ tự phần tử mảng gốc. Ví dụ: `arr.reverse()`.
* **`sort(compareFn)`:** Sắp xếp phần tử trên mảng gốc (cần hàm so sánh khi sort số). Ví dụ: `arr.sort((a,b)=>a-b)`.
* **`indexOf(item)`:** Tìm vị trí đầu tiên của phần tử (trả về -1 nếu không có). Ví dụ: `arr.indexOf("A")`.
* **`lastIndexOf(item)`:** Tìm vị trí cuối cùng của phần tử từ đuôi mảng lên. Ví dụ: `arr.lastIndexOf("A")`.
* **`includes(item)`:** Kiểm tra phần tử có trong mảng hay không (trả về true/false). Ví dụ: `arr.includes(5)`.
* **`find(callback)`:** Trả về giá trị phần tử đầu tiên khớp điều kiện hàm. Ví dụ: `arr.find(x => x > 10)`.
* **`findIndex(callback)`:** Trả về index đầu tiên khớp điều kiện hàm. Ví dụ: `arr.findIndex(x => x > 10)`.
* **`findLast(callback)`:** Trả về giá trị khớp điều kiện tính từ cuối mảng lên (ES2023).
* **`findLastIndex(callback)`:** Trả về index khớp điều kiện tính từ cuối mảng lên (ES2023).
* **`at(index)`:** Lấy phần tử theo chỉ mục, hỗ trợ số âm đếm ngược (`arr.at(-1)`).
* **`toSorted(compareFn)`:** Sắp xếp và trả về mảng mới (ES2023 không đột biến).
* **`toReversed()`:** Đảo ngược và trả về mảng mới (ES2023 không đột biến).
* **`toSpliced(start, count, ...items)`:** Cắt/sửa và trả về mảng mới (ES2023 không đột biến).
* **`with(index, value)`:** Thay thế giá trị tại index và trả về mảng mới (ES2023 không đột biến).
* **`Array.isArray(val)`:** Phương thức tĩnh kiểm tra xem một giá trị có phải là Mảng không.
* **`Array.from(iterable)`:** Chuyển đổi đối tượng dạng mảng (Array-like) hoặc Iterable thành Mảng thật.

</details>

---

### 📌 BÀI 3.5: CẤU TRÚC DỮ LIỆU HIỆN ĐẠI (JS SETS & JS MAPS TOÀN DIỆN)

---

#### 3.5.1. JS SETS (TẬP HỢP CÁC PHẦN TỬ ĐỘC NHẤT)

<details open>
<summary><b>1. Bản Chất, Khởi Tạo & Ứng Dụng Lọc Trùng Lặp Mảng</b> <i>(Bấm để xem)</i></summary>

* **Bản chất `Set`:** Là cấu trúc dữ liệu tập hợp các giá trị **hoàn toàn không trùng lặp** (mọi giá trị chỉ xuất hiện đúng 1 lần).
* **Đo kích thước:** Dùng thuộc tính **`.size`** (không dùng `.length`).
* **Ứng dụng thực chiến hàng đầu: Lọc sạch trùng lặp trong Mảng:**
  ```javascript
  const numbers = [1, 2, 2, 3, 4, 4, 5];
  const uniqueList = [...new Set(numbers)]; // [1, 2, 3, 4, 5]
  ```
* **Các phương thức cơ bản của Set:**
  * `set.add(value)`: Thêm giá trị mới (nếu đã có thì tự động bỏ qua).
  * `set.has(value)`: Kiểm tra tồn tại với tốc độ cực nhanh ($O(1)$).
  * `set.delete(value)`: Xóa giá trị khỏi Set (trả về `true` nếu xóa thành công).
  * `set.clear()`: Xóa sạch toàn bộ Set.
</details>

<br>

<details open>
<summary><b>2. Tính Năng Mới Toanh ES2024: Toán Tập Hợp (Set Logic)</b> <i>(Bấm để xem)</i></summary>

* Giả sử có 2 tập hợp: `A = new Set([1, 2, 3])` và `B = new Set([3, 4, 5])`:
  * **`A.union(B)` (Phép Hợp):** Lấy tất cả phần tử của cả 2 tập hợp → `{ 1, 2, 3, 4, 5 }`.
  * **`A.intersection(B)` (Phép Giao):** Chỉ lấy phần tử chung mà cả 2 cùng có → `{ 3 }`.
  * **`A.difference(B)` (Phép Hiệu):** Lấy phần tử có trong A nhưng không có trong B → `{ 1, 2 }`.
  * **`A.symmetricDifference(B)` (Hiệu đối xứng):** Lấy phần tử chỉ có ở riêng A hoặc riêng B (loại bỏ phần chung) → `{ 1, 2, 4, 5 }`.
  * **`A.isSubsetOf(B)`:** Kiểm tra A có phải tập con của B không (trả về `false`).
  * **`A.isDisjointFrom(B)`:** Kiểm tra 2 tập hợp có hoàn toàn rời nhau không (trả về `false` vì chung số 3).
</details>

<br>

<details open>
<summary><b>3. JS WeakSet: Bản Chất Con Trỏ Yếu & Chống Rò Rỉ Bộ Nhớ (RAM Leak)</b> <i>(Bấm để xem)</i></summary>

* **Quy tắc nghiêm ngặt:** `WeakSet` **CHỈ ĐƯỢC PHÉP CHỨA OBJECT** (cấm chứa số, chuỗi, boolean).
* **Cơ chế ngầm:** Giữ tham chiếu "yếu" (Weak Reference). Nếu một Object bên trong `WeakSet` không còn được tham chiếu ở bất kỳ nơi nào khác trong chương trình, **bộ dọn rác (Garbage Collector) sẽ tự động giải phóng nó khỏi RAM**.
* **Đặc điểm:** Không có thuộc tính `.size`, không thể duyệt bằng vòng lặp.
</details>

---

#### 3.5.2. JS MAPS (BẢNG TRA CỨU KEY-VALUE LINH HOẠT)

<details open>
<summary><b>1. So Sánh Đẳng Cấp: `Map` vs Đối Tượng Thông Thường (`Object`)</b> <i>(Bấm để xem)</i></summary>

| Tiêu chí | Đối tượng thông thường (Object) | Bảng tra cứu (`Map`) |
| :--- | :--- | :--- |
| **Kiểu dữ liệu của Key** | **Chỉ cho phép String hoặc Symbol** (tự ép số/object thành chuỗi) | **Bất kỳ kiểu gì** (Số, Chuỗi, Boolean, Hàm, Object khác!) |
| **Thứ tự phần tử** | Không đảm bảo thứ tự hoàn toàn | **Luôn bảo toàn nguyên vẹn thứ tự** chèn vào |
| **Lấy số lượng phần tử** | Phải đếm thủ công: `Object.keys(obj).length` | Có sẵn thuộc tính **`.size`** siêu tốc |
| **Hiệu năng (Performance)** | Không tối ưu cho việc thêm/xóa key liên tục | **Tối ưu cực mạnh** cho việc thêm/xóa/tra cứu liên tục |

```javascript
const myMap = new Map();
const userObj = { id: 1 };

// Dùng chính Object làm Key:
myMap.set(userObj, "Dữ liệu đặc biệt của User 1");
console.log(myMap.get(userObj)); // "Dữ liệu đặc biệt của User 1"
```
</details>

<br>

<details open>
<summary><b>2. Các Phương Thức Cốt Lõi Của Map & Cách Duyệt</b> <i>(Bấm để xem)</i></summary>

* **Phương thức cơ bản:**
  * `map.set(key, value)`: Thêm hoặc cập nhật cặp Key-Value.
  * `map.get(key)`: Lấy giá trị tương ứng của Key (nếu không có trả về `undefined`).
  * `map.has(key)`: Kiểm tra Key có tồn tại không ($O(1)$).
  * `map.delete(key)`: Xóa cặp Key-Value khỏi Map.
  * `map.clear()`: Xóa sạch toàn bộ Map.
  * `map.size`: Lấy tổng số lượng phần tử.
* **Duyệt Map bằng vòng lặp `for...of`:**
  ```javascript
  for (const [key, value] of myMap) {
      console.log(`${key} => ${value}`);
  }
  ```
</details>

<br>

<details open>
<summary><b>3. JS WeakMap: Quản Lý Dữ Liệu Riêng Tư & An Toàn Bộ Nhớ</b> <i>(Bấm để xem)</i></summary>

* **Quy tắc:** Key của `WeakMap` **BẮT BUỘC PHẢI LÀ OBJECT**.
* **Cơ chế ngầm:** Khi Object dùng làm Key bị hủy (ví dụ một thẻ DOM bị gỡ khỏi trang web), toàn bộ dữ liệu gắn với Key đó trong `WeakMap` sẽ tự động biến mất, không gây rò rỉ RAM.
* **Ứng dụng thực tế:** Lưu trữ dữ liệu ngầm (Metadata) cho DOM Elements hoặc tạo biến Private cho các Class trong kiến trúc lớn.
</details>

---

#### 3.5.3. BẢNG TRA CỨU PHƯƠNG THỨC SET & MAP (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Danh Sách Tra Cứu Toàn Bộ Phương Thức Set & Map</b> <i>(Bấm để xem)</i></summary>

* **`new Set(iterable)`:** Khởi tạo một đối tượng Set mới từ mảng hoặc iterable.
* **`set.add(value)`:** Thêm một phần tử mới vào Set.
* **`set.delete(value)`:** Xóa phần tử khỏi Set (trả về true/false).
* **`set.has(value)`:** Kiểm tra sự tồn tại của phần tử trong Set.
* **`set.clear()`:** Xóa toàn bộ phần tử trong Set.
* **`set.size`:** Thuộc tính trả về tổng số phần tử của Set.
* **`set.union(otherSet)`:** Phép toán hợp 2 tập hợp (chuẩn ES2024).
* **`set.intersection(otherSet)`:** Phép toán giao 2 tập hợp (chuẩn ES2024).
* **`set.difference(otherSet)`:** Phép toán hiệu giữa 2 tập hợp (chuẩn ES2024).
* **`set.symmetricDifference(otherSet)`:** Phép toán hiệu đối xứng 2 tập hợp (chuẩn ES2024).
* **`new Map(iterable)`:** Khởi tạo một đối tượng Map mới.
* **`map.set(key, value)`:** Thiết lập giá trị cho một key trong Map.
* **`map.get(key)`:** Lấy giá trị của key trong Map.
* **`map.has(key)`:** Kiểm tra key có tồn tại trong Map hay không.
* **`map.delete(key)`:** Xóa cặp key-value khỏi Map.
* **`map.clear()`:** Xóa toàn bộ dữ liệu trong Map.
* **`map.size`:** Thuộc tính trả về số lượng cặp key-value trong Map.
* **`map.keys()`:** Trả về một Iterator chứa toàn bộ key của Map.
* **`map.values()`:** Trả về một Iterator chứa toàn bộ value của Map.
* **`map.entries()`:** Trả về một Iterator chứa toàn bộ cặp `[key, value]` của Map.

</details>

---

### 📌 BÀI 3.4: BỘ HÀM MẢNG SỐNG CÒN (HIGHER-ORDER FUNCTIONS) & GIAO THỨC ITERATOR

---

> [!IMPORTANT]
> ## 🚨 TRỌNG TÂM CHIẾN LƯỢC SỐ 1: BỘ BA "BẢN NĂNG SỐNG CÒN" CỦA FRONTEND (`map`, `filter`, `forEach`)
> 
> **TẠI SAO BẠN PHẢI DÀNH 70% THỜI GIAN LUYỆN TẬP CHO 3 HÀM NÀY?**
> 
> * **Trong JavaScript thuần (Vanilla JS):** 90% các bài toán từ Giỏ hàng, To-do list, Danh sách bài viết đến Xử lý dữ liệu API đều xoay quanh việc duyệt (`forEach`), lọc bỏ phần tử (`filter`), và biến đổi dữ liệu sang giao diện (`map`).
> * **Khi bước chân sang REACT / VUE / NEXT.JS (BẮT BUỘC 100%):**
>   * 👉 **`map()` chính là linh hồn của React JSX:** Trong React **hoàn toàn không có vòng lặp `for`**! 100% các danh sách hiển thị trên màn hình (danh sách sản phẩm, tin nhắn chat, comment, thông báo) đều được render bằng cú pháp: `{items.map(item => <Card key={item.id} data={item} />)}`. Không thành thạo `map()`, bạn **không thể viết nổi 1 dòng code React**!
>   * 👉 **`filter()` là bản quyền của Quản lý State (Immutability):** Khi người dùng bấm nút **XÓA** một sản phẩm khỏi giỏ hàng, xóa một ghi chú, hay lọc sản phẩm dưới 500k, bạn **tuyệt đối không dùng `splice` làm rách mảng gốc**, mà luôn luôn dùng: `setCart(cart.filter(item => item.id !== idDelete))`.
>   * 👉 **`forEach()` là công cụ kích hoạt hành động phụ (Side-effects):** Dùng để duyệt qua mảng kích hoạt hàm lưu ổ cứng, tính toán phụ trợ, hoặc vẽ thẻ DOM thủ công.

---

#### 3.4.1. BẢNG ĐỐI CHIẾU "KHẮC CỐT GHI TÂM": BỘ BA THẦN THÁNH

<details open>
<summary><b>Bảng So Sánh Cốt Lõi, Hình Ảnh Ẩn Dụ & Ví Dụ Thực Tế</b> <i>(Bấm để xem)</i></summary>

| Tiêu chí | `forEach()` 🚶 | `map()` 🏭 | `filter()` 🔍 |
| :--- | :--- | :--- | :--- |
| **Hình ảnh ẩn dụ** | **Bác bảo vệ điểm danh:** Đi qua từng người, nhắc nhở làm việc nhưng **không tạo ra ai mới**. | **Chiếc máy biến hình 1-1:** Đưa vào 1 quả cam → Nhả ra 1 ly nước cam. **Số lượng đầu ra luôn bằng đúng số lượng đầu vào.** | **Chiếc lưới lọc cà phê:** Chỉ giữ lại những hạt đủ tiêu chuẩn. **Số lượng đầu ra luôn nhỏ hơn hoặc bằng đầu vào.** |
| **Mục đích duy nhất** | Duyệt tuần tự để thực hiện hành động (chạy hàm, in log, gọi DOM). | Biến đổi từng phần tử thành một giá trị mới (tạo mảng mới). | Lọc các phần tử thỏa mãn điều kiện logic (tạo mảng mới). |
| **Giá trị trả về** | **`undefined`** *(Tuyệt đối không gán biến!)* | **Mảng MỚI tinh** cùng độ dài. | **Mảng MỚI tinh** chứa các phần tử thỏa mãn. |
| **Bảo toàn mảng gốc?**| Có (trừ khi tự tay sửa thuộc tính con). | **100% Bảo toàn mảng gốc** (Không đột biến). | **100% Bảo toàn mảng gốc** (Không đột biến). |
| **Quy tắc `return`** | **Không quan tâm `return`** (viết return vô nghĩa). | **BẮT BUỘC PHẢI RETURN** (Quên return mảng mới nhận `undefined`!). | **BẮT BUỘC RETURN BOOLEAN** (`true` thì giữ, `false` thì vứt bỏ). |
| **Cú pháp ngắn gọn** | `arr.forEach(item => doAction(item));` | `const newArr = arr.map(item => transform(item));` | `const newArr = arr.filter(item => condition(item));` |

* **Code đối chiếu trực quan 3 hàm trên cùng 1 mảng số:**
  ```javascript
  const numbers = [1, 2, 3, 4, 5];

  // 1. forEach: Chỉ làm việc, không trả về mảng
  numbers.forEach(num => console.log(`Số: ${num}`)); // In ra 5 dòng

  // 2. map: Nhân đôi từng số (Số lượng phần tử = 5)
  const doubled = numbers.map(num => num * 2); 
  console.log(doubled); // [2, 4, 6, 8, 10] (Mảng mới)

  // 3. filter: Chỉ giữ lại các số chẵn (Số lượng phần tử <= 5)
  const evens = numbers.filter(num => num % 2 === 0); 
  console.log(evens); // [2, 4] (Mảng mới)
  ```

---

* **`reduce(callback, initialValue)` — "Máy nghiền gom mảng về 1 kết quả duy nhất":**
  * Cú pháp: `arr.reduce((accumulator, currentValue, index, arr) => { ... }, initialValue)`.
  * `accumulator` (Biến tích lũy): Giữ kết quả của các bước trước để cộng dồn hoặc gom nhóm.
  * **Ứng dụng:** Tính tổng tiền giỏ hàng (`reduce((sum, item) => sum + item.price * item.quantity, 0)`), đếm số lượng xuất hiện, gom nhóm Object.
  ```javascript
  const prices = [10, 20, 30];
  const total = prices.reduce((acc, cur) => acc + cur, 0); // 60
  ```

* **`flatMap(callback)` (ES2019):** Kết hợp giữa `map()` và làm phẳng `flat(1)` trong 1 bước duy nhất.
</details>

---

#### 3.4.2. CÁC HÀM KIỂM TRA ĐIỀU KIỆN (`some`, `every`) & TÌM KIẾM (`find`, `findIndex`)

<details open>
<summary><b>Kiểm Tra Logic & Tìm Kiếm Tối Ưu</b> <i>(Bấm để xem)</i></summary>

* **`some(callback)` (Toán tử OR):** Kiểm tra xem có **ít nhất 1 phần tử** thỏa mãn điều kiện hay không (Dừng lặp và trả về `true` ngay khi thấy phần tử đầu tiên khớp).
* **`every(callback)` (Toán tử AND):** Kiểm tra xem **tất cả phần tử** có thỏa mãn điều kiện không (Dừng lặp và trả về `false` ngay khi gặp phần tử đầu tiên không khớp).
* **`find(callback)`:** Trả về **giá trị phần tử đầu tiên** thỏa mãn điều kiện (nếu không thấy trả về `undefined`).
* **`findIndex(callback)`:** Trả về **chỉ mục (index) đầu tiên** thỏa mãn điều kiện (nếu không thấy trả về `-1`).
</details>

---

#### 3.4.3. BẪY PHỎNG VẤN SENIOR KINH ĐIỂN VỀ HÀM MẢNG

<details open>
<summary><b>3 Bẫy Tử Huyệt & Kỹ Thuật Method Chaining</b> <i>(Bấm để xem)</i></summary>

* **Bẫy 1: Quên `return` trong hàm `map()`:**
  * Nếu dùng ngoặc nhọn `{}` mà quên viết từ khóa `return`, mảng trả về sẽ toàn là `[undefined, undefined...]`.

* **Bẫy 2: BẪY NỔI TIẾNG NHẤT LỊCH SỬ JS: `["1", "2", "3"].map(parseInt)`:**
  * **Kết quả thực tế:** `[1, NaN, NaN]` (không phải `[1, 2, 3]`).
  * **Bản chất:** `map` truyền 3 đối số: `(item, index, array)`. Hàm `parseInt(str, radix)` lấy `index` làm **hệ cơ số (radix)**:
    * `parseInt("1", 0)` → coi như hệ thập phân → `1`.
    * `parseInt("2", 1)` → hệ cơ số 1 không tồn tại → `NaN`.
    * `parseInt("3", 2)` → số 3 trong hệ nhị phân không hợp lệ → `NaN`.
  * **Cách viết chuẩn:** `["1", "2", "3"].map(x => parseInt(x))`.

* **Bẫy 3: Quên `initialValue` trong `reduce()`:**
  * Nếu mảng rỗng `[]`, lệnh `[].reduce((a, b) => a + b)` sẽ **văng lỗi ngay lập tức: `TypeError: Reduce of empty array with no initial value`** làm sập toàn bộ ứng dụng! Luôn luôn truyền giá trị khởi tạo `0`, `""`, `[]` hoặc `{}`.

* **Kỹ thuật Method Chaining (Pipeline xử lý dữ liệu):**
  ```javascript
  const orders = [
      { name: "A", price: 100, status: "paid" },
      { name: "B", price: 200, status: "pending" },
      { name: "C", price: 300, status: "paid" }
  ];
  // Lọc đơn đã thanh toán -> Bóc tách giá tiền -> Tính tổng:
  const revenue = orders
      .filter(o => o.status === "paid")
      .map(o => o.price)
      .reduce((sum, price) => sum + price, 0); // 400
  ```

* **🌟 VÍ DỤ THỰC CHIẾN 1 (Dùng `map` định dạng dữ liệu API cho giao diện):**
  ```javascript
  const rawApiUsers = [
      { id: 101, first: "Thịnh", last: "Đỗ", birthYear: 2004 },
      { id: 102, first: "An", last: "Nguyễn", birthYear: 2000 }
  ];
  // Biến đổi thô thành DTO sạch cho UI:
  const uiUsers = rawApiUsers.map(u => ({
      id: u.id,
      fullName: `${u.first} ${u.last}`,
      age: 2026 - u.birthYear
  }));
  // Kết quả: [{ id: 101, fullName: "Thịnh Đỗ", age: 22 }, ...]
  ```

* **🌟 VÍ DỤ THỰC CHIẾN 2 (Dùng `reduce` đếm tần suất xuất hiện - Biến Mảng thành Object thống kê):**
  ```javascript
  const fruits = ["cam", "táo", "cam", "chuối", "táo", "cam"];
  // Đếm mỗi loại quả xuất hiện bao nhiêu lần:
  const fruitCounts = fruits.reduce((acc, fruit) => {
      acc[fruit] = (acc[fruit] || 0) + 1;
      return acc;
  }, {}); // initialValue là Object rỗng {}
  // Kết quả: { cam: 3, táo: 2, chuối: 1 }
  ```
</details>

---

#### 3.4.4. CHUYÊN ĐỀ SÂU: GIAO THỨC ITERATOR & ITERABLE PROTOCOL

<details open>
<summary><b>Bản Chất Động Cơ Ngầm Của Vòng Lặp `for...of` và Toán Tử `...`</b> <i>(Bấm để xem)</i></summary>

* **Câu hỏi cốt lõi:** Tại sao Mảng, Chuỗi, Set, Map duyệt được bằng `for...of` và bung xòe được bằng `...` (Spread), trong khi Object thông thường thì không?
* **Bản chất ngầm: Giao thức Iterable (Iterable Protocol):**
  * Một cấu trúc dữ liệu được coi là **Iterable (Có thể duyệt)** khi và chỉ khi nó sở hữu một phương thức ẩn mang tên: **`[Symbol.iterator]()`**.
  * Khi `for...of` chạy, JS Engine sẽ gọi hàm `[Symbol.iterator]()` này. Hàm này trả về một **Iterator Object**.
* **Giao thức Iterator (Iterator Protocol):**
  * Iterator Object bắt buộc phải có một hàm tên là **`.next()`**.
  * Mỗi lần gọi `.next()`, nó nhả ra một đối tượng gồm 2 thuộc tính:
    * **`value`:** Giá trị của phần tử hiện tại.
    * **`done`:** `Boolean` — Nếu `false` nghĩa là vẫn còn dữ liệu để duyệt tiếp; nếu `true` nghĩa là đã hết mảng, vòng lặp dừng lại!

* **⚠️ ĐÍNH CHÍNH QUAN TRỌNG: `for...in` KHÔNG PHẢI LÀ ITERATOR!**
  * `for (x in obj)`: Chỉ là vòng lặp duyệt **tên thuộc tính (Keys)** thời ES1, không dùng giao thức Iterator.
  * `for (x of arr)`: **ĐÂY MỚI LÀ ITERATOR**! Biến `x` chính là `value` mà Iterator nhả ra sau mỗi lần gọi ngầm `.next()`.

* **Ví dụ đối chiếu trực quan: Bấm tay `.next()` vs `for...of` tự động:**
  ```javascript
  const names = ["An", "Bình"];

  // 1. CƠ CHẾ NGẦM (Nếu bạn tự gọi Iterator bằng tay):
  const myIte = names[Symbol.iterator]();
  console.log(myIte.next()); // { value: "An", done: false }   -> Lấy được "An"
  console.log(myIte.next()); // { value: "Bình", done: false } -> Lấy được "Bình"
  console.log(myIte.next()); // { value: undefined, done: true } -> Báo hết!

  // 2. VÒNG LẶP for...of (Chính là người tự động bấm .next() thay bạn):
  for (const name of names) {
      console.log(name); // Tự động in "An", rồi in "Bình", gặp done: true thì dừng!
  }
  ```
</details>

---

#### 3.4.5. BẢNG TRA CỨU CÁC PHƯƠNG THỨC DUYỆT MẢNG (ARRAY ITERATION METHODS)

<details open>
<summary><b>Danh Sách Tra Cứu Toàn Bộ Phương Thức Lặp Mảng Chuẩn W3Schools</b> <i>(Bấm để xem)</i></summary>

* **`forEach(callback)`:** Gọi hàm callback cho từng phần tử (luôn trả về undefined).
* **`map(callback)`:** Tạo mảng mới từ kết quả biến đổi của callback trên từng phần tử.
* **`flatMap(callback)`:** Tạo mảng mới bằng cách map từng phần tử rồi làm phẳng kết quả 1 cấp (ES2019).
* **`filter(callback)`:** Tạo mảng mới chỉ chứa các phần tử vượt qua bài kiểm tra logic của callback.
* **`reduce(callback, init)`:** Thu gọn mảng thành một giá trị duy nhất từ trái sang phải.
* **`reduceRight(callback, init)`:** Thu gọn mảng thành một giá trị duy nhất từ phải sang trái.
* **`every(callback)`:** Kiểm tra xem toàn bộ phần tử trong mảng có thỏa mãn điều kiện không (trả về Boolean).
* **`some(callback)`:** Kiểm tra xem có ít nhất một phần tử thỏa mãn điều kiện không (trả về Boolean).
* **`from(iterable)`:** Phương thức tĩnh `Array.from()` tạo mảng mới từ một đối tượng có tính lặp (Iterable) hoặc Array-like.
* **`keys()`:** Trả về một Iterator chứa toàn bộ chỉ số index của mảng.
* **`values()`:** Trả về một Iterator chứa toàn bộ giá trị phần tử của mảng.
* **`entries()`:** Trả về một Iterator chứa toàn bộ cặp `[index, value]` của mảng.

</details>

---

### 📌 BÀI 3.6: TIỆN ÍCH SỐ HỌC & THỜI GIAN (JS MATH & JS DATES TOÀN DIỆN)

---

#### 3.6.1. ĐỐI TƯỢNG SỐ HỌC (JS MATH)

<details open>
<summary><b>1. Bản Chất, Bộ 4 Hàm Làm Tròn & Bẫy Random Xác Suất</b> <i>(Bấm để xem)</i></summary>

* **Bản chất:** `Math` là một **Đối tượng Tĩnh (Static Object)**, không dùng từ khóa `new Math()`.
* **Bộ 4 hàm làm tròn cần phân biệt:**
  * **`Math.round(x)`:** Làm tròn đến số nguyên gần nhất ($\ge 0.5$ làm tròn lên). Ví dụ: `Math.round(4.5) → 5`, `Math.round(4.4) → 4`.
  * **`Math.ceil(x)` (Trần nhà):** Luôn làm tròn **LÊN** số nguyên lớn hơn. Ví dụ: `Math.ceil(4.1) → 5`, `Math.ceil(-4.9) → -4`.
  * **`Math.floor(x)` (Sàn nhà):** Luôn làm tròn **XUỐNG** số nguyên nhỏ hơn. Ví dụ: `Math.floor(4.9) → 4`, `Math.floor(-4.1) → -5`.
  * **`Math.trunc(x)` (Chặt cụt - ES6):** Cắt bỏ hoàn toàn phần thập phân, lấy phần nguyên. Ví dụ: `Math.trunc(4.9) → 4`.

* **Bẫy `Math.random()` & Công thức sinh số nguyên ngẫu nhiên chuẩn toán học:**
  * `Math.random()` trả về số thực trong khoảng `[0, 1)` (không bao giờ chạm tới 1).
  * **❌ LỖI NẶNG XÁC SUẤT:** Dùng `Math.round(Math.random() * 10)` khiến 2 đầu mút `0` và `10` có xác suất chỉ bằng 50% các số ở giữa.
  * **✔️ CÔNG THỨC CHUẨN XÁC (Dùng `Math.floor`):**
    ```javascript
    function getRandomInt(min, max) {
        return Math.floor(Math.random() * (max - min + 1)) + min;
    }
    // Đổ xúc xắc từ 1 đến 6 đồng đều 100%:
    console.log(getRandomInt(1, 6));
    ```
</details>

---

#### 3.6.2. KHỞI TẠO DATE & 9 CÁCH TẠO THEO W3SCHOOLS

<details open>
<summary><b>Bản Chất Unix Epoch & 2 Bẫy Khởi Tạo Kinh Điển</b> <i>(Bấm để xem)</i></summary>

* **Bản chất thời gian:** Lưu trữ dưới dạng con số **Mili-giây (ms)** trôi qua kể từ mốc lịch sử **Unix Epoch: 00:00:00 UTC ngày 01/01/1970**.
* **9 Cách khởi tạo `new Date()` theo W3Schools:**
  1. `new Date()`: Lấy thời điểm hiện tại.
  2. `new Date(dateString)`: Khởi tạo từ chuỗi văn bản.
  3. `new Date(year, month)`: Năm và Tháng.
  4. `new Date(year, month, day)`: Năm, Tháng, Ngày.
  5. `new Date(year, month, day, hours)`
  6. `new Date(year, month, day, hours, minutes)`
  7. `new Date(year, month, day, hours, minutes, seconds)`
  8. `new Date(year, month, day, hours, minutes, seconds, ms)`
  9. `new Date(milliseconds)`: Tạo từ số timestamp.
* **⚠️ 2 BẪY TỬ HUYỆT:**
  * **Tháng đếm từ `0` đến `11`:** Tháng 1 là `0`, tháng 12 là `11`. Muốn tạo tháng 10 phải truyền số `9`: `new Date(2026, 9, 15)`. (Lưu ý quái đản: Tháng đếm từ `0`, nhưng Ngày lại đếm từ `1`).
  * **Năm 2 chữ số:** `new Date(99, 5, 24)` tự động bị hiểu là năm của thế kỷ trước: **1999**.
</details>

---

#### 3.6.3. ĐỊNH DẠNG NGÀY THÁNG (DATE FORMATS & PARSING)

<details open>
<summary><b>Chuẩn ISO 8601, Short Date & Bóc Tách Bằng `Date.parse()`</b> <i>(Bấm để xem)</i></summary>

* **Chuẩn ISO 8601 (Khuyên dùng số 1 trong ngành phần mềm):**
  * Cú pháp: `YYYY-MM-DD` hoặc `YYYY-MM-DDTHH:MM:SSZ`
  * Chữ `T`: Tách ngày và giờ. Chữ `Z`: Chỉ định múi giờ quốc tế chuẩn UTC.
  * Ví dụ: `"2026-09-14T10:45:00Z"`.
* **Cảnh báo Short Date (`"MM/DD/YYYY"`):** Ở Việt Nam hay viết `"DD/MM/YYYY"`, nhưng nếu truyền chuỗi này vào nhiều trình duyệt sẽ bị văng lỗi **`Invalid Date`**! Luôn dùng chuẩn ISO.
* **Bóc tách timestamp bằng `Date.parse(string)`:**
  * Chuyển chuỗi ngày tháng hợp lệ thành số mili-giây siêu tốc mà không cần tạo Object:
  ```javascript
  const ms = Date.parse("2026-09-14");
  console.log(ms); // 1789430400000 (ms)
  ```
</details>

---

#### 3.6.4. CÁC HÀM LẤY DỮ LIỆU (GET METHODS) & UTC

<details open>
<summary><b>Đọc Ngày Giờ Địa Phương vs Giờ Quốc Tế UTC</b> <i>(Bấm để xem)</i></summary>

* **Get Methods theo giờ máy tính (Local Time):**
  * `getFullYear()`: Năm 4 chữ số (CẤM dùng `getYear()` vì trả về năm - 1900).
  * `getMonth()`: Tháng ($0 - 11$).
  * `getDate()`: Ngày trong tháng ($1 - 31$).
  * `getDay()`: Thứ trong tuần ($0$ là Chủ Nhật, $1$ là Thứ Hai... $6$ là Thứ Bảy).
  * `getHours()`, `getMinutes()`, `getSeconds()`, `getMilliseconds()`.
  * `getTime()`: Số mili-giây từ năm 1970 (Timestamp).
  * `getTimezoneOffset()`: Độ chênh lệch phút so với UTC (Việt Nam UTC+7 trả về `-420` phút).
* **Lấy timestamp hiện tại siêu tốc:** Dùng **`Date.now()`** (không cần tạo `new Date()`).
* **Họ hàm quốc tế (UTC Get Methods):** `getUTCFullYear()`, `getUTCMonth()`, `getUTCDate()`, `getUTCHours()`... (giúp thống nhất giờ cho người dùng toàn cầu).
</details>

---

#### 3.6.5. CÁC HÀM THIẾT LẬP (SET METHODS) & SO SÁNH NGÀY THÁNG

<details open>
<summary><b>Thiết Lập Ngày Giờ, Tự Động Cuộn Tháng & Bẫy So Sánh `===`</b> <i>(Bấm để xem)</i></summary>

* **Các hàm Set:** `setFullYear()`, `setMonth()`, `setDate()`, `setHours()`, `setMinutes()`, `setSeconds()`.
* **Tính năng tự động cuộn tháng thần kỳ của `setDate`:**
  ```javascript
  const d = new Date();
  d.setDate(d.getDate() + 50); // Tự động nhảy sang đúng tháng sau 50 ngày nữa!
  ```
* **So sánh lớn hơn / nhỏ hơn (`>` / `<`):** Hoạt động chuẩn xác vì JS tự động ép Date sang Timestamp (ms).
* **⚠️ BẪY TỬ HUYỆT: CẤM SO SÁNH BẰNG NHAU VỚI `===` HOẶC `==`!**
  ```javascript
  const d1 = new Date("2026-09-14");
  const d2 = new Date("2026-09-14");
  console.log(d1 === d2); // false! (2 Object ở 2 ô nhớ Heap khác nhau: #001 !== #002)

  // ✔️ ĐÚNG: Bắt buộc so sánh thông qua Timestamp số mili-giây:
  console.log(d1.getTime() === d2.getTime()); // true 100%!
  ```
* **Tính khoảng cách số ngày giữa 2 mốc thời gian:**
  ```javascript
  const d1 = new Date("2026-09-01");
  const d2 = new Date("2026-09-15");
  const diffDays = Math.floor((d2 - d1) / (1000 * 60 * 60 * 24)); // 14 ngày
  ```
</details>

---

#### 3.6.6. BẢNG TRA CỨU PHƯƠNG THỨC MATH & DATE (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Danh Sách Tra Cứu Toàn Bộ Phương Thức Math & Date</b> <i>(Bấm để xem)</i></summary>

* **`Math.round(x)`:** Làm tròn đến số nguyên gần nhất.
* **`Math.ceil(x)`:** Làm tròn lên số nguyên lớn hơn.
* **`Math.floor(x)`:** Làm tròn xuống số nguyên nhỏ hơn.
* **`Math.trunc(x)`:** Chặt bỏ phần thập phân, giữ phần nguyên.
* **`Math.sign(x)`:** Trả về dấu của số (1: dương, -1: âm, 0: bằng 0).
* **`Math.pow(x, y)`:** Lũy thừa $x^y$ (hoặc `x ** y`).
* **`Math.sqrt(x)`:** Căn bậc hai của x.
* **`Math.abs(x)`:** Giá trị tuyệt đối của x.
* **`Math.min(...vals)`:** Tìm giá trị nhỏ nhất trong danh sách.
* **`Math.max(...vals)`:** Tìm giá trị lớn nhất trong danh sách.
* **`Math.random()`:** Sinh số thực ngẫu nhiên trong khoảng `[0, 1)`.
* **`new Date()`:** Khởi tạo đối tượng Date tại thời điểm hiện tại.
* **`Date.now()`:** Trả về timestamp hiện tại (ms) siêu tốc.
* **`Date.parse(str)`:** Bóc tách chuỗi ngày thành số timestamp ms.
* **`date.getFullYear()`:** Lấy năm 4 chữ số.
* **`date.getMonth()`:** Lấy tháng (0 - 11).
* **`date.getDate()`:** Lấy ngày trong tháng (1 - 31).
* **`date.getDay()`:** Lấy thứ trong tuần (0: CN, 1: T2... 6: T7).
* **`date.getTime()`:** Lấy số ms từ Unix Epoch (1970).
* **`date.toISOString()`:** Chuyển đổi sang chuỗi chuẩn ISO 8601 UTC.
* **`date.toLocaleDateString(locale)`:** Định dạng ngày theo ngôn ngữ địa phương (`"vi-VN"`).
* **`date.toLocaleTimeString(locale)`:** Định dạng giờ theo ngôn ngữ địa phương.
* **`date.setDate(day)`:** Thiết lập ngày trong tháng (tự động cuộn tháng nếu tràn).
* **`date.setFullYear(year)`:** Thiết lập năm.

</details>

---

### 📌 BÀI 3.7: BIỂU THỨC CHÍNH QUY (JAVASCRIPT REGEXP MASTER)

---

#### 3.7.1. CÚ PHÁP, 2 CÁCH KHỞI TẠO & CÁC LÁ CỜ BỔ TRỢ (FLAGS)

<details open>
<summary><b>1. Cú Pháp `/pattern/flags` & 6 Lá Cờ Bổ Trợ</b> <i>(Bấm để xem)</i></summary>

* **Cú pháp chung:** `/mẫu_tìm_kiếm/các_lá_cờ`
* **2 Cách khởi tạo:**
  * **Literal (Dùng 95%):** `const re = /thinh/i;`
  * **Constructor:** `const re = new RegExp("thinh", "i");` (Dùng khi từ khóa tìm kiếm là biến động).
* **6 Lá cờ điều khiển (Flags):**
  * **`i` (Ignore case):** Không phân biệt chữ hoa, chữ thường (`/abc/i` khớp cả `"ABC"`, `"Abc"`).
  * **`g` (Global):** Tìm kiếm toàn cục (tìm tất cả vị trí khớp, không dừng lại ở vị trí đầu tiên).
  * **`m` (Multiline):** Cho phép `^` và `$` khớp ở đầu và cuối của **mỗi dòng**.
  * **`s` (DotAll):** Cho phép dấu chấm `.` khớp được cả ký tự xuống dòng `\n`.
  * **`u` (Unicode):** Bật chế độ hỗ trợ đầy đủ ký tự Unicode và Emoji.
  * **`y` (Sticky):** Tìm kiếm dính chặt tại vị trí chỉ số `lastIndex`.
</details>

---

#### 3.7.2. BỘ 4 MẢNH GHÉP LEGO NỀN TẢNG CỦA MỌI MẪU REGEX

<details open>
<summary><b>Bảng Tra Cứu Ký Tự Đại Diện, Số Lượng & Ranh Giới</b> <i>(Bấm để xem)</i></summary>

* **1. Nhóm Tập Ký Tự (Classes `[...]`):**
  * `[abc]`: Khớp 1 ký tự duy nhất: hoặc `a`, hoặc `b`, hoặc `c`.
  * `[^abc]`: **Dấu `^` bên trong ngoặc vuông là PHỦ ĐỊNH** → Khớp bất kỳ ký tự nào **NGOẠI TRỪ** `a`, `b`, `c`.
  * `[0-9]`: Khớp 1 chữ số bất kỳ từ 0 đến 9.
  * `[a-z]`: Khớp 1 chữ thường từ a đến z.
  * `[a-zA-Z0-9]`: Khớp 1 chữ cái hoặc 1 chữ số.

* **2. Nhóm Ký Tự Viết Tắt (Metacharacters):**
  * `\d` *(digit)*: Đại diện cho 1 chữ số (tương đương `[0-9]`).
  * `\D`: Không phải chữ số (tương đương `[^0-9]`).
  * `\w` *(word)*: Chữ cái, chữ số và dấu gạch dưới `_` (tương đương `[a-zA-Z0-9_]`).
  * `\W`: Không phải ký tự word (khoảng trắng, ký tự đặc biệt `@`, `#`, `$`...).
  * `\s` *(space)*: Khoảng trắng, phím cách, tab, xuống dòng.
  * `\S`: Không phải khoảng trắng.
  * `.` *(dấu chấm)*: Bất kỳ ký tự nào (ngoại trừ xuống dòng).
  * `\.`: Dấu chấm thực sự (phải có dấu thoát `\` phía trước).

* **3. Nhóm Số Lượng Lặp (Quantifiers):**
  * `+`: Xuất hiện **ít nhất 1 lần** (từ 1 lần trở lên).
  * `*`: Xuất hiện **0 hoặc nhiều lần** (có cũng được, không có cũng được).
  * `?`: Xuất hiện **0 hoặc 1 lần** (tùy chọn - Optional).
  * `{n}`: Xuất hiện **chính xác n lần**. Ví dụ: `\d{4}` là đúng 4 chữ số.
  * `{min, max}`: Xuất hiện từ min đến max lần. Ví dụ: `\d{9,11}`.
  * `{min,}`: Xuất hiện từ min lần trở lên.

* **4. Nhóm Ranh Giới Neo Chốt (Assertions):**
  * `^` (ở đầu mẫu): Bắt buộc chuỗi phải bắt đầu ngay tại đây.
  * `$` (ở cuối mẫu): Bắt buộc chuỗi phải kết thúc ngay tại đây.
  * **Ý nghĩa:** Thiếu `^` và `$`, regex chỉ kiểm tra xem chuỗi có *chứa* mẫu hay không, chứ không bắt buộc *toàn bộ chuỗi* phải khớp!
</details>

---

#### 3.7.3. MỔ XẺ GIẢI MÃ CHI TIẾT TỪNG KÝ TỰ CÁC MẪU REGEX THỰC CHIẾN

<details open>
<summary><b>🔍 Bóc Tách Từng Ký Tự Mẫu Email & Mật Khẩu Mạnh (Dễ Hiểu 100%)</b> <i>(Bấm để xem)</i></summary>

---

#### 📌 MẪU 1: KIỂM TRA EMAIL CƠ BẢN
```text
/^[^\s@]+@[^\s@]+\.[^\s@]+$/
```

> **Mục tiêu:** Đảm bảo chuỗi có cấu trúc `tên_người_dùng@tên_miền.đuôi_miền` (ví dụ: `thinh.dc@gmail.com`), không có khoảng trắng thừa.

##### Bảng giải phẫu từng ký tự từ trái qua phải:

| Ký hiệu | Tên gọi | Ý nghĩa chi tiết (Bình dân hóa) |
| :--- | :--- | :--- |
| `/ ... /` | Khung bao Regex | Dấu hiệu nhận biết biểu thức chính quy trong JS. Mọi quy tắc nằm ở giữa 2 dấu gạch chéo này. |
| `^` | Neo đầu chuỗi *(Anchor)* | Bắt buộc phải khớp ngay từ ký tự đầu tiên của chuỗi, cấm có chữ rác đằng trước. |
| `[` và `]` | Tập ký tự *(Character Set)* | Nhóm chứa các ký tự cần chọn. Bình thường `[abc]` là chọn hoặc a, hoặc b, hoặc c. |
| `^` *(nằm **trong** `[...]`)* | Dấu phủ định *(Negation)* | **CỰC KỲ QUAN TRỌNG:** Khi nằm trong `[...]`, `^` biến thành chữ **"CẤM" / "NGOẠI TRỪ"**. |
| `\s` | Ký tự khoảng trắng | Phím cách (Space), Tab, hoặc Xuống dòng (`\n`). |
| `@` | Dấu a còng | Ký tự `@` bình thường. |
| **`[^\s@]`** | Gộp lại | **"Bất kỳ ký tự nào NGOẠI TRỪ khoảng trắng và NGOẠI TRỪ dấu @"** (Tức là ký tự gì cũng được, miễn không phải dấu cách và không phải `@`). |
| `+` | Lượng từ *(Quantifier)* | Xuất hiện **ít nhất 1 lần trở lên** (1, 2, 3... ký tự). |
| **`[^\s@]+`** | Đoạn 1: Tên tài khoản | Tên tài khoản email dài từ 1 ký tự trở lên, không chứa dấu cách, không chứa `@` (ví dụ: `thinh.dc05`). |
| `@` | Dấu `@` đơn độc | Bắt buộc tiếp theo phải có đúng **1 dấu `@`**. |
| **`[^\s@]+`** | Đoạn 2: Tên nhà cung cấp | Tương tự đoạn 1, là tên miền như `gmail`, `yahoo`, `company` (không có dấu cách, không có `@`). |
| `\.` | Dấu chấm thoát ký tự | Dấu gạch chéo `\` là ký tự thoát (Escape). Trong Regex, dấu chấm `.` một mình có nghĩa là "ký tự bất kỳ nào cũng được". Vì vậy, muốn máy tính hiểu đây là **dấu chấm thật `.`**, ta phải viết `\.`. |
| **`[^\s@]+`** | Đoạn 3: Đuôi tên miền | Đuôi cấp cao như `com`, `vn`, `edu.vn` (không chứa dấu cách, không chứa `@`). |
| `$` | Neo cuối chuỗi *(Anchor)* | Bắt buộc chuỗi phải kết thúc ngay tại đây, cấm có chữ rác bám theo sau. |

##### Minh họa đường đi của chuỗi `"thinh@gmail.com"`:
```text
  ^       [^\s@]+        @      [^\s@]+       \.      [^\s@]+      $
  |          |           |         |          |          |         |
Bắt đầu -> "thinh"  ->  "@" ->  "gmail"  ->  "." ->    "com"  -> Hết chuỗi (Hợp lệ ✅)
```

---

#### 📌 MẪU 2: KIỂM TRA MẬT KHẨU MẠNH VỚI LOOKAHEAD
```text
/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d).{8,}$/
```

> **Mục tiêu:** Mật khẩu phải có:
> 1. Ít nhất 1 chữ cái thường (`a-z`)
> 2. Ít nhất 1 chữ cái hoa (`A-Z`)
> 3. Ít nhất 1 chữ số (`0-9`)
> 4. Độ dài tối thiểu 8 ký tự
> *(Các điều kiện này có thể nằm ở bất kỳ thứ tự nào: số đứng trước cũng được, chữ hoa đứng cuối cũng được).*

##### Tại sao không viết Regex thông thường mà phải dùng Lookahead?
* Bình thường, Regex duyệt ký tự theo kiểu **"nuốt chửng" (Consume)**: Đọc đến chữ nào là con trỏ tiến lên phía trước qua chữ đó.
* Nhưng mật khẩu thì người dùng nhập thứ tự lộn xộn: `"aA1bcdef"` hay `"123Abcde"` hay `"Abcdef12"` đều hợp lệ! Nếu dùng Regex thông thường bạn phải viết hàng chục tổ hợp hoán vị.
* **Giải pháp:** Sử dụng **Lookahead `(?=...)` (Nhìn về phía trước)**.

##### Hình tượng hóa: "Cổng An Ninh & 3 Chiếc Flycam Soi Hành Lý"
* Bạn đang đứng trước vạch xuất phát `^`. 
* Cú pháp `(?=...)` giống như một **chiếc Flycam cất cánh từ vị trí hiện tại bay dọc theo chuỗi để soi điều kiện**, soi xong flycam bay về báo cáo, **con trỏ vẫn đứng yên tại vạch xuất phát chứ không hề di chuyển!**

| Thành phần | Cơ chế hoạt động (Flycam soi điều kiện) |
| :--- | :--- |
| `^` | Vạch xuất phát: Con trỏ đứng ở đầu chuỗi. |
| **`(?=.*[a-z])`** | **Flycam 1:** Bay từ đầu chuỗi, lướt qua các ký tự bất kỳ (`.*`), ngó xem **có tồn tại ít nhất 1 chữ thường `[a-z]` nào không?** Có -> Báo OK. Xong bay về vạch xuất phát. |
| **`(?=.*[A-Z])`** | **Flycam 2:** Tiếp tục bay từ vạch xuất phát, lướt qua (`.*`), ngó xem **có tồn tại ít nhất 1 chữ hoa `[A-Z]` nào không?** Có -> Báo OK. Xong bay về vạch xuất phát. |
| **`(?=.*\d)`** | **Flycam 3:** Tiếp tục bay từ vạch xuất phát, lướt qua (`.*`), ngó xem **có tồn tại ít nhất 1 chữ số `\d` nào không?** Có -> Báo OK. Xong bay về vạch xuất phát. |
| **`.{8,}`** | **BƯỚC ĐI THẬT SỰ:** Sau khi cả 3 Flycam đều báo OK, lúc này con trỏ mới bắt đầu thực sự đi bộ qua từng ký tự (`.`) và đếm: **Bắt buộc phải có từ 8 ký tự trở lên `{8,}`**. |
| `$` | Vạch đích: Kết thúc chuỗi ngay sau khi đã đếm đủ điều kiện. |

##### Minh họa kiểm tra chuỗi `"Secret89"`:
1. `^` : Đứng ở đầu chữ `S`.
2. `(?=.*[a-z])` : Flycam 1 bay dọc chuỗi thấy chữ `e` (chữ thường) → Đạt ✅.
3. `(?=.*[A-Z])` : Flycam 2 bay dọc chuỗi thấy chữ `S` (chữ hoa) → Đạt ✅.
4. `(?=.*\d)` : Flycam 3 bay dọc chuỗi thấy số `8` (chữ số) → Đạt ✅.
5. `.{8,}` : Cả 3 flycam thông qua! Con trỏ đi bộ đếm `"Secret89"` có 8 ký tự (>= 8) → Đạt ✅.
6. `$` : Chạm vạch đích kết thúc chuỗi → **HỢP LỆ TOÀN DIỆN (TRUE)** 🎉.

</details>

---

#### 3.7.4. PHƯƠNG THỨC THỰC THI & BẪY TỬ HUYỆT CỦA CỜ `g`

<details open>
<summary><b>1. Bộ Phương Thức Dùng Với Regex</b> <i>(Bấm để xem)</i></summary>

* **`regex.test(str)` (Dùng 90% khi làm Form):** Kiểm tra xem chuỗi có khớp không $\rightarrow$ Trả về `true` hoặc `false`.
  ```javascript
  const isValidPhone = /^0\d{9}$/.test("0912345678"); // true
  ```
* **`regex.exec(str)`:** Trả về mảng chi tiết các nhóm bóc tách (hoặc `null`).
* **Các hàm của String:**
  * `str.match(regex)`: Trích xuất các đoạn khớp thành Mảng.
  * `str.replace(regex, newStr)`: Tìm và thay thế chuỗi bằng mẫu regex.
  * `str.split(regex)`: Tách chuỗi thành mảng dựa trên mẫu phân cách.
</details>

<br>

<details open>
<summary><b>2. Bẫy Tử Huyệt Senior: Lỗi Tráo Đổi `true/false` Của Cờ `g`</b> <i>(Bấm để xem)</i></summary>

```javascript
const regex = /abc/g; // Chú ý có cờ global "g"
const str = "abc";

console.log(regex.test(str)); // true (Lần 1)
console.log(regex.test(str)); // false! (Lần 2 cùng chuỗi lại ra FALSE!)
console.log(regex.test(str)); // true (Lần 3 lại ra TRUE!)
```
* **Bản chất:** Khi có cờ `g`, đối tượng Regex lưu vị trí khớp cuối cùng vào biến ngầm **`regex.lastIndex`**. 
  * Lần 1: Khớp chữ `"abc"`, `lastIndex` nhảy lên vị trí `3` (cuối chuỗi).
  * Lần 2: Bắt đầu tìm từ vị trí số `3` trở đi $\rightarrow$ Hết chuỗi $\rightarrow$ Trả về `false` và tự động reset `lastIndex = 0`.
  * Lần 3: Tìm lại từ đầu $\rightarrow$ Lại thấy `true`!
* **Quy tắc vàng thực chiến:** **KHI CHỈ KIỂM TRA ĐIỀU KIỆN FORM VỚI `.test()`, TUYỆT ĐỐI CẤM DÙNG CỜ `g`!**
</details>

---

#### 3.7.5. BẢNG TRA CỨU KÝ HIỆU REGEXP (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Danh Sách Tra Cứu Toàn Bộ Ký Hiệu Biểu Thức Chính Quy</b> <i>(Bấm để xem)</i></summary>

* **`[abc]`:** Tìm bất kỳ ký tự nào nằm giữa các dấu ngoặc.
* **`[^abc]`:** Tìm bất kỳ ký tự nào KHÔNG nằm giữa các dấu ngoặc.
* **`[0-9]`:** Tìm bất kỳ chữ số nào từ 0 đến 9.
* **`(x|y)`:** Tìm bất kỳ phương án nào được phân tách bởi dấu gạch đứng (hoặc x, hoặc y).
* **`\d`:** Tìm một chữ số.
* **`\D`:** Tìm một ký tự không phải chữ số.
* **`\w`:** Tìm một ký tự chữ (chữ cái, chữ số, dấu gạch dưới).
* **`\W`:** Tìm một ký tự không phải chữ.
* **`\s`:** Tìm một ký tự khoảng trắng.
* **`\S`:** Tìm một ký tự không phải khoảng trắng.
* **`\b`:** Tìm một vị trí ranh giới từ (đầu hoặc cuối một từ).
* **`.`:** Tìm bất kỳ ký tự đơn nào (ngoại trừ ký tự xuống dòng).
* **`n+`:** Khớp bất kỳ chuỗi nào chứa ít nhất một chữ n.
* **`n*`:** Khớp bất kỳ chuỗi nào chứa 0 hoặc nhiều chữ n.
* **`n?`:** Khớp bất kỳ chuỗi nào chứa 0 hoặc 1 chữ n.
* **`n{X}`:** Khớp bất kỳ chuỗi nào chứa chuỗi X ký tự n liền nhau.
* **`n{X,Y}`:** Khớp bất kỳ chuỗi nào chứa chuỗi từ X đến Y ký tự n liền nhau.
* **`n{X,}`:** Khớp bất kỳ chuỗi nào chứa chuỗi có ít nhất X ký tự n liền nhau.
* **`n$`:** Khớp bất kỳ chuỗi nào có chữ n ở cuối chuỗi.
* **`^n`:** Khớp bất kỳ chuỗi nào có chữ n ở đầu chuỗi.
* **`(?=n)`:** Lookahead khẳng định (ngó về trước phải có n).
* **`(?!n)`:** Lookahead phủ định (ngó về trước không được có n).

</details>

---

## 🏛️ GIAI ĐOẠN 4: THAO TÁC GIAO DIỆN & WEB APIS (DOM, EVENTS & BROWSER APIS)

---

### 📌 BÀI 4.1: CÂY DOM, BỘ CHỌN & CONTEXTUAL APIS (DOM TREE & SELECTORS MASTER)

---

#### 4.1.1. CÂY DOM (DOM TREE), PHẢ HỆ & BẢN CHẤT CÁC LOẠI NODE

<details open>
<summary><b>1. Bản Chất Cây DOM trong RAM & Bảng Mã `nodeType`</b> <i>(Bấm để xem)</i></summary>

* **Định nghĩa:** DOM (Document Object Model) là cấu trúc dạng cây gồm các đối tượng C++ trong bộ nhớ RAM được Browser Engine phân tích từ mã nguồn HTML thô. DOM chính là cây cầu trung gian để JavaScript có thể đọc, sửa, xóa các phần tử trên trang web.
* **Cây phả hệ phân tầng:**
  * `window` (Cửa sổ trình duyệt / Global Object)
  * `└── document` (Tài liệu trang web)
  * `    └── documentElement` (Thẻ gốc `<html>`)
  * `        ├── head` (Thẻ `<head>`)
  * `        └── body` (Thẻ `<body>`)
  * `            └── Các thẻ con, chữ và chú thích...`
* **Phân loại Node bằng thuộc tính `node.nodeType` (Chứng minh thư của Node):**
  * `node.nodeType === 1` : **`ELEMENT_NODE`** (Thẻ HTML thật: `<div>`, `<p>`, `<button>`).
  * `node.nodeType === 3` : **`TEXT_NODE`** (Văn bản chữ bên trong thẻ, bao gồm cả phím cách và ký tự xuống dòng `\n`).
  * `node.nodeType === 8` : **`COMMENT_NODE`** (Đoạn chú thích `<!-- comment -->`).
  * `node.nodeType === 9` : **`DOCUMENT_NODE`** (Toàn bộ tài liệu `document`).
* **Đường cao tốc truy cập nhanh của `document` (Không cần chạy qua Selector Engine):**
  * `document.documentElement` : Trỏ thẳng tới thẻ `<html>`.
  * `document.head` : Trỏ thẳng tới thẻ `<head>`.
  * `document.body` : Trỏ thẳng tới thẻ `<body>`.
  * `document.title` : Đọc / ghi tiêu đề tab trình duyệt (`document.title = "Trang mới"`).
  * `document.forms` : Bộ sưu tập tất cả các thẻ `<form>`.
  * `document.images` : Bộ sưu tập tất cả các thẻ `<img>`.
  * `document.links` : Bộ sưu tập tất cả các thẻ liên kết `<a>` có thuộc tính `href`.
</details>

---

#### 4.1.2. HAI TRƯỜNG PHÁI BỘ CHỌN (SELECTORS: CŨ VS HIỆN ĐẠI)

<details open>
<summary><b>2. So Sánh Cơ Chế Ngầm Các Hàm Tìm Kiếm Phần Tử</b> <i>(Bấm để xem)</i></summary>

| Tiêu chí | `getElementById` | `getElementsByClassName` / `TagName` | `querySelector` | `querySelectorAll` |
| :--- | :--- | :--- | :--- | :--- |
| **Cú pháp tham số** | Tên ID trần trụi (`"btn"`) | Tên class / tag (`"box"`, `"p"`) | Biểu thức CSS (`"#btn"`, `".box > p"`) | Biểu thức CSS (`".item"`, `"input[type=text]"`) |
| **Phạm vi gọi** | Chỉ gọi từ `document` | `document` hoặc `element` | `document` hoặc `element` | `document` hoặc `element` |
| **Số lượng trả về** | Đúng 1 Element đầu tiên (hoặc `null`) | Danh sách nhiều phần tử | Đúng 1 Element đầu tiên (hoặc `null`) | Danh sách toàn bộ phần tử khớp |
| **Kiểu dữ liệu trả về** | `HTMLElement` | **`HTMLCollection` (Mảng sống - Live)** | `HTMLElement` | **`NodeList` (Mảng tĩnh - Static Snapshot)** |
| **Cơ chế ngầm** | **Hash Map Lookup O(1)** (Cực nhanh) | Duyệt DOM Tree theo index | **CSS Selector Matcher Engine** | **CSS Selector Matcher Engine** |
| **Hỗ trợ `.forEach()`?** | Không áp dụng | ❌ **KHÔNG** (Gây `TypeError`) | Không áp dụng | ✔️ **CÓ SẴN** `.forEach()` |

* **Quy tắc thực chiến:** 
  * Dùng `querySelector` / `querySelectorAll` cho 90% nhu cầu tìm kiếm vì hỗ trợ mọi cú pháp CSS mạnh mẽ và an toàn.
  * Dùng `getElementById` khi cần tối ưu tốc độ tối đa cho 1 phần tử có ID duy nhất.
  * Tránh dùng `getElementsByClassName` / `getElementsByTagName` vì bẫy Live Collection.
</details>

---

#### 4.1.3. DUYỆT CÂY DOM (DOM NAVIGATION) & BẪY KHOẢNG TRẮNG VÔ HÌNH

<details open>
<summary><b>3. Bản Đồ Điều Hướng Phả Hệ & Nhóm Thuộc Tính An Toàn</b> <i>(Bấm để xem)</i></summary>

```text
                  parentElement (Cha trực tiếp)
                        ▲
                        │
previousElementSibling ◄─── [ PHẦN TỬ HIỆN TẠI ] ───► nextElementSibling
(Anh liền kề)           │                           (Em liền kề)
                        ▼
                  children (Tập hợp các con)
                  ├─ firstElementChild (Con đầu)
                  └─ lastElementChild  (Con út)
```

* ⚠️ **Bẫy Tử Huyệt: TextNode Khoảng Trắng Vô Hình:**
  Trình duyệt coi **phím Enter xuống dòng và phím cách giữa các thẻ HTML là một Text Node**!
* **Quy tắc vàng phân biệt 2 nhóm thuộc tính điều hướng:**
  * **Nhóm AN TOÀN (Có chữ `Element`):** `parentElement`, `children`, `firstElementChild`, `lastElementChild`, `previousElementSibling`, `nextElementSibling` $\rightarrow$ **CHỈ DUYỆT THẺ HTML**, tự động bỏ qua toàn bộ khoảng trắng, text rác và comment.
  * **Nhóm NGUY HIỂM (Không có chữ `Element`):** `parentNode`, `childNodes`, `firstChild`, `lastChild`, `previousSibling`, `nextSibling` $\rightarrow$ Duyệt tất cả mọi loại Node, rất dễ bốc nhầm vào khoảng trắng `\n` làm sập logic code.
</details>

---

#### 4.1.4. CONTEXTUAL APIS: BỘ 3 HÀM KIỂM TRA PHẢ HỆ ĐỘNG (`closest`, `matches`, `contains`)

<details open>
<summary><b>4. Giải Pháp Cho Xử Lý Sự Kiện Thực Chiến & Click Outside</b> <i>(Bấm để xem)</i></summary>

* **Bối cảnh thực tế:** Trong giao diện thực tế, khi người dùng click chuột, bạn không xuất phát từ gốc tài liệu mà bắt đầu từ **tọa độ ngẫu nhiên nơi người dùng click trúng (`event.target`)**. Bộ 3 hàm này trả lời 3 câu hỏi ngữ cảnh động:

| Hàm | Hướng di chuyển | Kết quả trả về | Ứng dụng thực chiến sống còn |
| :--- | :--- | :---: | :--- |
| **`element.closest(sel)`** | **Ngược lên trên tổ tiên** (kể cả chính nó) | `Element` hoặc `null` | **Bắt đúng nút chứa dữ liệu:** Khi user click trúng icon `<i>` hoặc chữ `<span>` lồng bên trong nút `<button>`. |
| **`element.matches(sel)`** | **Đứng yên tại chỗ** kiểm tra điều kiện | `true` / `false` | **Event Delegation (Ủy nhiệm sự kiện):** Lọc xem phần tử kích hoạt có khớp bộ chọn CSS mong muốn hay không. |
| **`parent.contains(child)`** | **Quét toàn bộ con cháu** bên dưới | `true` / `false` | **Click Outside:** Nhận diện click ra ngoài phạm vi Dropdown, Modal, Popover để tự động đóng lại. |

* **Pattern "Click Outside to Close Dropdown / Modal" (Bắt buộc phải thuộc lòng):**
  ```javascript
  const dropdown = document.querySelector(".dropdown-menu");
  const toggleBtn = document.querySelector(".dropdown-toggle");

  document.addEventListener("click", (e) => {
      // Nếu click KHÔNG NẰM TRONG dropdown VÀ KHÔNG PHẢI nút toggle
      if (!dropdown.contains(e.target) && !toggleBtn.contains(e.target)) {
          dropdown.classList.remove("show"); // Đóng menu!
      }
  });
  ```
  *(Lưu ý: Bắt buộc phải có `!toggleBtn.contains(e.target)` để tránh việc click vào chính nút mở menu lại vô tình kích hoạt sự kiện đóng menu ngay lập tức).*
</details>

---

#### 4.1.5. BẪY PHỎNG VẤN SENIOR: LIVE HTMLCOLLECTION VS STATIC NODELIST

<details open>
<summary><b>5. Mổ Xẻ Cơ Chế Ngầm & Bẫy Vòng Lặp Vô Tận Treo Trình Duyệt</b> <i>(Bấm để xem)</i></summary>

* **Cơ chế ngầm:**
  * **`HTMLCollection` (Live Collection):** Giữ con trỏ tham chiếu trực tiếp đến cây DOM thời gian thực. Khi cây DOM bị thêm hoặc xóa thẻ, độ dài `.length` của nó **tự động co giãn tức thì**.
  * **`NodeList` (Static Snapshot):** Khi gọi `querySelectorAll()`, trình duyệt chụp một bức ảnh đóng băng danh sách tại thời điểm đó. Cây DOM bên ngoài thay đổi thế nào thì `NodeList` vẫn giữ nguyên.
* **Bẫy treo trình duyệt (Vòng lặp vô tận do Live Collection):**
  ```javascript
  // ❌ NGUY HIỂM CHẾT NGƯỜI: Vừa lặp Live Collection vừa thêm phần tử
  const liveBoxes = document.getElementsByClassName("box"); // length = 2

  for (let i = 0; i < liveBoxes.length; i++) {
      const newDiv = document.createElement("div");
      newDiv.className = "box";
      document.body.appendChild(newDiv);
      // Khi chèn newDiv, liveBoxes.length TỰ ĐỘNG TĂNG lên 3, 4, 5...
      // -> Biến i đuổi theo length mãi mãi không kịp -> TREO TRÌNH DUYỆT!
  }

  // ✔️ AN TOÀN TUYỆT ĐỐI: Dùng Static NodeList
  const staticBoxes = document.querySelectorAll(".box"); // length bị đóng băng = 2

  for (let i = 0; i < staticBoxes.length; i++) {
      const newDiv = document.createElement("div");
      newDiv.className = "box";
      document.body.appendChild(newDiv);
      // staticBoxes.length vẫn giữ nguyên là 2 -> Chạy đúng 2 lần rồi dừng!
  }
  ```
* **Bẫy thiếu hàm `.forEach()`:**
  * `document.querySelectorAll(".item").forEach(el => ...)` $\rightarrow$ Chạy mượt mà.
  * `document.getElementsByClassName("item").forEach(...)` $\rightarrow$ 💥 `TypeError: ...forEach is not a function`.
  * Khắc phục nếu bắt buộc dùng HTMLCollection: `Array.from(collection).forEach(...)`.
</details>

---

#### 4.1.6. BẢNG TRA CỨU PHƯƠNG THỨC & THUỘC TÍNH DOM SELECTORS (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Danh Sách Tra Cứu Toàn Bộ API Bộ Chọn & Điều Hướng Cây DOM</b> <i>(Bấm để xem)</i></summary>

* **`document.getElementById(id)`:** Tìm phần tử theo ID duy nhất (Hash Map O(1), trả về Element hoặc null).
* **`document.getElementsByClassName(name)`:** Tìm danh sách phần tử theo tên class (Trả về Live `HTMLCollection`).
* **`document.getElementsByTagName(name)`:** Tìm danh sách phần tử theo tên thẻ (Trả về Live `HTMLCollection`).
* **`element.querySelector(css)`:** Tìm phần tử đầu tiên khớp bộ chọn CSS (Trả về Element hoặc null).
* **`element.querySelectorAll(css)`:** Tìm tất cả phần tử khớp bộ chọn CSS (Trả về Static `NodeList`).
* **`element.closest(css)`:** Tìm tổ tiên gần nhất khớp bộ chọn CSS ngược từ chính nó lên trên gốc.
* **`element.matches(css)`:** Kiểm tra phần tử hiện tại có khớp bộ chọn CSS hay không (Trả về boolean).
* **`element.contains(node)`:** Kiểm tra xem phần tử hiện tại có chứa node con cháu bên trong không (Trả về boolean).
* **`node.parentElement`:** Trả về phần tử cha trực tiếp (Bỏ qua Text Node).
* **`node.children`:** Trả về tập hợp các phần tử con HTML thật sự (`HTMLCollection`).
* **`node.firstElementChild`:** Trả về phần tử con HTML đầu tiên.
* **`node.lastElementChild`:** Trả về phần tử con HTML cuối cùng.
* **`node.nextElementSibling`:** Trả về phần tử anh em kế tiếp là thẻ HTML.
* **`node.previousElementSibling`:** Trả về phần tử anh em liền trước là thẻ HTML.
* **`node.childNodes`:** Trả về tất cả các Node con (Bao gồm cả Text Node khoảng trắng).
* **`node.nodeType`:** Trả về mã số nhận diện loại Node (1: Element, 3: Text, 8: Comment, 9: Document).
* **`node.nodeName`:** Trả về tên viết HOA của Node (ví dụ: `"DIV"`, `"#text"`).
* **`document.documentElement`:** Thuộc tính trỏ thẳng tới phần tử gốc `<html>`.
* **`document.body`:** Thuộc tính trỏ thẳng tới phần tử `<body>`.
* **`document.head`:** Thuộc tính trỏ thẳng tới phần tử `<head>`.

</details>

---

### 📌 BÀI 4.2: THAO TÁC PHẦN TỬ (DOM MANIPULATION, ATTRIBUTES, STYLES & PERFORMANCE)

---

#### 4.2.1. NỘI DUNG & VÒNG ĐỜI CỦA NODE (TẠO, CHÈN, XÓA, NHÂN BẢN)

<details open>
<summary><b>1. Bộ Hàm Chèn & Xóa Hiện Đại (DOM Level 4)</b> <i>(Bấm để xem)</i></summary>

* **Tạo phần tử mới trong RAM:**
  ```javascript
  const newDiv = document.createElement("div"); // Mới nằm trong RAM, chưa xuất hiện trên màn hình
  newDiv.textContent = "Nội dung mới";
  ```
* **Bộ tứ hàm chèn hiện đại (`before`, `after`, `prepend`, `append`):**
  * `element.before(...nodes)`: Chèn ra **bên ngoài, phía trước** `element`.
  * `element.after(...nodes)`: Chèn ra **bên ngoài, phía sau** `element`.
  * `element.prepend(...nodes)`: Chèn vào **bên trong, ở vị trí con đầu tiên**.
  * `element.append(...nodes)`: Chèn vào **bên trong, ở vị trí con cuối cùng**.
  * 👉 **Ưu thế vượt trội so với `appendChild()`:**
    * Chèn được **nhiều phần tử cùng lúc**: `box.append(p1, p2, "Chữ text tự bọc")`.
    * Chèn trực tiếp chuỗi text mà không cần gọi `document.createTextNode()`.
* **Xóa phần tử siêu tốc:**
  * **Hiện đại:** `element.remove()` (Tự hủy trực tiếp 1 dòng, không cần tìm cha).
  * **Cổ xưa:** `element.parentNode.removeChild(element)`.
* **Nhân bản phần tử (`cloneNode`):**
  * `element.cloneNode(false)`: Chỉ sao chép vỏ thẻ và thuộc tính, không sao chép nội dung con bên trong.
  * `element.cloneNode(true)`: **Sao chép sâu (Deep Clone)**, bê nguyên vẹn toàn bộ cây thẻ con bên trong.
</details>

---

#### 4.2.2. THUỘC TÍNH (ATTRIBUTES VS PROPERTIES) & VŨ KHÍ `dataset`

<details open>
<summary><b>2. Phân Biệt Attribute vs Property & Tùy Biến `data-*`</b> <i>(Bấm để xem)</i></summary>

* **Phân biệt bản chất:**
  * **HTML Attribute:** Là giá trị chuỗi tĩnh được khai báo trong file HTML ban đầu (`<input value="Thinh">`).
  * **DOM Property:** Là thuộc tính động nằm trong đối tượng JS tương ứng trong RAM (`inputElement.value`). Khi người dùng gõ vào ô input, Property thay đổi nhưng Attribute vẫn giữ nguyên giá trị ban đầu.
* **Bộ 4 hàm thao tác Attribute chuẩn:**
  * `element.getAttribute("name")`: Lấy giá trị thuộc tính.
  * `element.setAttribute("name", "value")`: Thiết lập thuộc tính.
  * `element.removeAttribute("name")`: Xóa bỏ thuộc tính.
  * `element.hasAttribute("name")`: Kiểm tra phần tử có thuộc tính đó không (trả về boolean).
* **Vũ khí truyền dữ liệu `dataset` (`data-*` attributes):**
  * HTML: `<div class="card" data-user-id="101" data-is-admin="true"></div>`
  * JS tự động chuyển đổi từ **kebab-case** (`data-user-id`) sang **camelCase** (`userId`):
    ```javascript
    const card = document.querySelector(".card");
    console.log(card.dataset.userId);  // "101" (luôn là chuỗi String)
    console.log(card.dataset.isAdmin); // "true"
    card.dataset.userId = "202";       // Ghi ngược lại HTML: data-user-id="202"
    ```
</details>

---

#### 4.2.3. CAN THIỆP GIAO DIỆN: `classList` VS `element.style`

<details open>
<summary><b>3. Quản Lý Giao Diện Chuẩn Senior & `getComputedStyle`</b> <i>(Bấm để xem)</i></summary>

* **1. Can thiệp Class qua `element.classList` (Khuyên dùng 95%):**
  * Tách bạch hoàn toàn giữa Logic (JS) và Giao diện (CSS).
  * `classList.add("dark", "animated")`: Thêm 1 hoặc nhiều class.
  * `classList.remove("hidden")`: Xóa class.
  * `classList.toggle("active")`: Công tắc bật/tắt class (có thì xóa, chưa có thì thêm).
  * `classList.contains("active")`: Kiểm tra xem phần tử có chứa class không (trả về `true`/`false`).
  * `classList.replace("old-class", "new-class")`: Đổi tên class cũ thành class mới.
* **2. Can thiệp trực tiếp qua `element.style` (Inline Style):**
  * Chỉ can thiệp vào thuộc tính `style="..."` trực tiếp trên thẻ (độ ưu tiên cực cao).
  * Tên thuộc tính viết hoa theo **camelCase**: `style.backgroundColor = "blue"`, `style.fontSize = "16px"`.
* **3. Đọc Style thực tế sau gộp CSS (`window.getComputedStyle`):**
  * Nếu một thuộc tính được định nghĩa trong file `.css` ngoài, `element.style.color` sẽ trả về chuỗi rỗng `""`!
  * Để đọc kích thước và màu sắc thực tế mà trình duyệt đang hiển thị, bắt buộc phải dùng:
    ```javascript
    const styles = window.getComputedStyle(element);
    console.log(styles.width); // "250.5px" (Tính cả padding, border)
    console.log(styles.backgroundColor); // "rgb(255, 0, 0)"
    ```
</details>

---

#### 4.2.4. BẪY HIỆU NĂNG SENIOR: REFLOW, REPAINT & `DocumentFragment`

<details open>
<summary><b>4. Mổ Xẻ Cơ Chế Tụt FPS & Kỹ Thuật Tối Ưu Render 1.000 Thẻ</b> <i>(Bấm để xem)</i></summary>

* **1. Phân biệt Reflow (Layout) vs Repaint (Paint):**
  * **Repaint (Sơn lại - Nhẹ):** Chỉ thay đổi màu sắc thị giác (`color`, `background-color`, `box-shadow`). Không làm thay đổi kích thước hay xê dịch phần tử xung quanh.
  * **Reflow (Tính toán lại khung hình - CỰC KỲ NẶNG):** Thay đổi hình học (`width`, `height`, `margin`, `padding`, `display`, `append()`, `remove()`). Trình duyệt phải tính toán lại tọa độ của phần tử đó và toàn bộ các phần tử chịu ảnh hưởng trên trang!
* **2. Bẫy "DOM Thrashing" (Đọc - Ghi đan xen liên tục):**
  ```javascript
  // ❌ NGUY HIỂM: Ép trình duyệt Flush Queue Reflow liên tục
  for (let el of elements) {
      el.style.width = "200px";        // [GHI]
      const h = el.offsetHeight;       // [ĐỌC: Ép Reflow ngay lập tức để lấy số đo!]
      el.style.height = (h * 2) + "px"; // [GHI]
  }
  // ✔️ GIẢI PHÁP: Tách bạch Đọc trước toàn bộ, rồi mới Ghi một lượt (Batching)!
  ```
* **3. Kỹ thuật chèn 1.000 phần tử chỉ tốn ĐÚNG 1 LẦN REFLOW (`DocumentFragment`):**
  ```javascript
  // Tạo cây DOM ảo siêu nhẹ trong RAM (không tồn tại trên màn hình)
  const fragment = document.createDocumentFragment();

  for (let i = 0; i < 1000; i++) {
      const li = document.createElement("li");
      li.textContent = `Sản phẩm ${i}`;
      fragment.appendChild(li); // Chỉ chèn vào RAM -> 0 lần Reflow!
  }

  // Xả 1 lượt duy nhất vào cây DOM thật:
  document.querySelector("#product-list").appendChild(fragment); // CHỈ 1 LẦN REFLOW DUY NHẤT! ⚡
  ```
</details>

---

#### 4.2.5. BẢNG TRA CỨU PHƯƠNG THỨC THAO TÁC PHẦN TỬ (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Danh Sách Tra Cứu Toàn Bộ API Thao Tác Node, Thuộc Tính & Style</b> <i>(Bấm để xem)</i></summary>

* **`document.createElement(tagName)`:** Tạo một Element mới trong bộ nhớ RAM.
* **`document.createDocumentFragment()`:** Tạo vùng nhớ đệm ảo chứa các node trước khi xả vào DOM.
* **`element.append(...nodesOrStrings)`:** Chèn thêm một hoặc nhiều node/chuỗi vào cuối phần tử.
* **`element.prepend(...nodesOrStrings)`:** Chèn thêm một hoặc nhiều node/chuỗi vào đầu phần tử.
* **`element.before(...nodesOrStrings)`:** Chèn node/chuỗi vào ngay trước phần tử hiện tại.
* **`element.after(...nodesOrStrings)`:** Chèn node/chuỗi vào ngay sau phần tử hiện tại.
* **`element.appendChild(node)`:** Chèn 1 node vào cuối phần tử (cổ điển, chỉ nhận 1 Node).
* **`element.remove()`:** Tự xóa phần tử hiện tại khỏi cây DOM.
* **`element.removeChild(childNode)`:** Xóa một node con khỏi phần tử cha (cổ điển).
* **`element.replaceChild(newNode, oldNode)`:** Thay thế node con cũ bằng node mới.
* **`element.cloneNode(deep)`:** Nhân bản node (deep = true sao chép cả cây con).
* **`element.getAttribute(name)`:** Lấy giá trị chuỗi của thuộc tính HTML.
* **`element.setAttribute(name, value)`:** Thiết lập hoặc cập nhật giá trị thuộc tính HTML.
* **`element.removeAttribute(name)`:** Xóa bỏ thuộc tính HTML.
* **`element.hasAttribute(name)`:** Kiểm tra sự tồn tại của thuộc tính HTML (boolean).
* **`element.dataset`:** Đối tượng chứa toàn bộ các thuộc tính tùy chỉnh `data-*`.
* **`element.classList.add(...classes)`:** Thêm một hoặc nhiều class CSS vào phần tử.
* **`element.classList.remove(...classes)`:** Xóa một hoặc nhiều class CSS khỏi phần tử.
* **`element.classList.toggle(class)`:** Bật / tắt class CSS (trả về true nếu vừa thêm, false nếu vừa xóa).
* **`element.classList.contains(class)`:** Kiểm tra phần tử có chứa class hay không (boolean).
* **`element.classList.replace(old, new)`:** Thay thế class cũ bằng class mới.
* **`window.getComputedStyle(element)`:** Đọc toàn bộ thuộc tính CSS thực tế sau khi đã tính toán kế thừa.

</details>

---

### 📌 BÀI 4.3: XỬ LÝ SỰ KIỆN (HTML EVENTS, BUBBLING & EVENT DELEGATION)

---

#### 4.3.1. MÔ HÌNH LẮNG NGHE SỰ KIỆN & ĐỐI TƯỢNG EVENT OBJECT `e`

<details open>
<summary><b>1. Cú Pháp `addEventListener` & Phân Biệt `e.target` vs `e.currentTarget`</b> <i>(Bấm để xem)</i></summary>

* **Cú pháp chuẩn:**
  ```javascript
  element.addEventListener(eventType, handlerFunction, options);
  ```
* **Các thuộc tính cốt lõi của Event Object `e`:**
  * **`e.type`:** Tên sự kiện (`"click"`, `"submit"`, `"keydown"`...).
  * **`e.target` (Nơi phát nổ sự kiện):** Là phần tử **sâu nhất trong DOM** mà người dùng thực sự click trúng (ví dụ: thẻ con `<i>` hoặc `<span>` bên trong nút).
  * **`e.currentTarget` (Nơi gắn biển lắng nghe):** Là phần tử mà bạn gọi lệnh `.addEventListener()` (chính là thẻ `<button>` cha). Trong hàm thông thường, `e.currentTarget` tương đương với con trỏ `this`.
  * **`e.clientX` / `e.clientY`:** Tọa độ chuột so với khung nhìn trình duyệt (Viewport).
  * **`e.key`:** Tên phím được bấm trên bàn phím (`"Enter"`, `"Escape"`, `"ArrowUp"`...).
</details>

---

#### 4.3.2. BA PHA LAN TRUYỀN SỰ KIỆN (EVENT PROPAGATION) & CAN THIỆP LUỒNG

<details open>
<summary><b>2. Capturing, Target, Bubbling & Ngắt Hành Vi Mặc Định</b> <i>(Bấm để xem)</i></summary>

* **3 Pha lan truyền theo chuẩn W3C:**
  ```text
  1. Pha Capturing (Rơi từ trên xuống):  window ──► document ──► <body> ──► <div> (cha)
                                                                             │
  2. Pha Target (Trúng đích phần tử):                                     <button> (con)
                                                                             │
  3. Pha Bubbling (Sủi bọt ngược lên): window ◄── document ◄── <body> ◄── <div> (cha)
  ```
  * Mặc định 99% trường hợp, các hàm lắng nghe sự kiện chạy ở **Pha 3 (Bubbling - Sủi bọt từ con nổi lên cha)**.
* **Bộ 3 phương thức can thiệp luồng sự kiện:**
  * **`e.preventDefault()`:** Chặn đứng **hành vi mặc định của trình duyệt** (ví dụ: ngăn form reload trang khi submit, ngăn thẻ `<a>` nhảy link).
  * **`e.stopPropagation()`:** Ngắt chuỗi sủi bọt (Bubbling), không cho sự kiện lan truyền tiếp lên các phần tử cha ông bên trên.
  * **`e.stopImmediatePropagation()`:** Ngắt sủi bọt VÀ chặn luôn tất cả các Event Listener khác cùng loại đang được gắn trên **chính phần tử đó**.
</details>

---

#### 4.3.3. KỸ THUẬT VÀNG SENIOR: EVENT DELEGATION (ỦY NHIỆM SỰ KIỆN)

<details open>
<summary><b>3. Tối Ưu RAM Cho 1.000 Thẻ & Xử Lý Phần Tử Sinh Ra Động</b> <i>(Bấm để xem)</i></summary>

* **Bản chất:** Thay vì gắn 1.000 listener riêng lẻ lên từng thẻ con (gây tràn RAM và không bắt được sự kiện cho phần tử mới thêm sau này), ta **chỉ gắn ĐÚNG 1 LISTENER lên thẻ cha duy nhất**, tận dụng cơ chế sủi bọt (Bubbling) nổi lên và dùng `closest()` / `matches()` để lọc:
* **Code thực chiến chuẩn mực (To-do List / Giỏ hàng):**
  ```javascript
  const todoList = document.querySelector("#todo-list"); // Thẻ <ul> cha

  // Chỉ gắn 1 listener duy nhất cho toàn bộ danh sách hiện tại & tương lai:
  todoList.addEventListener("click", (e) => {
      // Dùng closest để tóm đúng nút xóa, kể cả khi click trúng icon <i> bên trong
      const deleteBtn = e.target.closest(".btn-delete");
      
      if (deleteBtn) {
          const item = deleteBtn.closest(".todo-item");
          item.remove(); // Xóa công việc tương ứng
          console.log("Đã xóa ID:", deleteBtn.dataset.id);
      }
  });
  ```

---

* 🌟 **BẢNG VÀNG SENIOR: BỘ 3 VŨ KHÍ SỰ KIỆN PHỔ BIẾN NHẤT:**

| Tên vũ khí | Cơ chế hoạt động | Ứng dụng sống còn trong dự án |
| :--- | :--- | :--- |
| **`e.target.closest("selector")`** | **Dò ngược lên tổ tiên** từ phần tử bị click để tìm thẻ cha gần nhất khớp selector. | Cứu tinh khi người dùng **click trúng icon hoặc chữ con bên trong nút bấm** (`<i>`, `<span>`), giúp tóm chuẩn xác thẻ `<button>` hoặc thẻ `<li>` cha! |
| **`e.target.matches("selector")`** | **Đứng yên tại chỗ** kiểm tra xem phần tử bị click có đúng là thẻ đó không (`true`/`false`). | Dùng để phân nhánh hành vi trong Event Delegation (ví dụ: click trúng `.todo-checkbox` thì toggle, click trúng `.btn-delete` thì xóa). |
| **`element.dataset.tenThuocTinh`** | Đọc/ghi các thuộc tính **`data-*`** được khai báo trên HTML. | Cầu nối truyền dữ liệu (ID sản phẩm, trạng thái, vai trò) từ file HTML sang file JavaScript mà không làm bẩn class CSS. |

---

* ⚠️ **BẢNG TRA CỨU: CÁI GÌ TRÌNH DUYỆT CỐ ĐỊNH — CÁI GÌ DO MÌNH TỰ ĐẶT?**

| Thành phần trong code | Bản chất | Quy tắc nhớ |
| :--- | :---: | :--- |
| **`"click"`, `"submit"`, `"keydown"`** | 🔒 **CỐ ĐỊNH** | **Do Trình duyệt quy định.** Tên sự kiện chuẩn tiếng Anh, không được tự chế. |
| **Biến `e`** (hoặc `event`) | 🟢 **TỰ ĐẶT** | Tên tham số đại diện cho sự kiện (thích đặt là `e`, `event`, `evt` đều được). |
| **`e.target`, `e.preventDefault()`** | 🔒 **CỐ ĐỊNH** | **Do Trình duyệt quy định.** Thuộc tính/hàm nằm sẵn trong Event Object. |
| **`closest()`, `matches()`** | 🔒 **CỐ ĐỊNH** | **Hàm có sẵn của JS DOM.** Bắt buộc gõ đúng chính tả. |
| **`".todo-item"`, `".btn-delete"`** | 🟡 **TỰ ĐẶT Ở HTML** | **Khớp 100% với tên class trong file HTML** (`class="btn-delete"`). |
| **`dataset.id`** | 🟡 **TỰ ĐẶT Ở HTML** | **Khớp với thuộc tính `data-id`** khai báo trên thẻ HTML. |
| **Các biến: `li`, `id`, `todo`** | 🟢 **TỰ ĐẶT** | Biến cục bộ trong code JS do bạn tự đặt tên. |

</details>

---

#### 4.3.4. BẪY PHỎNG VẤN SENIOR & QUẢN LÝ BỘ NHỚ (MEMORY LEAKS)

<details open>
<summary><b>4. Bẫy `removeEventListener` Với Hàm Ẩn Danh & Tùy Chọn Sự Kiện</b> <i>(Bấm để xem)</i></summary>

* **Bẫy Rò rỉ bộ nhớ (Memory Leak) khi gỡ bỏ sự kiện:**
  ```javascript
  // ❌ SAI LẦM: Không bao giờ gỡ được vì 2 hàm mũi tên là 2 đối tượng khác nhau trong RAM!
  btn.addEventListener("click", () => console.log("Click"));
  btn.removeEventListener("click", () => console.log("Click")); // VÔ TÁC DỤNG!

  // ✔️ ĐÚNG CHUẨN: Bắt buộc phải truyền đúng tham chiếu hàm có tên
  function onClick() { console.log("Click"); }
  btn.addEventListener("click", onClick);
  btn.removeEventListener("click", onClick); // Giải phóng RAM thành công!
  ```
* **Các tùy chọn nâng cao (`options` object):**
  * `{ once: true }` : Sự kiện chỉ kích hoạt đúng 1 lần duy nhất rồi tự động hủy (Dùng cho nút mở quà, xác nhận 1 lần).
  * `{ passive: true }` : Cam kết không gọi `e.preventDefault()`, giúp trình duyệt tối ưu cuộn trang siêu mượt (60 FPS) cho các sự kiện `scroll`, `touchstart`.
  * `{ capture: true }` : Kích hoạt sự kiện ở pha Capturing (rơi từ trên xuống) thay vì Bubbling.
</details>

---

#### 4.3.5. BẢNG TRA CỨU SỰ KIỆN PHỔ BIẾN (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Danh Sách Tra Cứu Các Nhóm Sự Kiện Web Thường Gặp Nhất</b> <i>(Bấm để xem)</i></summary>

* **1. Chuột (Mouse Events):**
  * **`click`:** Nhấn và thả nút chuột trái trên phần tử.
  * **`dblclick`:** Nhấp đúp chuột.
  * **`mouseenter` / `mouseleave`:** Con trỏ chuột đi vào / rời khỏi phần tử (không sủi bọt qua con).
  * **`mouseover` / `mouseout`:** Con trỏ chuột đi vào / rời khỏi phần tử (có sủi bọt).
  * **`contextmenu`:** Nhấp chuột phải (mở menu ngữ cảnh).
* **2. Bàn phím (Keyboard Events):**
  * **`keydown`:** Phím được nhấn xuống (phản hồi nhanh nhất).
  * **`keyup`:** Phím được nhả ra.
* **3. Form & Ô nhập liệu (Form Events):**
  * **`submit`:** Form được gửi đi (bấm Enter hoặc nút Submit).
  * **`input`:** Giá trị ô input thay đổi tức thì sau mỗi ký tự gõ vào (Dùng làm tìm kiếm Real-time).
  * **`change`:** Giá trị ô input thay đổi và người dùng mất tiêu điểm (blur) hoặc chọn dropdown mới.
  * **`focus`:** Phần tử nhận tiêu điểm con trỏ chuột.
  * **`blur`:** Phần tử mất tiêu điểm (con trỏ chuột rời đi chỗ khác).
* **4. Khung nhìn & Tài liệu (Window / Document Events):**
  * **`DOMContentLoaded`:** Cây DOM đã được phân tích cú pháp hoàn tất (chưa cần chờ ảnh/CSS tải xong - khuyên dùng để chạy JS).
  * **`load`:** Toàn bộ trang web bao gồm cả ảnh, styles, tài nguyên ngoài đã tải xong hoàn toàn.
  * **`resize`:** Cửa sổ trình duyệt bị thay đổi kích thước.
  * **`scroll`:** Người dùng cuộn trang web.

</details>

---

### 📌 BÀI 4.4: BỘ CÔNG CỤ TRÌNH DUYỆT (BOM) & KHO LƯU TRỮ (WEB STORAGE)

---

#### 4.4.1. TỔNG QUAN BOM (BROWSER OBJECT MODEL) & CÁC ĐỐI TƯỢNG CON

<details open>
<summary><b>1. Bộ Công Cụ Điều Khiển Trình Duyệt (`location`, `history`, `navigator`, `screen`)</b> <i>(Bấm để xem)</i></summary>

* **Định nghĩa:** BOM là các API do trình duyệt cung cấp thông qua đối tượng tối cao `window` để JavaScript tương tác với chính phần mềm trình duyệt và thiết bị của người dùng:
* **1. `window.location` (Điều khiển thanh địa chỉ URL):**
  * `location.href`: Đọc hoặc gán URL hiện tại (`location.href = "https://..."` $\rightarrow$ chuyển trang ngay lập tức).
  * `location.pathname`: Lấy đường dẫn sau tên miền (ví dụ: `"/san-pham/ao-thun"`).
  * `location.search`: Lấy chuỗi truy vấn Query Parameters (ví dụ: `"?id=123&page=2"`).
  * `location.hash`: Lấy phần neo sau dấu `#` (ví dụ: `"#section-contact"`).
  * `location.reload()`: Tải lại trang web (tương đương nhấn F5).
  * `location.replace(url)`: Chuyển trang nhưng **xóa trang cũ khỏi lịch sử duyệt** (người dùng bấm nút Back không quay lại được - dùng sau khi đăng nhập/thanh toán).
* **2. `window.history` (Lịch sử duyệt & Nền tảng SPA Router):**
  * `history.back()` / `history.forward()`: Lùi về trang trước hoặc tiến tới trang sau.
  * `history.go(n)`: Nhảy n trang (`history.go(-2)` lùi 2 trang).
  * `history.pushState(state, title, url)`: **Trọng tâm của Single Page Apps (React/Vue Router)** $\rightarrow$ Thay đổi URL trên thanh địa chỉ nhưng **KHÔNG hề tải lại trang web**!
* **3. `window.navigator` (Thông số thiết bị & Tiện ích phần cứng):**
  * `navigator.onLine`: Trả về `true` (có mạng) hoặc `false` (mất kết nối Internet).
  * `navigator.clipboard.writeText(str)`: Sao chép văn bản vào khay nhớ tạm của máy tính (Async Promise).
  * `navigator.language`: Ngôn ngữ của hệ điều hành người dùng (`"vi-VN"`, `"en-US"`).
  * `navigator.userAgent`: Chuỗi thông tin định danh trình duyệt và hệ điều hành.
* **4. `window.screen` vs Viewport:**
  * `screen.width` / `screen.height`: Kích thước vật lý toàn bộ màn hình máy tính/điện thoại.
  * `window.innerWidth` / `window.innerHeight`: Kích thước khung nhìn hiển thị trang web thực tế của trình duyệt (thay đổi khi người dùng co giãn cửa sổ).
</details>

---

#### 4.4.2. WEB STORAGE TOÀN DIỆN (`localStorage` VS `sessionStorage` VS `COOKIES`)

<details open>
<summary><b>2. Bản Chất Kho Lưu Trữ Trên Từng Thiết Bị Cá Nhân</b> <i>(Bấm để xem)</i></summary>

* **Định vị bản chất:** "Local" trong `localStorage` là **Local của từng người dùng riêng lẻ trên thiết bị của họ**, chứ không phải local của lập trình viên! Mỗi người dùng khi truy cập website đều có một kho lưu trữ riêng biệt trên máy của mình.

| Tiêu chí so sánh | `localStorage` | `sessionStorage` | `Cookies` |
| :--- | :--- | :--- | :--- |
| **Dung lượng tối đa** | **5MB - 10MB** (Rất lớn) | **5MB** | **4KB** (Rất bé) |
| **Vòng đời dữ liệu** | **Vĩnh viễn** (Trừ khi người dùng xóa cache hoặc code gọi lệnh xóa) | **Mất ngay khi đóng Tab** | Có thể đặt hạn hết hạn (`Expires` / `Max-Age`) |
| **Chia sẻ dữ liệu** | Dùng chung cho **mọi Tab/Cửa sổ** cùng tên miền (Same-Origin) | **Độc lập theo từng Tab** (Tab này không đọc được của Tab kia) | Dùng chung toàn bộ cùng tên miền |
| **Gửi lên Server?** | ❌ **KHÔNG** (Thuần Client-side, không tốn băng thông mạng) | ❌ **KHÔNG** (Thuần Client-side) | ✔️ **TỰ ĐỘNG** gửi kèm theo Header trong mọi HTTP Request |
| **Ứng dụng thực tế** | Lưu chế độ Dark/Light mode, Ngôn ngữ, Trạng thái Sidebar, Bản nháp Autosave | Lưu dữ liệu form nhiều bước trong 1 phiên, Trạng thái cuộn của tab hiện tại | Xác thực Session đăng nhập, Nhận diện người dùng giữa Client & Server |

* **Bộ 4 hàm thao tác Web Storage chuẩn:**
  ```javascript
  // 1. Lưu dữ liệu (Key - Value):
  localStorage.setItem("theme", "dark");

  // 2. Đọc dữ liệu ra:
  const currentTheme = localStorage.getItem("theme"); // "dark" (trả về null nếu không tồn tại)

  // 3. Xóa đúng 1 mục theo key:
  localStorage.removeItem("theme");

  // 4. Xóa sạch toàn bộ kho:
  localStorage.clear();
  ```
</details>

---

#### 4.4.3. BẪY HIỆU NĂNG & KỸ THUẬT ÉP BUỘC JSON

<details open>
<summary><b>3. Bẫy `[object Object]` & Nguy Cơ Nghẽn Main Thread</b> <i>(Bấm để xem)</i></summary>

* **Bẫy 1: Tử huyệt `[object Object]` (Chỉ lưu được kiểu chuỗi String):**
  * `localStorage` chỉ chấp nhận dữ liệu kiểu String. Nếu truyền Object trực tiếp, nó sẽ tự động gọi `.toString()` biến thành `"[object Object]"` và mất sạch dữ liệu!
  * **Quy tắc vàng:** Luôn dùng `JSON.stringify()` khi Ghi và `JSON.parse()` khi Đọc:
    ```javascript
    const settings = { theme: "dark", fontSize: 16 };

    // [GHI]: Đóng gói thành chuỗi JSON
    localStorage.setItem("app_settings", JSON.stringify(settings));

    // [ĐỌC]: Mở gói chuỗi JSON ngược lại thành Object
    const saved = JSON.parse(localStorage.getItem("app_settings"));
    console.log(saved.theme); // "dark"
    ```
* **Bẫy 2: Chặn Main Thread (Synchronous Blocking I/O):**
  * Thao tác đọc/ghi Web Storage là **đồng bộ (Synchronous)**. Nếu lưu trữ dữ liệu quá nặng (vài MB), trình duyệt sẽ bị khựng UI trong vài chục mili-giây.
  * 👉 Không dùng `localStorage` để lưu file ảnh hoặc cơ sở dữ liệu lớn (Dữ liệu lớn phải dùng **IndexedDB**).
* **Bẫy 3: Sập ứng dụng do tràn bộ nhớ hoặc chế độ ẩn danh:**
  * Khi ổ đĩa đầy hoặc người dùng duyệt web ẩn danh (Private Mode chặn Storage), `setItem()` sẽ quăng lỗi `QuotaExceededError`.
  * 👉 **Quy tắc thực chiến:** Luôn bọc trong khối `try...catch`:
    ```javascript
    try {
        localStorage.setItem("key", "value");
    } catch (e) {
        console.warn("Không thể ghi vào localStorage (bộ nhớ đầy hoặc bị chặn)!");
    }
    ```
</details>

---

#### 4.4.4. BẪY BẢO MẬT PHỎNG VẤN SENIOR: LƯU TOKEN Ở ĐÂU? (XSS VS CSRF)

<details open>
<summary><b>4. So Kèo Bảo Mật Giữa `localStorage` & `httpOnly Cookie`</b> <i>(Bấm để xem)</i></summary>

* **Câu hỏi phỏng vấn Senior:** *"Sau khi người dùng đăng nhập thành công, em lưu JWT Access Token ở đâu? Tại sao?"*
* **Mổ xẻ nguy cơ:**
  * **Nếu lưu ở `localStorage`:** 
    * ⚠️ **Hiểm họa XSS (Cross-Site Scripting):** Mọi đoạn mã JavaScript chạy trên trang (bao gồm cả thư viện bên thứ 3) đều có quyền gọi `localStorage.getItem("token")`. Nếu trang bị dính lỗi XSS, hacker sẽ gửi token về server của chúng trong 1 nốt nhạc!
  * **Nếu lưu ở `httpOnly Cookie`:**
    * 🛡️ Trình duyệt **CẤM TUYỆT ĐỐI JavaScript không được đọc Cookie này**, hacker dính XSS cũng bất lực!
    * ⚠️ **Hiểm họa CSRF (Cross-Site Request Forgery):** Vì Cookie tự động gửi kèm theo request, kẻ gian có thể lừa người dùng bấm vào link độc để mượn Cookie gửi lệnh xấu.
* **Chiến lược phòng thủ toàn diện chuẩn Senior:**
  * **Access Token (Hạn ngắn 15 phút):** Lưu trực tiếp trong **Bộ nhớ RAM của JavaScript (Biến thông thường)**. Tắt tab hoặc F5 thì mất, nhưng hacker không thể đánh cắp từ ổ cứng.
  * **Refresh Token (Hạn dài 7 ngày):** Lưu trong **`httpOnly Cookie` kèm thuộc tính `SameSite=Strict` và `Secure`** (Chống cả XSS lẫn CSRF) để tự động xin cấp lại Access Token mới khi hết hạn!
* **Kỹ thuật đồng bộ đa Tab thời gian thực (`storage` event):**
  ```javascript
  // Lắng nghe khi một Tab KHÁC thay đổi dữ liệu trong localStorage:
  window.addEventListener("storage", (e) => {
      console.log("Key bị đổi:", e.key);
      console.log("Giá trị cũ:", e.oldValue);
      console.log("Giá trị mới:", e.newValue);
      if (e.key === "theme") {
          document.body.className = e.newValue; // Tab này đổi màu theo ngay lập tức!
      }
  });
  ```
</details>

---

#### 4.4.5. BẢNG TRA CỨU PHƯƠNG THỨC & THUỘC TÍNH BOM / WEB STORAGE (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Danh Sách Tra Cứu Toàn Bộ API Điều Khiển Trình Duyệt & Bộ Nhớ</b> <i>(Bấm để xem)</i></summary>

* **`localStorage.setItem(key, val)`:** Lưu cặp key-value vào bộ nhớ vĩnh viễn của trình duyệt.
* **`localStorage.getItem(key)`:** Đọc giá trị theo key (trả về null nếu không có).
* **`localStorage.removeItem(key)`:** Xóa một cặp key-value khỏi bộ nhớ.
* **`localStorage.clear()`:** Xóa sạch toàn bộ dữ liệu trong kho lưu trữ của domain hiện tại.
* **`localStorage.length`:** Trả về tổng số lượng mục đang được lưu trữ.
* **`localStorage.key(index)`:** Lấy tên của key tại vị trí chỉ số index.
* **`sessionStorage.*`:** Bộ phương thức tương tự localStorage nhưng dữ liệu bị hủy ngay khi đóng Tab.
* **`location.href`:** Đọc hoặc gán URL điều hướng của trang web.
* **`location.pathname`:** Lấy phần đường dẫn thư mục sau tên miền.
* **`location.search`:** Lấy chuỗi tham số Query (bắt đầu bằng `?`).
* **`location.hash`:** Lấy phần mỏ neo (bắt đầu bằng `#`).
* **`location.reload()`:** Tải lại trang web hiện tại.
* **`location.replace(url)`:** Điều hướng đến trang mới và xóa trang hiện tại khỏi lịch sử.
* **`history.back()`:** Quay lại trang liền trước trong lịch sử.
* **`history.forward()`:** Tiến tới trang liền sau trong lịch sử.
* **`history.pushState(state, title, url)`:** Thêm một mục vào lịch sử duyệt web mà không reload trang.
* **`navigator.onLine`:** Kiểm tra trạng thái kết nối mạng Internet (boolean).
* **`navigator.clipboard.writeText(str)`:** Ghi chuỗi văn bản vào bộ nhớ Clipboard của hệ điều hành.
* **`navigator.language`:** Trả về mã ngôn ngữ mặc định của trình duyệt/hệ điều hành.
* **`window.innerWidth` / `innerHeight`:** Chiều rộng / chiều cao của vùng hiển thị trang web (Viewport).

</details>

---

### 📌 BÀI 4.5: ĐỊNH DẠNG DỮ LIỆU JS JSON (JAVASCRIPT OBJECT NOTATION)

---

#### 4.5.1. BẢN CHẤT JSON & 4 QUY TẮC CÚ PHÁP NGHIÊM NGẶT

<details open>
<summary><b>1. Bản Chất Chuỗi Văn Bản & So Kèo Cú Pháp Với JS Object</b> <i>(Bấm để xem)</i></summary>

* **Định nghĩa:** JSON là định dạng dữ liệu dạng chuỗi văn bản (String-based) độc lập với mọi ngôn ngữ lập trình. JSON dùng để đóng gói dữ liệu lưu vào Web Storage hoặc truyền tải giữa Frontend và Backend qua mạng.
* **4 Khác biệt cú pháp sống còn giữa JS Object và JSON:**
  1. **Khóa (Keys):** BẮT BUỘC phải bọc trong **dấu ngoặc kép đôi `"`** (Ví dụ: `{"name": "Thinh"}`. Cấm để trần `{name: ...}` hoặc dùng ngoặc đơn `{'name': ...}`).
  2. **Chuỗi (Strings):** CHỈ ĐƯỢC dùng **dấu ngoặc kép đôi `"`** (Ví dụ: `"Hà Nội"`. Cấm dùng nháy đơn `'...'` hoặc backticks).
  3. **Dấu phẩy thừa (Trailing Commas):** **CẤM TUYỆT ĐỐI!** `{ "a": 1, "b": 2, }` sẽ gây lỗi cú pháp `SyntaxError` sập ứng dụng ngay lập tức!
  4. **Giới hạn 6 kiểu dữ liệu:** Chỉ chấp nhận: `String`, `Number`, `Boolean`, `null`, `Array`, `Object`.
</details>

---

#### 4.5.2. BỘ ĐÔI PHƯƠNG THỨC: `JSON.stringify()` & `JSON.parse()`

<details open>
<summary><b>2. Đóng Thùng Hút Chân Không & Khui Hàng Lắp Ráp</b> <i>(Bấm để xem)</i></summary>

* **1. `JSON.stringify(value)` (Gói hàng - Serialization):**
  * Chuyển đổi một đối tượng JavaScript trong RAM thành một chuỗi văn bản JSON.
  ```javascript
  const user = { id: 1, name: "Thinh", active: true };
  const jsonStr = JSON.stringify(user);
  console.log(typeof jsonStr); // "string"
  console.log(jsonStr);        // '{"id":1,"name":"Thinh","active":true}'
  ```
* **2. `JSON.parse(text)` (Mở hàng - Deserialization):**
  * Phân tích một chuỗi JSON để khôi phục lại thành đối tượng JavaScript trong RAM.
  ```javascript
  const obj = JSON.parse(jsonStr);
  console.log(obj.name); // "Thinh"
  ```
* ⚠️ **Quy tắc an toàn sống còn:** Bất kỳ lần gọi `JSON.parse()` nào cũng **BẮT BUỘC PHẢI BỌC TRONG `try...catch`** vì nếu chuỗi đầu vào bị lỗi cú pháp hoặc rỗng `""`, nó sẽ quăng lỗi làm sập toàn bộ ứng dụng:
  ```javascript
  let data = null;
  try {
      data = JSON.parse(rawServerResponse);
  } catch (err) {
      console.error("Dữ liệu JSON bị lỗi cú pháp:", err.message);
  }
  ```
</details>

---

#### 4.5.3. BẪY HIỆU NĂNG & TỬ HUYỆT SENIOR (SERIALIZATION TRAPS)

<details open>
<summary><b>3. Sự Bốc Hơi Của Hàm/Undefined & Bẫy Kiểu Ngày Tháng `Date`</b> <i>(Bấm để xem)</i></summary>

* **Bẫy 1: "Sự bốc hơi kỳ bí" của `undefined`, `Function` và `Symbol`:**
  * Vì JSON là chuẩn chung cho cả Python/Java nên nó không hỗ trợ khái niệm Function, undefined hay Symbol của JS.
  * **Trong Object:** Các thuộc tính này **BỊ XÓA BỎ HOÀN TOÀN KHÔNG ĐỂ LẠI DẤU VẾT**!
    ```javascript
    const badObj = { a: "ok", b: undefined, c: () => {}, d: Symbol("x") };
    console.log(JSON.stringify(badObj)); // '{"a":"ok"}'
    ```
  * **Trong Array:** Các giá trị này bị ép thành `null`:
    ```javascript
    console.log(JSON.stringify([1, undefined, () => {}])); // '[1,null,null]'
    ```
  * `NaN` và `Infinity` cũng tự động bị ép thành `null`.
* **Bẫy 2: Tử huyệt kiểu Ngày tháng (`Date` Trap):**
  * Khi `JSON.stringify()`, đối tượng `Date` bị biến thành chuỗi String ISO 8601 (`"2026-09-14T..."`).
  * Khi `JSON.parse()` lại, nó **VẪN LÀ CHUỖI STRING**, không thể tự khôi phục thành đối tượng Date ban đầu!
  ```javascript
  const order = { time: new Date() };
  const restored = JSON.parse(JSON.stringify(order));
  console.log(typeof restored.time); // "string" (Muốn dùng hàm phải new Date(restored.time)!)
  ```
* **Bẫy 3: Tham chiếu vòng tròn (Circular Reference):**
  * Khi 2 đối tượng trỏ lồng vào nhau, gọi `JSON.stringify()` sẽ quăng lỗi ngay lập tức: `TypeError: Converting circular structure to JSON`.
</details>

---

#### 4.5.4. THAM SỐ ẨN QUYỀN NĂNG: `replacer`, `space` & HÀM `reviver`

<details open>
<summary><b>4. Whitelist Filter, Pretty-Print & Tự Động Phục Hồi `Date`</b> <i>(Bấm để xem)</i></summary>

* **1. Tham số `replacer` (Bộ lọc Whitelist):**
  * Chỉ xuất ra những trường dữ liệu mong muốn, loại bỏ các trường nhạy cảm (như mật khẩu):
  ```javascript
  const account = { id: 101, username: "thinh", passwordHash: "secret123" };
  // Chỉ lấy id và username:
  const safeJson = JSON.stringify(account, ["id", "username"]);
  console.log(safeJson); // '{"id":101,"username":"thinh"}'
  ```
* **2. Tham số `space` (Làm đẹp mã nguồn - Pretty Print):**
  * Thụt lề để in chuỗi JSON đẹp mắt ra màn hình console hoặc debug:
  ```javascript
  console.log(JSON.stringify(account, null, 2)); // Thụt lề 2 dấu cách
  ```
* **3. Hàm `reviver` trong `JSON.parse(str, reviver)` (Tự động hóa phục hồi `Date`):**
  ```javascript
  const jsonStr = '{"createdAt":"2026-09-14T08:00:00.000Z","amount":500}';

  const data = JSON.parse(jsonStr, (key, value) => {
      // Nhận diện chuỗi ngày tháng ISO và tự động biến thành đối tượng Date thật:
      if (typeof value === "string" && /^\d{4}-\d{2}-\d{2}T/.test(value)) {
          return new Date(value);
      }
      return value;
  });

  console.log(data.createdAt instanceof Date); // true ✅ (Đã khôi phục thành Date thật!)
  ```
</details>

---

#### 4.5.5. BẢNG TRA CỨU JSON (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Danh Sách Tra Cứu Toàn Bộ Phương Thức & Cú Pháp Chuẩn JSON</b> <i>(Bấm để xem)</i></summary>

* **`JSON.stringify(value)`:** Chuyển đổi giá trị JavaScript thành chuỗi JSON.
* **`JSON.stringify(value, replacer)`:** Lọc các thuộc tính theo danh sách mảng (Whitelist) hoặc hàm biến đổi.
* **`JSON.stringify(value, replacer, space)`:** Tạo chuỗi JSON được định dạng thụt lề đẹp mắt (Pretty-print).
* **`JSON.parse(text)`:** Chuyển đổi chuỗi văn bản JSON thành đối tượng JavaScript.
* **`JSON.parse(text, reviver)`:** Chuyển đổi chuỗi JSON kèm hàm biến đổi giá trị trước khi trả về (Dùng khôi phục Date).
* **`obj.toJSON()`:** Phương thức đặc biệt được khai báo bên trong Object, tự động được `JSON.stringify` gọi để tùy biến dữ liệu xuất ra.

</details>

---

### 📌 BÀI 4.6: DỰ ÁN MINI THỰC CHIẾN (KIẾN TRÚC DATA-DRIVEN TRONG JS THUẦN)

---

#### 4.6.1. TỔNG KẾT NGUYÊN LÝ DỮ LIỆU DẪN DẮT (DATA-DRIVEN ARCHITECTURE)

<details open>
<summary><b>1. Bản Chất 5 Mắt Xích Vàng Trong Ứng Dụng Frontend</b> <i>(Bấm để xem)</i></summary>

* **Định nghĩa:** Kiến trúc Data-Driven tách bạch $100\%$ giữa **Dữ liệu nguồn (State trong RAM & LocalStorage)** và **Giao diện hiển thị (DOM Tree trên màn hình)**. Thay vì đi tìm từng thẻ HTML để chắp vá sửa đổi thủ công, mọi thao tác người dùng đều chỉ thay đổi mảng dữ liệu, sau đó gọi hàm Render để giao diện tự động đồng bộ.
* **Quy trình 5 mắt xích vàng:**
  ```text
  1. Người dùng tương tác (Click nút / Gửi Form)
                 │
                 ▼
  2. Bắt sự kiện (Event Delegation) & Chặn reload (e.preventDefault)
                 │
                 ▼
  3. Thao tác trên MẢNG DỮ LIỆU (push, unshift, filter, find, sort)
                 │
                 ▼
  4. Đồng bộ vào ổ cứng (localStorage.setItem với JSON.stringify)
                 │
                 ▼
  5. Vẽ lại giao diện từ Mảng (render function với innerHTML hoặc cloneNode)
  ```
* **Đấu trường luyện tập:** Đã thiết lập 2 dự án thực chiến hoàn chỉnh kèm kịch bản, khung HTML/CSS và link tra cứu tra nhanh tại [exjs.md](file:///d:/Downloads/Prac_FE/exjs.md):
  * **Dự án 1:** Giỏ Hàng Mua Sắm Mini (Mini Shopping Cart).
  * **Dự án 2:** Bảng Ghi Chú Sticky Notes Đổi Màu & Ghim Lên Đầu (Sticky Notes Pro).

</details>

---

# 📍 GIAI ĐOẠN 5: BẤT ĐỒNG BỘ & GIAO TIẾP MẠNG (ASYNC JS & NETWORKING)

---

## 📌 BÀI 5.1: QUẢN LÝ THỜI GIAN & EVENT LOOP (JS TIMERS & BROWSER CONCURRENCY)

---

### 5.1.1. NGHỊCH LÝ ĐƠN LUỒNG & BỘ TỨ TRỤ RUNTIME

<details open>
<summary><b>1. Bản Chất Single-Threaded & Cấu Trúc Browser Runtime</b> <i>(Bấm để xem)</i></summary>

* **Bản chất JavaScript Engine (V8, SpiderMonkey):**
  * Là **Single-Threaded (Đơn luồng)**: Chỉ có duy nhất 1 **Call Stack** (Ngăn xếp thực thi).
  * Tại một thời điểm, CPU chỉ có thể chạy đúng 1 dòng lệnh JavaScript.
* **Nghịch lý "Không bao giờ bị đóng băng":**
  * JS đơn luồng nhưng Web vẫn chạy các tác vụ tải dữ liệu nặng, hẹn giờ, hoạt ảnh mà không làm đơ giao diện.
  * Lý do: JS Engine không hoạt động độc lập mà nằm bên trong **Môi trường Trình duyệt (Browser Runtime)** đa luồng viết bằng C++.
* **Mô hình Bộ Tứ Trụ:**
  ```text
  ┌─────────────────────────────────────────────────────────────┐
  │                    BROWSER RUNTIME                          │
  │                                                             │
  │   ┌─────────────────────┐       ┌──────────────────────┐   │
  │   │   JS ENGINE (V8)    │       │       WEB APIs       │   │
  │   │  ┌───────────────┐  │       │  (C++ Đa luồng ngầm)  │   │
  │   │  │  CALL STACK   │  │ ────► │  - setTimeout/Timer  │   │
  │   │  │ (Đơn luồng)   │  │       │  - fetch / AJAX      │   │
  │   │  └───────────────┘  │       │  - DOM Events        │   │
  │   └──────────▲──────────┘       └──────────┬───────────┘   │
  │              │                             │               │
  │              │      ┌──────────────────┐   │               │
  │              │      │   EVENT LOOP     │   │               │
  │              └──────┤ (Người gác cổng) ├◄──┘               │
  │                     └────────┬─────────┘                   │
  │                              │                             │
  │       ┌──────────────────────┴──────────────────────┐      │
  │       │               QUEUES (HÀNG ĐỢI)             │      │
  │       │  - Microtask Queue: Promise.then, async...  │      │
  │       │  - Macrotask Queue: setTimeout, setInterval │      │
  │       └─────────────────────────────────────────────┘      │
  └─────────────────────────────────────────────────────────────┘
  ```
* **Chức năng của từng trụ:**
  1. **Call Stack:** Nơi thực thi mã đồng bộ (Synchronous). Vào sau ra trước (LIFO).
  2. **Web APIs:** Hệ thống tiểu trình C++ ngầm của trình duyệt. Đảm nhận việc đếm giờ (`Timer`), gửi request HTTP (`fetch`), lắng nghe chuột/phím (`DOM Events`).
  3. **Callback Queues (Hàng đợi):** Nơi chứa các hàm callback đã hoàn tất đếm giờ hoặc đã nhận dữ liệu, đứng chờ để được vào Call Stack.
  4. **Event Loop (Vòng lặp sự kiện):** Tiến trình liên tục giám sát: Nếu Call Stack **hoàn toàn trống rỗng**, nó sẽ bốc tác vụ từ Hàng đợi đưa lên Call Stack thực thi.

</details>

---

### 5.1.2. HAI LÀN XE ƯU TIÊN: MICROTASKS VS MACROTASKS

<details open>
<summary><b>2. Quy Tắc Ưu Tiên Tuyệt Đối Của Event Loop</b> <i>(Bấm để xem)</i></summary>

* Trình duyệt phân chia các tác vụ bất đồng bộ thành 2 làn xe:

| Đặc điểm | Microtask Queue (Làn VIP 🏎️) | Macrotask Queue / Task Queue (Làn Thường 🚗) |
| :--- | :--- | :--- |
| **Nguồn gốc** | `Promise.then / catch / finally`, `queueMicrotask()`, `MutationObserver` | `setTimeout`, `setInterval`, `setImmediate` (Node), I/O mạng, DOM Event callbacks |
| **Độ ưu tiên** | **Tuyệt đối cao nhất**. Được dọn sạch sành sanh trước khi chuyển việc khác. | Ưu tiên sau. Mỗi vòng lặp Event Loop chỉ xử lý **1 Macrotask** rồi lại kiểm tra Microtask. |
| **Ảnh hưởng UI** | Nếu Microtask bị lặp vô tận (đệ quy), trình duyệt sẽ bị **đóng băng hoàn toàn (Freeze)** vì Event Loop không thể sang bước Render UI! | Macrotask nhường quyền cho Trình duyệt thực hiện vẽ lại màn hình (UI Rendering) giữa các lượt. |

* **Thuật toán vòng lặp của Event Loop:**
  1. Thực thi toàn bộ mã đồng bộ trong **Call Stack** cho đến khi cạn kiệt.
  2. Kiểm tra **Microtask Queue**: Thực thi lần lượt cho đến khi hàng đợi này **trống hoàn toàn 100%** (nếu trong lúc chạy Microtask lại sinh thêm Microtask con, nó chạy tiếp luôn không dừng!).
  3. Kiểm tra xem Trình duyệt có cần **Render lại giao diện (UI Paint/Reflow)** không → Thực hiện Render.
  4. Lấy đúng **1 Macrotask đầu tiên** trong Macrotask Queue đẩy lên Call Stack để chạy.
  5. Quay lại Bước 2 (Vòng lặp vĩnh cửu).

</details>

---

### 5.1.3. BỘ HÀM HẸN GIỜ (JS TIMERS API): `setTimeout` & `setInterval`

<details open>
<summary><b>3. Cú Pháp, Tham Số Ẩn & Kỹ Thuật Hủy Hẹn Giờ</b> <i>(Bấm để xem)</i></summary>

* **1. `setTimeout(callback, delay, param1, param2, ...)`:**
  * Kích hoạt hàm `callback` một lần duy nhất sau khoảng thời gian `delay` (tính bằng mili-giây, `1s = 1000ms`).
  * Trả về một mã định danh số nguyên `timerId`.
  * **Tham số phụ truyền thẳng:** Có thể truyền tham số vào callback mà không cần bọc hàm mũi tên:
    ```javascript
    function greet(greeting, name) {
        console.log(`${greeting}, ${name}!`);
    }
    // Truyền "Xin chào" và "Thịnh" làm đối số:
    const timerId = setTimeout(greet, 2000, "Xin chào", "Thịnh");
    ```
* **2. `clearTimeout(timerId)`:**
  * Hủy bỏ lệnh hẹn giờ trước khi nó kịp kích hoạt.
  * Nếu `timerId` không hợp lệ hoặc đã chạy xong, hàm im lặng bỏ qua không quăng lỗi.
* **3. `setInterval(callback, interval, ...)`:**
  * Lặp đi lặp lại việc gọi `callback` sau mỗi chu kỳ `interval` mili-giây.
  * Trả về một `intervalId`.
* **4. `clearInterval(intervalId)`:**
  * Dừng chu kỳ lặp lại vĩnh viễn. Bắt buộc phải gọi khi chuyển trang hoặc hủy component để tránh **Rò rỉ bộ nhớ (Memory Leak)**!
    ```javascript
    let count = 1;
    const intervalId = setInterval(() => {
        console.log(`Nhịp tim thứ: ${count}`);
        if (count >= 5) {
            clearInterval(intervalId); // Tự hủy sau 5 nhịp
            console.log("Đã dừng nhịp tim!");
        }
        count++;
    }, 1000);
    ```

</details>

---

### 5.1.4. BẪY PHỎNG VẤN SENIOR VÀ CÁC TỬ HUYỆT VỀ THỜI GIAN

<details open>
<summary><b>4. Trôi Thời Gian (Timer Drift), setTimeout 0ms & Bẫy Closures `var`</b> <i>(Bấm để xem)</i></summary>

* **Bẫy 1: Tại sao `setTimeout(..., 0)` không chạy ngay lập tức?**
  * `delay = 0` không có nghĩa là thực thi ở mili-giây thứ 0. Nó có nghĩa là: *"Hãy nạp callback này vào hàng đợi Macrotask càng sớm càng tốt"*.
  * Theo chuẩn HTML5, các lệnh timer lồng nhau sâu hơn 5 tầng sẽ bị áp mức giới hạn sàn tối thiểu là **4ms** (Clamping threshold).
  * Callback bắt buộc phải đợi Call Stack chạy hết code đồng bộ và giải phóng sạch Microtask Queue.

* **Bẫy 2: Hiểm họa trôi thời gian (Timer Drift) của `setInterval`:**
  * Nếu hàm callback bên trong `setInterval` mất 1500ms để tính toán dữ liệu, trong khi `interval` đặt là 1000ms:
  * Trình duyệt sẽ dồn ứ các callback lại với nhau (Queue Stacking), dẫn đến các lần chạy sau bị dính chùm hoặc giật lag nghiêm trọng!
  * **Giải pháp Senior (Recursive setTimeout):** Dùng `setTimeout` đệ quy để đảm bảo khoảng cách giữa các lần chạy luôn chính xác tuyệt đối sau khi việc cũ đã làm xong:
    ```javascript
    function smartInterval() {
        // Làm tác vụ nặng ở đây...
        console.log("Xong 1 chu kỳ công việc");

        // Chỉ hẹn giờ chu kỳ tiếp theo SAU KHI việc hiện tại đã hoàn tất:
        setTimeout(smartInterval, 1000);
    }
    setTimeout(smartInterval, 1000);
    ```

* **Bẫy 3: Bẫy kinh điển vòng lặp `var` vs `let` kết hợp `setTimeout`:**
  ```javascript
  for (var i = 0; i < 3; i++) {
      setTimeout(() => console.log(i), 100);
  }
  // Kết quả: 3, 3, 3 ❌ (Vì var có Function/Global Scope, biến i bị ghi đè thành 3 trước khi timer nổ!)

  for (let j = 0; j < 3; j++) {
      setTimeout(() => console.log(j), 100);
  }
  // Kết quả: 0, 1, 2 ✅ (Vì let có Block Scope, mỗi vòng lặp tạo một Lexical Environment riêng biệt!)
  ```

</details>

---

### 5.1.5. BẢNG TRA CỨU TIMERS & EVENT LOOP (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Toàn Bộ Phương Thức & Hằng Số Chuẩn Trình Duyệt</b> <i>(Bấm để xem)</i></summary>

* **`window.setTimeout(fn, delay, ...args)`:** Thiết lập bộ đếm thời gian 1 lần (Macrotask).
* **`window.clearTimeout(id)`:** Hủy bỏ bộ đếm thời gian `setTimeout`.
* **`window.setInterval(fn, interval, ...args)`:** Thiết lập bộ đếm lặp tuần hoàn (Macrotask).
* **`window.clearInterval(id)`:** Dừng bộ đếm lặp tuần hoàn `setInterval`.
* **`window.queueMicrotask(fn)`:** Đưa trực tiếp một hàm vào Microtask Queue (Làn xe ưu tiên VIP).
* **`window.requestAnimationFrame(fn)`:** Hàm chuyên dụng đồng bộ với tần số quét màn hình (60Hz / 120Hz) cho hoạt ảnh mượt mà, chạy ngay trước bước Repaint.

</details>

---

## 📌 BÀI 5.2: TIẾN TRÌNH BẤT ĐỒNG BỘ: TỪ CALLBACK HELL ĐẾN ĐỐI TƯỢNG PROMISE

---

### 5.2.1. NỖI ĐAU LỊCH SỬ & SỰ KHỦNG HOẢNG CỦA CALLBACK TRUYỀN THỐNG

<details open>
<summary><b>1. Bản Chất 3 Tử Huyệt Của Callback Hell & Inversion of Control</b> <i>(Bấm để xem)</i></summary>

* **Bản chất của Callback:** Là một hàm được truyền như một đối số vào một hàm khác để được gọi lại (call back) khi một tác vụ hoàn tất.
* **3 Tử huyệt trí mạng của kiến trúc Callback cũ:**
  1. **Kim tự tháp chết chóc (Pyramid of Doom / Callback Hell):**
     * Khi các tác vụ bất đồng bộ phụ thuộc kết quả nối tiếp nhau (Tuần tự: Việc A xong mới làm việc B, B xong làm C...).
     * Mã nguồn bị thụt lề xiên dần sang bên phải màn hình (horizontal growth), tạo thành cấu trúc tam giác nhọn cực kỳ khó đọc, khó refactor và khó theo dõi luồng dữ liệu.
  2. **Mất quyền kiểm soát mã nguồn (Inversion of Control - IoC):**
     * Đây là vấn đề nghiêm trọng nhất về mặt độ tin cậy phần mềm (Software Trust).
     * Khi bạn đưa một callback vào một thư viện bên thứ 3 (ví dụ: `paymentGateway.charge(cart, callback)`):
       * Bạn đã trao quyền thực thi mã nguồn của mình cho người khác quản lý.
       * Bạn không thể đảm bảo: Thư viện đó sẽ gọi callback của bạn **0 lần** (bị treo), **1 lần** (đúng), hay vô tình dính lỗi lặp và gọi **5 lần liên tiếp** (khiến tài khoản người dùng bị trừ tiền 5 lần!).
  3. **Sự bất lực của khối `try...catch` truyền thống:**
     * `try...catch` chỉ có thể bắt được lỗi xảy ra **đồng bộ trong cùng một Call Stack Frame**.
     * Khi hàm callback bất đồng bộ được thực thi bởi Event Loop, thì khối `try...catch` ở Call Stack ban đầu **đã kết thúc và bốc hơi khỏi bộ nhớ từ lâu**.
     * Lỗi bị quăng ra không gian toàn cục (Uncaught Exception) làm sập ứng dụng nếu không tự viết cơ chế bắt lỗi thủ công ở từng tầng callback.

</details>

---

### 5.2.2. KIẾN TRÚC & TIÊU CHUẨN PROMISES/A+

<details open>
<summary><b>2. Định Nghĩa, 3 Trạng Thái Bất Biến & Vòng Đời Promise</b> <i>(Bấm để xem)</i></summary>

* **Định nghĩa chuẩn Promises/A+:** `Promise` là một đối tượng đại diện cho **kết quả cuối cùng** của một thao tác bất đồng bộ — một giá trị có thể chưa biết tại thời điểm khởi tạo nhưng sẽ được phân định (settled) tại một thời điểm trong tương lai.
* **Bộ 3 Trạng thái bất biến (Promise States):**
  ```text
                 ┌────────────────────────────────────────────────────────┐
                 │                  1. PENDING (Đang chờ)                 │
                 │   (Trạng thái ban đầu, chưa thành công cũng chưa lỗi)  │
                 └──────────────┬──────────────────────────┬──────────────┘
                                │                          │
                     resolve(value)                      reject(reason)
                                │                          │
                                ▼                          ▼
  ┌──────────────────────────────────────────┐   ┌──────────────────────────────────────────┐
  │         2. FULFILLED (Thành công)        │   │          3. REJECTED (Thất bại)          │
  │    Mang theo giá trị kết quả (value)     │   │     Mang theo nguyên nhân lỗi (reason)   │
  │     Kích hoạt các hàm trong .then()      │   │   Kích hoạt các hàm trong .catch()       │
  └──────────────────────────────────────────┘   └──────────────────────────────────────────┘
  ```
* **Quy tắc Bất Biến Tuyệt Đối (Immutability Law):**
  * Một Promise bắt đầu từ `Pending` và chỉ có thể chuyển trạng thái **DUY NHẤT 1 LẦN** sang `Fulfilled` HOẶC `Rejected`.
  * Một khi đã chuyển trạng thái (đã được Settled), trạng thái và giá trị mang theo bị **ĐÓNG BĂNG VĨNH VIỄN**.
  * Mọi lời gọi `resolve()` hay `reject()` thứ hai, thứ ba sau đó đều bị JS Engine bỏ qua không thương tiếc.
  * 👉 **Giải quyết dứt điểm vấn đề Inversion of Control:** Đảm bảo mã nguồn của bạn chỉ chạy đúng 1 lần duy nhất!

</details>

---

### 5.2.3. CÚ PHÁP TẠO & TIÊU THỤ: PRODUCER VS CONSUMER

<details open>
<summary><b>3. Executor Function Chạy Đồng Bộ & Bộ 3 Phương Thức Tiêu Thụ</b> <i>(Bấm để xem)</i></summary>

* **1. Nhà sản xuất (Producer) - `new Promise(executor)`:**
  * Hàm `executor` nhận 2 hàm đối số: `resolve` và `reject`.
  * **BẪY BẢN CHẤT:** Hàm `executor` được thực thi **NGAY LẬP TỨC VÀ ĐỒNG BỘ** tại thời điểm khởi tạo Promise, không hề có độ trễ!
  ```javascript
  console.log("1. Bắt đầu");
  const myPromise = new Promise((resolve, reject) => {
      console.log("2. Executor đang chạy đồng bộ!"); // Chạy ngay lập tức!
      setTimeout(() => {
          resolve("Dữ liệu bảo mật");
      }, 1000);
  });
  console.log("3. Kết thúc khởi tạo");
  // Thứ tự in: 1 -> 2 -> 3
  ```
* **2. Người tiêu thụ (Consumer) - `.then()`, `.catch()`, `.finally()`:**
  * **`.then(onFulfilled, onRejected)`:**
    * Nhận vào tối đa 2 callback. Callback 1 chạy khi thành công (`resolve`), Callback 2 chạy khi thất bại (`reject`).
    * Thực tế chuẩn mực: Chỉ dùng `.then(onFulfilled)` và dùng `.catch()` riêng để bắt lỗi.
  * **`.catch(onRejected)`:**
    * Tương đương với `.then(null, onRejected)`.
    * Lắng nghe bất kỳ lỗi nào xuất hiện trong toàn bộ chuỗi Promise phía trước.
  * **`.finally(onFinally)`:**
    * Luôn luôn được thực thi khi Promise đã Settled (dù thành công hay thất bại).
    * **Đặc tính kỹ thuật:** `finally` không nhận bất kỳ đối số nào (không biết kết quả là gì) và trả nguyên vẹn giá trị/lỗi của Promise phía trước xuống tiếp (Pass-through), cực kỳ lý tưởng để đóng kết nối, tắt spinner loading.

</details>

---

### 5.2.4. NGHỆ THUẬT PROMISE CHAINING & CÁC QUY TẮC RETURN

<details open>
<summary><b>4. Chuỗi Hàng Dọc Phẳng & 4 Kịch Bản Return Trong `.then`</b> <i>(Bấm để xem)</i></summary>

* **Cơ chế Chaining:** Phương thức `.then()` **LUÔN LUÔN TRẢ VỀ MỘT PROMISE MỚI**. Nhờ vậy, ta có thể nối đuôi các `.then()` liên tiếp thành một đường thẳng hàng dọc thanh lịch.
* **4 Kịch bản giá trị trả về trong `.then()`:**
  1. **Return một giá trị nguyên thủy / Object thông thường:**
     * Trình duyệt tự động bọc giá trị đó vào `Promise.resolve(value)`.
     * Dữ liệu được chuyển thẳng làm tham số cho `.then()` tiếp theo ngay lập tức.
  2. **Return một Promise khác (Quan trọng nhất):**
     * `.then()` tiếp theo sẽ **tạm dừng xếp hàng**, kiên nhẫn đợi cho đến khi Promise vừa được return hoàn tất (Settled) rồi mới chạy tiếp.
     * Đây chính là cơ chế giúp làm phẳng hoàn toàn các chuỗi tác vụ bất đồng bộ phụ thuộc nhau!
  3. **Quăng lỗi bằng `throw new Error(...)`:**
     * Trình duyệt tự động bọc lỗi vào `Promise.reject(error)`.
     * Toàn bộ các `.then()` phía sau bị bỏ qua, luồng nhảy thẳng xuống hàm `.catch()` gần nhất!
  4. **Không return gì (hoặc return undefined):**
     * Trình duyệt tự động bọc thành `Promise.resolve(undefined)`. `.then()` tiếp theo nhận giá trị `undefined`.

* **Ví dụ mẫu chuẩn Senior:**
  ```javascript
  function fetchUser(id) {
      return Promise.resolve({ id, name: "Thịnh" });
  }
  function fetchOrders(userId) {
      return Promise.resolve(["Đơn hàng A", "Đơn hàng B"]);
  }

  fetchUser(10)
      .then(user => {
          console.log("User:", user.name);
          return fetchOrders(user.id); // Trả về Promise tiếp theo
      })
      .then(orders => {
          console.log("Danh sách đơn:", orders);
          return orders.length; // Trả về số nguyên thông thường
      })
      .then(count => {
          console.log(`Tổng cộng có ${count} đơn hàng.`);
      })
      .catch(err => {
          console.error("Lỗi tập trung:", err.message);
      });
  ```

</details>

---

### 5.2.5. BỘ TỨ PHƯƠNG THỨC KẾT HỢP (PROMISE COMBINATORS - SENIOR TOOLKIT)

<details open>
<summary><b>5. Chạy Song Song: all, allSettled, race & any</b> <i>(Bấm để xem)</i></summary>

Khi có nhiều tác vụ bất đồng bộ độc lập cần thực thi song song, đây là 4 vũ khí tối thượng:

| Phương thức | Chiến lược | Khi nào Resolved? | Khi nào Rejected? | Trường hợp ứng dụng thực tế |
| :--- | :--- | :--- | :--- | :--- |
| **`Promise.all`** | **Tất cả hoặc Không gì cả** *(Fail-Fast)* | Khi **TẤT CẢ** promise đều thành công. Trả về mảng kết quả theo đúng thứ tự mảng truyền vào. | Ngay khi có **1 promise ĐẦU TIÊN bị reject**. Các promise còn lại vẫn chạy ngầm nhưng kết quả bị vứt bỏ. | Tải dữ liệu tổng hợp cho trang Dashboard (User info + Cài đặt + Quyền hạn). Một cái lỗi là trang web không dùng được. |
| **`Promise.allSettled`** *(ES2020)* | **Kiên nhẫn tới cùng** *(Never Fails)* | Luôn luôn thành công khi **tất cả đã phân định** (dù thành công hay lỗi). Trả về mảng các Object: `{ status: "fulfilled", value }` hoặc `{ status: "rejected", reason }`. | **KHÔNG BAO GIỜ BỊ REJECT**! | Gửi hàng loạt email thông báo. Cái nào thành công thì ghi nhận, cái nào lỗi thì log lại để gửi lại sau. |
| **`Promise.race`** | **Đua tốc độ** *(Cuộc đua cá nhân)* | Lấy kết quả của **thằng đầu tiên về đích**, bất kể thằng đó thành công (`resolve`) hay thất bại (`reject`). | Ngay khi thằng đầu tiên về đích bị `reject`. | **Làm Network Timeout:** Đua giữa request gọi API và một hàm `setTimeout` đếm 5 giây báo hết giờ. |
| **`Promise.any`** *(ES2021)* | **Tìm người chiến thắng đầu tiên** | Ngay khi có **1 thằng ĐẦU TIÊN thành công (`resolve`)**. Bỏ qua mọi thằng bị lỗi trước đó. | Chỉ reject khi **TẤT CẢ đều bị lỗi**. Quăng ra lỗi tổng hợp `AggregateError`. | Gọi tải file ảnh từ nhiều cụm Server CDN khác nhau. Cụm nào trả về ảnh nhanh nhất thì lấy, mặc kệ các cụm kia bị lỗi kết nối. |

* **Code minh họa `Promise.allSettled`:**
  ```javascript
  const p1 = Promise.resolve("Thành công 1");
  const p2 = Promise.reject("Lỗi mạng ở server 2");
  const p3 = Promise.resolve("Thành công 3");

  Promise.allSettled([p1, p2, p3]).then(results => {
      results.forEach((res, index) => {
          if (res.status === "fulfilled") {
              console.log(`Tác vụ ${index}: Đạt kết quả:`, res.value);
          } else {
              console.warn(`Tác vụ ${index}: Bị từ chối vì:`, res.reason);
          }
      });
  });
  ```

</details>

---

### 5.2.6. BẪY PHỎNG VẤN SENIOR VỀ PROMISE

<details open>
<summary><b>6. Bẫy `.then(f1, f2)` vs `.then(f1).catch(f2)` & Unhandled Rejection</b> <i>(Bấm để xem)</i></summary>

* **Bẫy 1: Sự khác biệt chết người giữa `.then(f1, f2)` và `.then(f1).catch(f2)`:**
  ```javascript
  // CÁCH 1:
  promise.then(
      function f1(data) { throw new Error("Lỗi bên trong f1!"); },
      function f2(err) { console.error("Bắt được lỗi:", err); }
  );
  // Kết quả: f2 KHÔNG HỀ BẮT ĐƯỢC LỖI của f1! Lỗi bị văng ra ngoài làm sập app!
  // Vì f1 và f2 cùng nằm ngang hàng trong 1 mắt xích, f2 chỉ bắt lỗi của Promise ban đầu.

  // CÁCH 2 (CHUẨN SENIOR):
  promise
      .then(function f1(data) { throw new Error("Lỗi bên trong f1!"); })
      .catch(function f2(err) { console.error("Bắt được lỗi ngon lành:", err); });
  // Kết quả: Bắt được 100% mọi lỗi từ cả promise ban đầu LẪN lỗi phát sinh trong ruột f1!
  ```
* **Bẫy 2: "Promise Hell" (Lại thụt lề xiên sang phải vì không biết Chaining):**
  * Nhiều bạn chuyển từ Callback sang Promise nhưng vẫn giữ thói quen lồng:
    ```javascript
    // ❌ TƯ DUY SAI (PROMISE HELL):
    api1().then(res1 => {
        api2(res1).then(res2 => {
            api3(res2).then(res3 => console.log(res3));
        });
    });

    // ✅ TƯ DUY ĐÚNG (CHAINING PHẲNG):
    api1()
        .then(res1 => api2(res1))
        .then(res2 => api3(res2))
        .then(res3 => console.log(res3));
    ```

</details>

---

### 5.2.7. BẢNG TRA CỨU PROMISE (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Toàn Bộ Phương Thức Instance & Static Chuẩn ECMAScript</b> <i>(Bấm để xem)</i></summary>

* **`new Promise(executor)`:** Khởi tạo một thể hiện Promise mới.
* **`promise.then(onFulfilled, onRejected)`:** Đăng ký hàm xử lý khi Promise thành công hoặc thất bại, trả về Promise mới.
* **`promise.catch(onRejected)`:** Đăng ký hàm xử lý khi Promise thất bại (Tương đương `.then(null, onRejected)`).
* **`promise.finally(onFinally)`:** Đăng ký hàm luôn chạy khi Promise Settled, không nhận đối số.
* **`Promise.resolve(value)`:** Tạo nhanh một Promise đã ở trạng thái `Fulfilled` với giá trị cho trước.
* **`Promise.reject(reason)`:** Tạo nhanh một Promise đã ở trạng thái `Rejected` với lý do lỗi cho trước.
* **`Promise.all(iterable)`:** Chờ tất cả thành công, reject ngay khi có 1 thất bại.
* **`Promise.allSettled(iterable)`:** Chờ tất cả hoàn tất, không bao giờ reject, trả về mảng trạng thái chi tiết.
* **`Promise.race(iterable)`:** Nhận kết quả của Promise đầu tiên Settled (dù resolve hay reject).
* **`Promise.any(iterable)`:** Nhận kết quả của Promise đầu tiên Fulfilled, chỉ reject khi toàn bộ thất bại.

</details>

---

## 📌 BÀI 5.3: CÚ PHÁP HIỆN ĐẠI: `async / await` & TỐI ƯU LUỒNG BẤT ĐỒNG BỘ (ASYNC ORCHESTRATION)

---

### 5.3.1. BẢN CHẤT KIẾN TRÚC & CƠ CHẾ VẬN HÀNH CỦA `async / await` DƯỚI GÓC NHÌN V8 ENGINE

<details open>
<summary><b>1. Syntactic Sugar, Máy Trạng Thái (State Machine) & Vòng Đời Tạm Ngắt</b> <i>(Bấm để xem)</i></summary>

* **Bản chất kỹ thuật (Under the Hood):**
  * `async / await` **không phải là một cơ chế mới thay thế Promise**.
  * Bản chất nó là một **Lớp áo cú pháp (Syntactic Sugar)** được xây dựng trên sự kết hợp giữa **Generators (`function*` / `yield`)** và **Promises**.
  * Khi mã JavaScript được V8 biên dịch (Compile), mỗi hàm `async` được chuyển đổi thành một **Máy trạng thái hữu hạn (State Machine)**.
* **Từ khóa `async` (Cam kết kiểu trả về `Promise<T>`):**
  * Khi một hàm được gắn tiền tố `async`:
    * Nó **LUÔN LUÔN VÀ BẮT BUỘC trả về một Promise**, bất kể nội dung bên trong hàm return cái gì.
    * Nếu bạn `return value` (dạng nguyên thủy hoặc object thông thường): V8 tự động gói giá trị đó vào `Promise.resolve(value)`.
    * Nếu bạn `throw new Error(...)`: V8 tự động gói lỗi đó vào `Promise.reject(error)`.
* **Từ khóa `await` (Cơ chế tạm ngắt & Đánh thức - Suspend & Resume):**
  * Chỉ được phép xuất hiện bên trong hàm `async` (hoặc cấp cao nhất trong ES Modules - Top-Level Await).
  * **Cơ chế hoạt động:**
    1. Khi gặp biểu thức `await promise`, JS Engine **đóng băng ngữ cảnh thực thi (Execution Context)** của hàm `async` đó và lưu lại trạng thái các biến cục bộ.
    2. Hàm `async` tạm thời **nhường lại quyền chiếm dụng Call Stack** cho luồng chính (Main Thread), giúp trình duyệt tiếp tục xử lý tương tác người dùng hoặc render giao diện.
    3. Khi Promise mà `await` đang chờ chuyển trạng thái sang `Fulfilled`, một Microtask được đẩy vào **Microtask Queue** để báo hiệu V8 Engine đánh thức hàm `async`, khôi phục Call Stack Frame và tiếp tục chạy các dòng lệnh phía sau `await`.

</details>

---

### 5.3.2. MÔ HÌNH QUẢN TRỊ NGOẠI LỆ CHUYÊN SÂU (ADVANCED ERROR HANDLING)

<details open>
<summary><b>2. Khối try...catch Chuẩn Mực & Kỹ Thuật Golang-Style Tuple Handling</b> <i>(Bấm để xem)</i></summary>

* **1. Cấu trúc kinh điển: `try...catch...finally` với `async / await`:**
  * Khác với Callback, `try...catch` khi kết hợp với `await` có thể tóm gọn **100% mọi loại lỗi**: từ lỗi đồng bộ (lỗi cú pháp, truy xuất thuộc tính của `undefined`) đến lỗi bất đồng bộ (mất mạng, HTTP 500 từ Server).
  ```javascript
  async function fetchUserProfile(userId) {
      try {
          // Bắt đầu hiển thị trạng thái đang tải
          ui.showLoading();
          const response = await api.getUser(userId);
          return response.data;
      } catch (error) {
          // Xử lý lỗi tập trung tại một nơi duy nhất
          console.error("Lỗi khi tải thông tin người dùng:", error.message);
          ui.showNotification("Không thể tải dữ liệu, vui lòng thử lại sau!");
          throw error; // Re-throw nếu muốn hàm cha cấp cao hơn cũng nhận biết
      } finally {
          // Luôn luôn chạy để dọn dẹp tài nguyên (dù thành công hay lỗi)
          ui.hideLoading();
      }
  }
  ```

* **2. Kỹ thuật cao cấp: Golang-Style Tuple Error Handling (Loại bỏ Try-Catch Hell):**
  * Trong các ứng dụng lớn, việc bọc quá nhiều khối `try...catch` lồng nhau khiến code bị rối mắt.
  * Các kỹ sư Senior thường viết một hàm tiện ích nhỏ (thường gọi là `to` hoặc `safeAwait`) mô phỏng cú pháp xử lý lỗi của ngôn ngữ Go:
  ```javascript
  // Hàm trợ thủ bọc Promise trả về mảng Tuple [error, data]:
  const safeAwait = (promise) =>
      promise
          .then(data => [null, data])
          .catch(error => [error, null]);

  // Áp dụng thực tế: Code cực kỳ phẳng và sạch sẽ
  async function handleOrder(orderId) {
      const [orderErr, order] = await safeAwait(api.getOrder(orderId));
      if (orderErr) return ui.showError("Không tìm thấy đơn hàng!");

      const [payErr, receipt] = await safeAwait(api.processPayment(order.total));
      if (payErr) return ui.showError("Thanh toán thất bại!");

      ui.showSuccess("Đặt hàng thành công: " + receipt.id);
  }
  ```

</details>

---

### 5.3.3. NGHỆ THUẬT ĐIỀU PHỐI LUỒNG BẤT ĐỒNG BỘ (ASYNC ORCHESTRATION)

<details open>
<summary><b>3. Thác Nước (Waterfall Trap), Song Song (Concurrency) & Bẫy Vòng Lặp Mảng</b> <i>(Bấm để xem)</i></summary>

* **1. Cảnh giác Bẫy Thác Nước (Waterfall / Sequential Trap):**
  * **Sai lầm phổ biến:** Viết các lệnh `await` liên tiếp khi các tác vụ **hoàn toàn độc lập** với nhau:
    ```javascript
    // ❌ CHẬM (Tuần tự lãng phí): Tổng thời gian = 2s + 2s = 4 giây!
    const user = await fetchUser();       // Mất 2 giây
    const banners = await fetchBanners(); // Chờ thêm 2 giây nữa
    ```
  * **Giải pháp Senior (Chạy song song đồng thời):**
    ```javascript
    // ✅ NHANH (Song song tối ưu): Tổng thời gian chỉ bằng thời gian của tác vụ lâu nhất = 2 giây!
    const [user, banners] = await Promise.all([
        fetchUser(),
        fetchBanners()
    ]);
    ```

* **2. TỬ HUYỆT VÒNG LẶP: `forEach` vs `for...of` vs `Promise.all(map)`:**
  * Đây là một trong những câu hỏi phỏng vấn phân loại ứng viên gắt gao nhất:

  * **❌ BẪY CHẾT NGƯỜI VỚI `Array.prototype.forEach`:**
    ```javascript
    // HÀM NÀY KHÔNG HOẠT ĐỘNG NHƯ BẠN NGHĨ!
    async function updateUsers(userIds) {
        userIds.forEach(async (id) => {
            await api.deleteUser(id);
        });
        console.log("Xong hết rồi!"); // Dòng này in ra NGAY LẬP TỨC khi việc xóa CHƯA HỀ XONG!
    }
    ```
    * **Lý do:** Bản thân hàm `forEach` bên trong engine JS được viết hoàn toàn đồng bộ, nó không hề chờ đợi kết quả trả về của callback `async`!

  * **✅ GIẢI PHÁP 1: Chạy tuần tự từng phần tử (Dùng `for...of`):**
    * Thích hợp khi tác vụ sau cần chờ tác vụ trước (ví dụ: giới hạn số lượng request để không làm sập server):
    ```javascript
    for (const id of userIds) {
        await api.deleteUser(id); // Chờ xóa xong user này mới sang user tiếp theo
    }
    console.log("Đã xóa tuần tự từng user thành công!");
    ```

  * **✅ GIẢI PHÁP 2: Chạy song song toàn bộ mảng (Dùng `map` kết hợp `Promise.all`):**
    * Thích hợp khi muốn giải quyết hàng loạt tác vụ với tốc độ nhanh nhất:
    ```javascript
    // Biến mảng IDs thành mảng các Promise đang chạy song song:
    const deletePromises = userIds.map(id => api.deleteUser(id));
    // Đợi toàn bộ mảng hoàn thành đồng thời:
    await Promise.all(deletePromises);
    console.log("Đã xóa đồng thời tất cả user trong chớp mắt!");
    ```

</details>

---

### 5.3.4. BẪY PHỎNG VẤN SENIOR VỀ `async / await`

<details open>
<summary><b>4. Top-Level Await, Thứ Tự Event Loop & Lỗi Nuốt Exception</b> <i>(Bấm để xem)</i></summary>

* **Bẫy 1: Top-Level Await và nguy cơ nghẽn tải Module (Blocking Module Tree):**
  * Từ ES2022, bạn có thể dùng `await` trực tiếp ở tầng ngoài cùng của file mã nguồn (không cần bọc trong hàm `async`), **MIỄN LÀ file đó là một ES Module (`type="module"`)**.
  * **Cảnh báo Senior:** Một module dùng Top-Level Await sẽ **ngăn chặn toàn bộ cây module phụ thuộc (Module Graph)** nạp tiếp cho đến khi tác vụ đó hoàn tất. Chỉ dùng nó để nạp cấu hình khởi động sống còn (DB connection, config credentials), tuyệt đối không dùng cho các request mạng không chắc chắn!

* **Bẫy 2: Nuốt lỗi (Swallowed Errors) trong các hàm Async chạy ngầm (Fire-and-Forget):**
  ```javascript
  // Một hàm async bị gọi mà KHÔNG CÓ await cũng KHÔNG CÓ .catch():
  async function sendAnalytics() {
      throw new Error("Lỗi mạng phân tích!");
  }

  function handleButtonClick() {
      sendAnalytics(); // ❌ Không await!
      console.log("Xử lý click xong!");
  }
  ```
  * Khi hàm `sendAnalytics()` gặp lỗi, lỗi đó biến thành một **Unhandled Promise Rejection**. Trong Node.js, lỗi này có thể làm sụp đổ toàn bộ tiến trình ứng dụng (Crash Process)!
  * **Giải pháp:** Luôn luôn gắn `.catch()` nếu cố tình chạy ngầm: `sendAnalytics().catch(console.error);`.

</details>

---

### 5.3.5. BẢNG SO SÁNH TỔNG KẾT (PROMISE VS ASYNC/AWAIT)

<details open>
<summary><b>Bảng Đối Chiếu Tính Năng & Lời Khuyên Ứng Dụng Thực Chiến</b> <i>(Bấm để xem)</i></summary>

| Tiêu chí | Promise Thuần (`.then / .catch`) | `async / await` Hiện Đại |
| :--- | :--- | :--- |
| **Cú pháp** | Dựa trên Chuỗi Callbacks nối tiếp (Chaining). | Dạng phẳng tuần tự (Synchronous-looking style). |
| **Bắt lỗi** | Dùng phương thức `.catch()`. | Dùng khối lệnh tự nhiên `try...catch...finally`. |
| **Chia sẻ biến** | Phức tạp, phải truyền biến qua nhiều mắt xích `.then()` hoặc khai báo biến phạm vi rộng bên ngoài. | Rất dễ, mọi biến đều nằm trong cùng một phạm vi hàm (Scope). |
| **Tác vụ song song** | Bản năng mạnh mẽ (`Promise.all`, `Promise.race`). | Phải kết hợp cùng `Promise.all` (`await Promise.all(...)`). |
| **Khuyên dùng** | Dùng khi cần xử lý song song độc lập hoặc tạo các chuỗi tiện ích Functional Reactive. | Dùng làm chuẩn mặc định cho 95% logic luồng nghiệp vụ trong mã nguồn dự án. |

</details>

---

## 📌 BÀI 5.4: GIAO TIẾP MẠNG & FETCH API (HTTP REQUESTS & NETWORK RESILIENCE)

---

### 5.4.1. KIẾN TRÚC CLIENT-SERVER & VÒNG ĐỜI GIAO TIẾP HTTP

<details open>
<summary><b>1. Bản Chất Chu Kỷ Request - Response & Cấu Trúc Gói Tin</b> <i>(Bấm để xem)</i></summary>

* **Bản chất giao tiếp Web:** Là kiến trúc Không lưu trạng thái (Stateless) dựa trên giao thức HTTP/HTTPS. Mọi tương tác diễn ra theo chu kỳ 2 chiều:
  ```text
  [ CLIENT (Trình duyệt) ] ───────── HTTP Request (Phương thức, URL, Headers, Body) ─────────► [ SERVER ]
                          ◄──────── HTTP Response (Status Code, Headers, Data Body) ─────────
  ```
* **Cấu trúc gói tin HTTP:**
  1. **Start Line:** Chứa HTTP Method (`GET`, `POST`, ...) và Đường dẫn Endpoint (`/api/v1/users`).
  2. **Headers (Tiêu đề):** Metadata chứa thông tin ngữ cảnh:
     * `Content-Type: application/json` (Báo kiểu dữ liệu trong Body).
     * `Authorization: Bearer <token>` (Vé xác thực danh tính người dùng).
     * `Accept: application/json` (Kỳ vọng kiểu dữ liệu Server trả về).
  3. **Body (Thân gói tin):** Dữ liệu truyền tải thực tế (Payload), thường là chuỗi JSON mã hóa UTF-8 (chỉ có ở `POST`, `PUT`, `PATCH`).

</details>

---

### 5.4.2. CƠ CHẾ LỜI HỨA HAI BƯỚC (TWO-STEP PROMISE) CỦA FETCH

<details open>
<summary><b>2. Mổ Xẻ Quá Trình Nhận Headers & Đọc Stream Body</b> <i>(Bấm để xem)</i></summary>

* Hàm `fetch(url, options)` không trả về dữ liệu ngay lập tức mà trải qua **2 giai đoạn Promise nối tiếp**:
  ```javascript
  // GIAI ĐOẠN 1: Chờ phản hồi HTTP Headers từ Server
  const response = await fetch("https://api.example.com/products");

  // GIAI ĐOẠN 2: Tiêu thụ luồng dữ liệu (ReadableStream) và chuyển đổi định dạng
  const data = await response.json();
  ```
* **Tại sao phải chia làm 2 giai đoạn?**
  * Server có thể gửi về một file dữ liệu khổng lồ (vài chục MB).
  * Giai đoạn 1 giúp trình duyệt biết ngay Server có phản hồi hay không, mã HTTP là gì, kiểu file là gì mà **không cần đợi tải hết toàn bộ file về RAM**.
  * Giai đoạn 2 đọc luồng nhị phân (`ReadableStream`) truyền dần về qua card mạng và phân tích cú pháp (Parsing).
* **Bộ 5 phương thức giải mã Body của đối tượng Response:**
  * **`response.json()`:** Đọc stream và giải mã thành JavaScript Object/Array.
  * **`response.text()`:** Đọc stream thành chuỗi văn bản thô (HTML, XML, CSV).
  * **`response.blob()`:** Đọc stream thành dữ liệu nhị phân không đổi (Binary Large Object - dùng cho tải ảnh, file PDF, âm thanh).
  * **`response.formData()`:** Đọc dữ liệu gửi từ Form nhiều trường (`multipart/form-data`).
  * **`response.arrayBuffer()`:** Đọc dữ liệu nhị phân cấp thấp thô để xử lý đồ họa WebGL hoặc WebAssembly.

</details>

---

### 5.4.3. TỬ HUYỆT CỦA FETCH & CƠ CHẾ KIỂM TRA `response.ok`

<details open>
<summary><b>3. Bản Chất Lỗi Mạng vs Lỗi Logic 404/500 Của Server</b> <i>(Bấm để xem)</i></summary>

* **TỬ HUYỆT KINH ĐIỂN CỦA `fetch()`:**
  * Khác với các thư viện bên ngoài như Axios, **`fetch()` KHÔNG BAO GIỜ REJECT PROMISE khi Server trả về mã lỗi HTTP 404 (Not Found), 401 (Unauthorized) hay 500 (Server Error)!**
  * Dưới góc nhìn của tầng mạng: Miễn là máy chủ phản hồi (dù phản hồi là thông báo lỗi), kết nối mạng vẫn được xem là thành công mỹ mãn (`Resolved`).
* **Khi nào `fetch()` MỚI THỰC SỰ BỊ REJECT?**
  * Rút dây mạng / mất Wifi hoàn toàn (Offline Network Failure).
  * Sai địa chỉ máy chủ / DNS không phân giải được tên miền.
  * Bị chặn bởi chính sách bảo mật chia sẻ tài nguyên gốc chéo (**CORS Error**).
  * Bị ngắt tín hiệu chủ động bởi **`AbortController`**.
* **Chuẩn mực kiểm tra `response.ok` (Bắt buộc trong mọi codebase Senior):**
  * Thuộc tính `response.ok` là cờ Boolean:
    * `true`: Nếu `response.status` nằm trong khoảng **`200 → 299`**.
    * `false`: Nếu `response.status` nằm ngoài dải trên (4xx, 5xx).
  ```javascript
  async function apiRequest(url) {
      const response = await fetch(url);

      // 🛡️ TẤM KHIÊN BẢO VỆ CHỐNG LỖI CÚ PHÁP JSON:
      if (!response.ok) {
          throw new Error(`HTTP Error ${response.status}: ${response.statusText}`);
      }

      return await response.json();
  }
  ```

</details>

---

### 5.4.4. BỘ PHƯƠNG THỨC HTTP & CẤU HÌNH REQUEST NÂNG CAO

<details open>
<summary><b>4. Phân Biệt CRUD Methods, Headers & Cấu Hình Bảo Mật</b> <i>(Bấm để xem)</i></summary>

* **Bảng chuẩn quy ước Restful API:**

| HTTP Method | Ý nghĩa CRUD | Có Body không? | Tính lũy đẳng (Idempotent) | Ví dụ ứng dụng |
| :--- | :--- | :--- | :--- | :--- |
| **`GET`** | **Read** (Đọc) | **Không** | Có (Gọi 10 lần kết quả Server không đổi) | Lấy danh sách sản phẩm, xem chi tiết bài viết. |
| **`POST`** | **Create** (Tạo mới) | **Có** | Không (Gọi 2 lần sinh ra 2 bản ghi mới) | Đăng ký tài khoản, gửi đơn hàng, tạo note. |
| **`PUT`** | **Update** (Thay thế) | **Có** | Có (Đè toàn bộ bản ghi cũ bằng bản ghi mới) | Cập nhật toàn bộ thông tin User Profile. |
| **`PATCH`** | **Modify** (Sửa 1 phần) | **Có** | Không / Tùy biến | Chỉ đổi trạng thái `isPinned: true` của note. |
| **`DELETE`** | **Delete** (Xóa) | Thường không | Có (Xóa 1 lần hay 10 lần thì bản ghi vẫn mất) | Xóa một tờ ghi chú theo ID. |

* **Mẫu cấu hình Request hoàn chỉnh (POST / PUT / PATCH):**
  ```javascript
  async function createNote(newNoteData) {
      const response = await fetch("https://api.example.com/api/notes", {
          method: "POST", // Phương thức HTTP
          headers: {
              // Báo định dạng dữ liệu gửi lên:
              "Content-Type": "application/json",
              // Báo định dạng mong muốn nhận về:
              "Accept": "application/json",
              // Vé thông hành xác thực JWT:
              "Authorization": "Bearer eyJhbGciOi..."
          },
          // Chế độ gửi Cookie kèm theo (CORS):
          credentials: "include", // 'omit' | 'same-origin' | 'include'
          // Ép Object thành chuỗi JSON:
          body: JSON.stringify(newNoteData)
      });

      if (!response.ok) {
          const errorDetail = await response.json().catch(() => ({}));
          throw new Error(errorDetail.message || "Tạo ghi chú thất bại!");
      }

      return await response.json();
  }
  ```

</details>

---

### 5.4.5. KIỂM SOÁT VÒNG ĐỜI REQUEST VỚI `AbortController`

<details open>
<summary><b>5. Hủy Bỏ Request, Ngăn Ngừa Race Condition & Network Timeout</b> <i>(Bấm để xem)</i></summary>

* **Vấn đề thực tế 1: Cuộc đua tranh chấp dữ liệu (Race Condition trong Tìm kiếm / Filter):**
  * Người dùng gõ nhanh chữ `"a"` → Request A bay đi (mất 2s để Server trả lời).
  * Người dùng gõ tiếp chữ `"ab"` → Request B bay đi (mất 0.5s để Server trả lời).
  * Request B về trước, giao diện hiện dữ liệu của `"ab"`. Sau đó Request A mới về tới, **đè ngược dữ liệu cũ của `"a"` lên màn hình!**
  * **Giải pháp:** Khi người dùng gửi Request mới, dùng `AbortController` hủy ngay lập tức Request cũ đang bay lơ lửng!
* **Vấn đề thực tế 2: Giới hạn thời gian phản hồi (Network Timeout):**
  * Mặc định `fetch` không có thuộc tính `timeout`. Nếu mạng lag, nó có thể treo đến 2 phút.
* **Cú pháp sử dụng `AbortController`:**
  ```javascript
  let searchController = null;

  async function handleSearch(keyword) {
      // 1. Hủy request cũ nếu vẫn đang chạy:
      if (searchController) {
          searchController.abort(); // Hủy request trước!
      }

      // 2. Tạo controller mới cho lượt tìm kiếm này:
      searchController = new AbortController();

      try {
          const res = await fetch(`/api/search?q=${encodeURIComponent(keyword)}`, {
              signal: searchController.signal // Gắn ăng-ten tín hiệu
          });
          if (!res.ok) throw new Error("Lỗi tìm kiếm");
          const results = await res.json();
          renderSearchResults(results);
      } catch (err) {
          // Bỏ qua lỗi do chủ động hủy, chỉ log lỗi mạng thật sự:
          if (err.name === "AbortError") {
              console.log("Đã hủy request tìm kiếm cũ để ưu tiên từ khóa mới.");
          } else {
              console.error("Lỗi tìm kiếm:", err.message);
          }
      }
  }
  ```
* **Cú pháp Timeout hiện đại chuẩn ES2022 (`AbortSignal.timeout`):**
  ```javascript
  // Tự động ngắt request sau 5000ms:
  const res = await fetch("/api/data", {
      signal: AbortSignal.timeout(5000)
  });
  ```

</details>

---

### 5.4.6. BẪY PHỎNG VẤN SENIOR VỀ FETCH & NETWORK

<details open>
<summary><b>6. Bẫy Body Đã Đọc (bodyUsed), CORS Preflight & Token Refresh Interceptor</b> <i>(Bấm để xem)</i></summary>

* **Bẫy 1: Tử huyệt "Body stream already read" (`bodyUsed` Trap):**
  * Luồng dữ liệu của `Response` trong JavaScript là một `ReadableStream` một chiều. Một khi đã đọc qua một lần thì con trỏ stream đã đi đến cuối và dữ liệu bị tiêu thụ sạch khỏi bộ nhớ tạm.
  ```javascript
  const res = await fetch("/api/data");
  const text = await res.text();
  const json = await res.json(); // ❌ LỖI NGAY: TypeError: Failed to execute 'json' on 'Response': body stream already read!
  ```
  * **Cách khắc phục:** Nếu cần đọc dữ liệu 2 lần (ví dụ vừa log text vừa parse json), phải sao chép stream trước bằng hàm `res.clone()`:
    ```javascript
    const resCopy = res.clone();
    const text = await res.text();
    const json = await resCopy.json(); // ✅ Hoạt động bình thường
    ```

* **Bẫy 2: Tại sao trình duyệt lại tự động bắn 1 request `OPTIONS` (CORS Preflight)?**
  * Khi bạn gửi một request phức tạp (dùng method `PUT/DELETE` hoặc thêm custom headers như `Authorization`, `Content-Type: application/json` sang một tên miền khác Domain/Port):
  * Trình duyệt tự động gửi một gói tin mồi gọi là **Preflight Request (Method: `OPTIONS`)** lên Server trước để thăm dò: *"Này máy chủ, tao chuẩn bị gửi dữ liệu dạng JSON qua, mày có cho phép không?"*.
  * Chỉ khi Server phản hồi `204 No Content` hoặc `200 OK` kèm các header cấp phép (`Access-Control-Allow-Origin`, `Access-Control-Allow-Methods`), trình duyệt mới thực sự bắn request thật đi!

</details>

---

### 5.4.7. BẢNG TRA CỨU FETCH API (W3SCHOOLS REFERENCE)

<details open>
<summary><b>Toàn Bộ Thuộc Tính & Phương Thức Của Fetch, Response & Request</b> <i>(Bấm để xem)</i></summary>

* **`fetch(resource, [init])`:** Bắt đầu tiến trình lấy tài nguyên mạng, trả về một Promise phân định thành đối tượng `Response`.
* **Thuộc tính đối tượng `Response`:**
  * **`response.ok`:** Boolean kiểm tra mã HTTP có nằm trong khoảng `200 - 299` hay không.
  * **`response.status`:** Mã trạng thái số nguyên HTTP (ví dụ: `200`, `201`, `400`, `404`, `500`).
  * **`response.statusText`:** Chuỗi văn bản trạng thái (ví dụ: `"OK"`, `"Not Found"`).
  * **`response.headers`:** Đối tượng `Headers` chứa toàn bộ metadata phản hồi từ máy chủ.
  * **`response.url`:** URL cuối cùng của phản hồi (sau khi trải qua các chuyển hướng Redirect).
  * **`response.bodyUsed`:** Cờ Boolean cho biết luồng dữ liệu Body đã bị đọc hay chưa.
* **Phương thức giải mã đối tượng `Response`:**
  * **`response.json()`:** Trả về Promise phân định thành JavaScript Object.
  * **`response.text()`:** Trả về Promise phân định thành chuỗi văn bản UTF-8.
  * **`response.blob()`:** Trả về Promise phân định thành đối tượng Binary Blob.
  * **`response.clone()`:** Tạo ra một bản sao nguyên vẹn của đối tượng Response với stream độc lập.
* **Đối tượng điều khiển tín hiệu:**
  * **`new AbortController()`:** Khởi tạo bộ điều khiển hủy request.
  * **`controller.abort(reason)`:** Phát tín hiệu ngắt ngay lập tức mọi request đang lắng nghe signal.
  * **`AbortSignal.timeout(ms)`:** Phương thức tĩnh tạo signal tự hủy sau khoảng thời gian chỉ định.

</details>






















